<rss version="0.92"><channel><item>
            <title>[LU-20350] o2iblnd: NIC-local bounce pool to avoid cross-NUMA bulk RDMA on single-HCA clients</title>
                <link>https://jira.whamcloud.com/browse/LU-20350</link>
                <project id="10000" key="LU">Lustre</project>
                    <description>&lt;h3&gt;&lt;a name="Problem"&gt;&lt;/a&gt;Problem&lt;/h3&gt;

&lt;p&gt;On a dual-socket client the HCA is attached to one socket's PCIe root complex. Any RDMA the device performs against memory on the other socket crosses the inter-socket link (UPI/xGMI). Without PCIe Relaxed Ordering that path serializes toward single-outstanding-write behavior and consumes inter-socket bandwidth shared with the CPUs.&lt;/p&gt;

&lt;p&gt;The cost is direction-asymmetric and large. On ConnectX mlx5 (RO off, fio bs=1m numjobs=32 iodepth=64), a NUMA-remote read sink runs at 2036 MiB/s against 9291 MiB/s for a NUMA-local sink: a cross-socket HCA-write penalty of roughly 4.5x. Writes (HCA reads) are barely affected (about 5%).&lt;/p&gt;

&lt;p&gt;Lustre bulk lands in the application's buffer, placed wherever the job's thread ran, so the HCA frequently DMAs cross-socket.&lt;/p&gt;
&lt;h3&gt;&lt;a name="Whytheexistingleverisnotenough"&gt;&lt;/a&gt;Why the existing lever is not enough&lt;/h3&gt;

&lt;p&gt;On a multi-rail client this is already mitigated. The client bulk payload is passive and connection-bound: the server drives the RDMA over the connection the request arrived on, targeting the client NID (`rq_source`) the request RPC egressed on. Stock LNet steers that egress by the bulk buffer's NUMA node, because the request MD carries `LNET_MD_BULK_HANDLE` and `lnet_cpt_of_md()` → `lnet_get_first_page()` follow the bulk handle to rank the egress NI by the bulk buffer's first page. With a healthy buffer-local rail, the bulk already lands NIC-local.&lt;/p&gt;

&lt;p&gt;That lever cannot help two cases:&lt;/p&gt;

&lt;p&gt;1. A single-HCA client has no buffer-local rail to select; the one connection's HCA crosses sockets for any remote buffer. This is exactly the case `numactl` also cannot fix, since you cannot move the NIC.&lt;br/&gt;
2. No &lt;b&gt;{&lt;/b&gt;}healthy buffer-local rail exists (local rail down or degraded).&lt;/p&gt;
&lt;h3&gt;&lt;a name="Proposedsolution"&gt;&lt;/a&gt;Proposed solution&lt;/h3&gt;

&lt;p&gt;Add an optional NIC-local bounce pool inside ko2iblnd. Stage the bulk through a per-HCA, pre-registered arena allocated on the HCA's own NUMA node, and pay a CPU copy for the last hop. A cross-socket CPU copy is cheaper than a cross-socket NIC DMA and is immune to the PCIe ordering penalty, so one mechanism addresses both costs. The bounce is confined to the LND: transparent to ptlrpc/osc/llite and to the server, client-only, with no wire-protocol change.&lt;/p&gt;
&lt;ul class="alternate" type="square"&gt;
	&lt;li&gt;Inbound (read / PUT sink): advertise a NIC-local slot's `REMOTE_WRITE` rkey as the sink; the peer RDMA-writes into it; copy-out runs off the CQ poller on an HCA-CPT-bound workqueue, with `lnet_finalize` deferred behind the copy.&lt;/li&gt;
	&lt;li&gt;Outbound (write / GET source): copy-in inline, then the device DMAs from the NIC-local slot. Measured at -16% on writes (FAR 20.0 → 16.7 GiB/s) because HCA reads are barely socket-penalized, so the added copy-in costs more than it saves. Outbound is therefore opt-in (FORCE) only; AUTO never engages it.&lt;/li&gt;
&lt;/ul&gt;


&lt;h3&gt;&lt;a name="Engagementandsafety"&gt;&lt;/a&gt;Engagement and safety&lt;/h3&gt;
&lt;ul class="alternate" type="square"&gt;
	&lt;li&gt;OFF by default. `bounce_enable` is tri-state (`0` off / `1` force / `2` auto). AUTO allocates the arena only on a multi-node NUMA box and engages the inbound path only when the buffer is NUMA-remote from the HCA, so local I/O stays zero-copy.&lt;/li&gt;
	&lt;li&gt;Slot exhaustion or any rd-build failure falls back to the direct app buffer; a bounce can never fail an I/O.&lt;/li&gt;
	&lt;li&gt;Slots are recycled across transfers, so a non-conformant peer that under-writes yet reports success could leak prior-transfer bytes in the untransferred tail. Each slot is zeroed on release (fused into the inbound copy-out while cache-hot); gated by `bounce_scrub`, default on.&lt;/li&gt;
	&lt;li&gt;At `bounce_enable=0` the datapath is byte-for-byte identical to stock; the two bulk rd-build sites add count-only predicate math and nothing reroutes.&lt;/li&gt;
&lt;/ul&gt;


&lt;h4&gt;&lt;a name="Tunables%28ko2iblndmoduleparams%29"&gt;&lt;/a&gt;Tunables (ko2iblnd module params)&lt;/h4&gt;
&lt;div class="preformatted panel" style="border-width: 1px;"&gt;&lt;div class="preformattedContent panelContent"&gt;
&lt;pre&gt;parm:           bounce_stats:NIC-local bounce pool counters. Read to dump. Any write resets the cumulative counters (hits misses fallback copyout_wq copyout_inline bytes would_* local_*) for a fresh window without rmmod; NOT reset: the live inflight gauge (leak detector) and the per-pool exhaustion latch.
parm:           bounce_enable:NIC-local bounce pool: 0=off (default), 1=force-on (both directions, allocate even on single-socket; testing), 2=auto (inbound/read only - outbound is a measured cross-socket loss - on if multi-node NUMA); invalid -&amp;gt; off
parm:           bounce_pool_mb:bounce pool size per HCA, MiB (int)
parm:           bounce_slot_kb:bounce slot size, KiB (int)
parm:           bounce_min_nob:min transfer size to consider bouncing, bytes (default 64 KiB: below this a copy+FastReg costs more than the cross-socket DMA it replaces) (int)
parm:           bounce_dist_thresh:engage when node_distance(page, hca) exceeds this (default LOCAL_DISTANCE) (int)
parm:           bounce_bp_node:override HCA NUMA node for the pool (-1 autodetect) (int)
parm:           bounce_scrub:zero NIC-local bounce slots on release to prevent short-write residual disclosure (1=on, default; 0=off for the SEC-1a negative-control test only) (int)
parm:           bounce_copyout_max_active:inbound copy-out workqueue concurrency per HCA CPT (default 2 = the Group 6 knee; 1 = serialized; &amp;gt;2 = marginal/no gain) (int) &lt;/pre&gt;
&lt;/div&gt;&lt;/div&gt;
&lt;h3&gt;&lt;a name="Testingandresults"&gt;&lt;/a&gt;Testing and results&lt;/h3&gt;

&lt;p&gt;Functional gates on SoftRoCE (byte-exact read+write, exhaustion fallback, scrub invariant via `fail_loc=0xe012`, clean teardown), all passing.&lt;/p&gt;

&lt;p&gt;Performance on real ConnectX mlx5 (mlx5_1 on NUMA node 1, RO off, fio bs=1m numjobs=32 iodepth=64), shipping fused+scrub build:&lt;/p&gt;
&lt;ul class="alternate" type="square"&gt;
	&lt;li&gt;Inbound read, NUMA-remote sink: 2036 → 7422 MiB/s with the bounce engaged, a 3.65x gain that recovers to about 80% of the NUMA-local ceiling (9291 MiB/s). Copy-out confirmed off the CQ poller (copyout_wq counter tracks engagement 1:1). The residual gap versus local is the inherent 1 MiB slot zero-write that the cache-hot fusion cannot remove (about 8%).&lt;/li&gt;
	&lt;li&gt;No regression when local: with a NUMA-local sink, AUTO matches baseline (9312 vs 9291 MiB/s) with engagement count 0, so the topology predicate correctly stays out of the way.&lt;/li&gt;
	&lt;li&gt;No regression on writes: with AUTO and a NUMA-remote write buffer, write bandwidth matches baseline (19.6 vs 20.1 GiB/s) with engagement count 0, confirming AUTO never touches the outbound path.&lt;/li&gt;
&lt;/ul&gt;
</description>
                <environment />
        <key id="98018">LU-20350</key>
            <summary>o2iblnd: NIC-local bounce pool to avoid cross-NUMA bulk RDMA on single-HCA clients</summary>
                <type id="2" iconUrl="https://jira.whamcloud.com/secure/viewavatar?size=xsmall&amp;avatarId=11311&amp;avatarType=issuetype">New Feature</type>
                                            <priority id="10104" iconUrl="https://jira.whamcloud.com/images/icons/priorities/medium.svg">Medium</priority>
                        <status id="3" iconUrl="https://jira.whamcloud.com/images/icons/statuses/inprogress.png" description="This issue is being actively worked on at the moment by the assignee.">In Progress</status>
                    <statusCategory id="4" key="indeterminate" colorName="inprogress" />
                                    <resolution id="-1">Unresolved</resolution>
                                        <assignee username="hornc">Chris Horn</assignee>
                                    <reporter username="hornc">Chris Horn</reporter>
                        <labels>
                    </labels>
                <created>Wed, 10 Jun 2026 15:59:07 +0000</created>
                <updated>Fri, 12 Jun 2026 02:28:12 +0000</updated>
                                                                                <due />
                            <votes>0</votes>
                                    <watches>3</watches>
                                                                            <comments>
                            <comment id="495958" author="gerrit" created="Wed, 10 Jun 2026 20:10:04 +0000">&lt;p&gt;"Chris Horn &amp;lt;chorn@ddn.com&amp;gt;" uploaded a new patch: &lt;a href="https://review.whamcloud.com/c/fs/lustre-release/+/66588" class="external-link" target="_blank" rel="nofollow noopener"&gt;https://review.whamcloud.com/c/fs/lustre-release/+/66588&lt;/a&gt;&lt;br/&gt;
Subject: &lt;a href="https://jira.whamcloud.com/browse/LU-20350" title="o2iblnd: NIC-local bounce pool to avoid cross-NUMA bulk RDMA on single-HCA clients" class="issue-link" data-issue-key="LU-20350"&gt;LU-20350&lt;/a&gt; o2iblnd: Bounce cross-NUMA bulk via NIC-local pool&lt;br/&gt;
Project: fs/lustre-release&lt;br/&gt;
Branch: master&lt;br/&gt;
Current Patch Set: 1&lt;br/&gt;
Commit: ba8317e42896173a334e11e58d303be3fa140733&lt;/p&gt;</comment>
                    </comments>
                    <attachments>
                    </attachments>
                <subtasks>
                    </subtasks>
                <customfields>
                                                                                                                                                                <customfield id="customfield_10890" key="com.atlassian.jira.plugins.jira-development-integration-plugin:devsummary">
                        <customfieldname>Development</customfieldname>
                        <customfieldvalues>
                            
                        </customfieldvalues>
                    </customfield>
                                                                                                                                                                                                                                                                                                                                                        <customfield id="customfield_10390" key="com.pyxis.greenhopper.jira:gh-lexo-rank">
                        <customfieldname>Rank</customfieldname>
                        <customfieldvalues>
                            <customfieldvalue>1|i06v73:</customfieldvalue>

                        </customfieldvalues>
                    </customfield>
                                                                <customfield id="customfield_10090" key="com.pyxis.greenhopper.jira:gh-global-rank">
                        <customfieldname>Rank (Obsolete)</customfieldname>
                        <customfieldvalues>
                            <customfieldvalue>9223372036854775807</customfieldvalue>
                        </customfieldvalues>
                    </customfield>
                                                                                            <customfield id="customfield_10060" key="com.atlassian.jira.plugin.system.customfieldtypes:select">
                        <customfieldname>Severity</customfieldname>
                        <customfieldvalues>
                                <customfieldvalue key="10022">3</customfieldvalue>

                        </customfieldvalues>
                    </customfield>
                                                                                                                                                                                                                                                                                                                                                        </customfields>
    </item></channel></rss>
<rss version="0.92"><channel><item>
            <title>[LU-20632] libcfs and obdclass size several CPU-id-indexed arrays by num_possible_cpus(), so a sparse cpu_possible_mask overflows them</title>
                <link>https://jira.whamcloud.com/browse/LU-20632</link>
                <project id="10000" key="LU">Lustre</project>
                    <description>&lt;p&gt;I am mostly filing this as a curiosity noted by Claude reviewing other code since I don't think anything could be triggerigntihs now. But future might change.&lt;/p&gt;

&lt;p&gt;I also have patches for it but not sure if I'll post them.&lt;/p&gt;

&lt;p&gt;Below is AI generated content:&lt;/p&gt;

&lt;p&gt;   * On x86 (including Xen PV), sparc, mips and PowerPC the possible mask is&lt;br/&gt;
    dense by construction, so those platforms cannot hit any of these&lt;br/&gt;
    defects at all.&lt;/p&gt;
&lt;ul&gt;
	&lt;li&gt;arm64 CAN produce a hole (mechanism quoted below), but no real machine&lt;br/&gt;
    that does was found or demonstrated, and no failure has been reported.&lt;/li&gt;
	&lt;li&gt;If the precondition does occur, defect 4 is unbounded heap corruption on&lt;br/&gt;
    the debug fast path of libcfs.ko - a spin_lock() and a list_add() on&lt;br/&gt;
    memory past the end of the allocation, on every CDEBUG/CERROR issued&lt;br/&gt;
    from a CPU whose id is &amp;gt;= cpumask_weight(cpu_possible_mask).  libcfs.ko&lt;br/&gt;
    is loaded by every Lustre module on both client and server, so this is&lt;br/&gt;
    the broadest and hottest of the four; that is why the ticket is Major&lt;br/&gt;
    rather than Minor.&lt;/li&gt;
	&lt;li&gt;Defects 1 and 2 are also out-of-bounds heap writes on normal paths - one&lt;br/&gt;
    at every obdclass module load, one on the lprocfs stats path.  Defect 3&lt;br/&gt;
    is an LBUG, but it needs an allocation failure on top of the sparse&lt;br/&gt;
    mask.&lt;br/&gt;
Reproduced: NO.  Demonstrated statically only; see "Steps to reproduce".&lt;br/&gt;
Runtime testing on x86 (4 CPUs, cpu_possible_mask = 0-3, KASAN kernel)&lt;br/&gt;
exercised the patched code but cannot exercise the defect, because with a&lt;br/&gt;
dense mask nr_cpu_ids == num_possible_cpus() and the fixes are no-ops there.&lt;br/&gt;
For defect 4 that run was libcfs loaded with debug=-1 and debug_mb=200, I/O&lt;br/&gt;
pinned to each of the four CPUs, repeated "lctl dk" drains and a&lt;br/&gt;
lustre_rmmod: no KASAN report, no "trace pages aren't empty", and the&lt;br/&gt;
debug_mb round-trip still reported the same value as before the patch,&lt;br/&gt;
confirming the new walk visits exactly num_possible_cpus() tcds per type.&lt;br/&gt;
That demonstrates no regression, not the defect.&lt;/li&gt;
&lt;/ul&gt;


&lt;p&gt;Environment:&lt;br/&gt;
master, 2.17.57.  All four defects are long-standing; see "Introduced by".&lt;/p&gt;

&lt;p&gt;Description:&lt;/p&gt;

&lt;p&gt;All four come from the same mistake: cpu_possible_mask is treated as if its&lt;br/&gt;
set bits were 0..N-1, so an array indexed by CPU &lt;b&gt;id&lt;/b&gt; is sized by the &lt;b&gt;count&lt;/b&gt;&lt;br/&gt;
of possible CPUs.&lt;/p&gt;

&lt;p&gt;DEFECT 1 - out-of-bounds write into cl_envs[]  (fires at module load)&lt;/p&gt;

&lt;div class="preformatted panel" style="border-width: 1px;"&gt;&lt;div class="preformattedContent panelContent"&gt;
&lt;pre&gt;  lustre/obdclass/cl_object.c:1160 (cl_global_init())
      OBD_ALLOC_PTR_ARRAY(cl_envs, num_possible_cpus());
&lt;/pre&gt;
&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;  num_possible_cpus() is cpumask_weight(cpu_possible_mask)&lt;br/&gt;
  (include/linux/cpumask.h:1111) and OBD_ALLOC_PTR_ARRAY(ptr, n) allocates&lt;br/&gt;
  exactly n elements (lustre/include/obd_support.h:868).  Every use indexes&lt;br/&gt;
  by CPU id:&lt;/p&gt;

&lt;div class="preformatted panel" style="border-width: 1px;"&gt;&lt;div class="preformattedContent panelContent"&gt;
&lt;pre&gt;      cl_object.c:974-976   cl_envs[i] inside for_each_possible_cpu(i)
      cl_object.c:756-780   cl_envs[cpu], cpu = get_cpu()   (cl_env_get)
      cl_object.c:930-938   cl_envs[cpu], cpu = smp_processor_id()
      cl_object.c:879-893   cl_envs[i] inside for_each_possible_cpu(i)
&lt;/pre&gt;
&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;  With possible = &lt;/p&gt;
{0, 2, 3} the array holds 3 entries but&lt;br/&gt;
  cl_env_percpu_init() writes cl_envs&lt;span class="error"&gt;&amp;#91;3&amp;#93;&lt;/span&gt; during module load - a heap&lt;br/&gt;
  overflow on the success path, with no allocation failure required.&lt;br/&gt;
  cl_global_fini() then frees the wrong size too (:1184, :1205).&lt;br/&gt;
&lt;br/&gt;
DEFECT 2 - out-of-bounds access on lprocfs ls_percpu[]  (fires on the stats&lt;br/&gt;
path, the hottest of the three)&lt;br/&gt;
&lt;br/&gt;
  lustre/obdclass/lprocfs_status.c:1445/1448 (lprocfs_stats_alloc())&lt;br/&gt;
      num_entry = num_possible_cpus();&lt;br/&gt;
      LIBCFS_ALLOC(stats, offsetof(typeof(*stats), ls_percpu&lt;span class="error"&gt;&amp;#91;num_entry&amp;#93;&lt;/span&gt;));&lt;br/&gt;
&lt;br/&gt;
  but the array is indexed by CPU id:&lt;br/&gt;
      lprocfs_status.c:540-542  ls_percpu&lt;span class="error"&gt;&amp;#91;cpuid&amp;#93;&lt;/span&gt;, cpuid = get_cpu(), read on&lt;br/&gt;
                                every stats update via&lt;br/&gt;
                                lprocfs_stats_lock(LPROCFS_GET_SMP_ID)&lt;br/&gt;
      lprocfs_status.c:1407-11  lprocfs_stats_alloc_one() writes&lt;br/&gt;
                                ls_percpu&lt;span class="error"&gt;&amp;#91;cpuid&amp;#93;&lt;/span&gt; the first time a CPU is seen&lt;br/&gt;
      lprocfs_status.h:500      lprocfs_stats_counter_get() dereferences&lt;br/&gt;
                                ls_percpu&lt;span class="error"&gt;&amp;#91;cpuid&amp;#93;&lt;/span&gt;&lt;br/&gt;
  and the iteration bound is also in id space: lprocfs_stats_alloc_one()&lt;br/&gt;
  sets ls_biggest_alloc_num = cpuid + 1, which LPROCFS_GET_NUM_CPU returns,&lt;br/&gt;
  so lprocfs_stats_collect() (:614), lprocfs_stats_collector() (:1693),&lt;br/&gt;
  lprocfs_stats_clear() (:1716) and __lprocfs_counter_init_units() (:1960)&lt;br/&gt;
  all walk past the end as well.  stats_free() (:1541/1544/1561) frees with&lt;br/&gt;
  the weight, so on a sparse mask it would also fail to free the slots whose&lt;br/&gt;
  id is &amp;gt;= the weight.&lt;br/&gt;
&lt;br/&gt;
DEFECT 3 - cl_env_percpu_init() error unwind  (needs an allocation failure)&lt;br/&gt;
&lt;br/&gt;
  lustre/obdclass/cl_object.c:961&lt;br/&gt;
      static struct cl_env cl_env_percpu&lt;span class="error"&gt;&amp;#91;NR_CPUS&amp;#93;&lt;/span&gt;;   /* indexed by CPU id */&lt;br/&gt;
&lt;br/&gt;
  cl_object.c:970..995 initialises cl_env_percpu&lt;span class="error"&gt;&amp;#91;i&amp;#93;&lt;/span&gt; under&lt;br/&gt;
  for_each_possible_cpu&lt;img class="emoticon" src="https://jira.whamcloud.com/images/icons/emoticons/information.png" height="16" width="16" align="absmiddle" alt="" border="0"/&gt; and breaks on the first failure, but&lt;br/&gt;
  cl_object.c:996..1005 unwinds over dense indices:&lt;br/&gt;

&lt;div class="preformatted panel" style="border-width: 1px;"&gt;&lt;div class="preformattedContent panelContent"&gt;
&lt;pre&gt;      if (rc != 0) {
              /* Indices 0 to i (excluding i) were correctly initialized,
               * thus we must uninitialize up to i, the rest are undefined. */
              for (j = 0; j &amp;lt; i; j++) {
                      cle = &amp;amp;cl_env_percpu[j];
                      lu_context_exit(&amp;amp;cle-&amp;gt;ce_ses);
                      lu_context_fini(&amp;amp;cle-&amp;gt;ce_ses);
                      lu_env_fini(&amp;amp;cle-&amp;gt;ce_lu);
              }
      }
&lt;/pre&gt;
&lt;/div&gt;&lt;/div&gt;&lt;br/&gt;
&lt;br/&gt;
  cl_env_percpu_fini() gets this right (it uses for_each_possible_cpu());&lt;br/&gt;
  only the error unwind does not.  Note [0, i) is a &lt;b&gt;superset&lt;/b&gt; of the&lt;br/&gt;
  initialised entries, so nothing above the failing CPU is missed - the bug&lt;br/&gt;
  is that holes below i are finalised even though they were never&lt;br/&gt;
  initialised.&lt;br/&gt;
&lt;br/&gt;
DEFECT 4 - out-of-bounds access on cfs_trace_data[]  (fires on every&lt;br/&gt;
CDEBUG/CERROR; the hottest and broadest of the four)&lt;br/&gt;
&lt;br/&gt;
  lnet/libcfs/tracefile.c:1288 (cfs_tracefile_init())&lt;br/&gt;
      cfs_trace_data&lt;span class="error"&gt;&amp;#91;i&amp;#93;&lt;/span&gt; = kmalloc_array(num_possible_cpus(),&lt;br/&gt;
                                        sizeof(union cfs_trace_data_union),&lt;br/&gt;
                                        GFP_KERNEL);&lt;br/&gt;
&lt;br/&gt;
  but every consumer indexes it by CPU id:&lt;br/&gt;
      tracefile.c:108-112   cfs_trace_get_tcd() -&amp;gt;&lt;br/&gt;
                            (*cfs_trace_data&lt;span class="error"&gt;&amp;#91;type&amp;#93;&lt;/span&gt;)&lt;span class="error"&gt;&amp;#91;get_cpu()&amp;#93;&lt;/span&gt;.tcd,&lt;br/&gt;
                            called from libcfs_debug_msg() for every&lt;br/&gt;
                            CDEBUG/CERROR/CWARN in the whole stack&lt;br/&gt;
      tracefile.c:94-97     cfs_tcd_for_each_type_lock(tcd, i, cpu) -&amp;gt;&lt;br/&gt;
                            (*cfs_trace_data&lt;span class="error"&gt;&amp;#91;i&amp;#93;&lt;/span&gt;)&lt;span class="error"&gt;&amp;#91;cpu&amp;#93;&lt;/span&gt;.tcd, used at :762&lt;br/&gt;
                            (collect_pages_on_all_cpus), :788&lt;br/&gt;
                            (put_pages_back_on_all_cpus) and :1348&lt;br/&gt;
                            (trace_cleanup_on_all_cpus), all three under&lt;br/&gt;
                            for_each_possible_cpu(cpu)&lt;br/&gt;
  and the page ownership tag is a CPU id too: tracefile.c:211 sets&lt;br/&gt;
  tage-&amp;gt;cpu = smp_processor_id(), which :797 matches against the&lt;br/&gt;
  for_each_possible_cpu() id.  So CPU-id indexing is the intended&lt;br/&gt;
  semantics of the array throughout.&lt;br/&gt;
&lt;br/&gt;
  With possible = {0, 2, 3}
&lt;p&gt; the array holds 3 entries.  A CDEBUG on CPU 3&lt;br/&gt;
  takes spin_lock() on, and list_add()s trace pages onto, memory one entry&lt;br/&gt;
  past the end of the kmalloc object.  That is unbounded heap corruption&lt;br/&gt;
  from a spin_lock and a doubly-linked-list insertion, not a bounded&lt;br/&gt;
  overwrite, and it happens on the debug path that every Lustre module&lt;br/&gt;
  uses.  The same slot is then touched out of bounds again by&lt;br/&gt;
  collect_pages_on_all_cpus() on every "lctl dk" and by&lt;br/&gt;
  trace_cleanup_on_all_cpus() at libcfs unload.&lt;/p&gt;

&lt;p&gt;  This one is NOT fixed by resizing the allocation alone.  The&lt;br/&gt;
  initialisation walk is the odd one out - it is &lt;b&gt;dense&lt;/b&gt;:&lt;/p&gt;

&lt;p&gt;      tracefile.c:88-92&lt;/p&gt;
&lt;div class="preformatted panel" style="border-width: 1px;"&gt;&lt;div class="preformattedContent panelContent"&gt;
&lt;pre&gt;        #define cfs_tcd_for_each(tcd, i, j)                             \
                for (i = 0; i &amp;lt; CFS_TCD_TYPE_CNT &amp;amp;&amp;amp; cfs_trace_data[i]; i++) \
                        for (j = 0, ((tcd) = &amp;amp;(*cfs_trace_data[i])[j].tcd); \
                             j &amp;lt; num_possible_cpus();                   \
                             j++, (tcd) = &amp;amp;(*cfs_trace_data[i])[j].tcd)
&lt;/pre&gt;
&lt;/div&gt;&lt;/div&gt;

&lt;p&gt;  cfs_tracefile_init() (:1297) is the only place the tcds are initialised&lt;br/&gt;
  and it uses that macro, so it initialises entries 0..weight-1.  Resizing&lt;br/&gt;
  the allocation to nr_cpu_ids without touching the walk would replace the&lt;br/&gt;
  out-of-bounds access with an access to an allocated but *never&lt;br/&gt;
  initialised* tcd (kmalloc_array does not zero) - CPU 3's first CDEBUG&lt;br/&gt;
  would spin_lock() a garbage lock word and splice pages onto a garbage&lt;br/&gt;
  list_head, and that tcd would never be drained by tracefiled either.&lt;br/&gt;
  That is not better than the overflow, so the array must be sized by&lt;br/&gt;
  nr_cpu_ids AND the walk must move to CPU-id space.  cfs_tcd_for_each()&lt;br/&gt;
  is also used by panic_collect_pages() (:751),&lt;br/&gt;
  cfs_trace_set_debug_mb() (:1103) and cfs_trace_get_debug_mb() (:1121);&lt;br/&gt;
  moving it to id space keeps get_debug_mb()'s sum consistent with&lt;br/&gt;
  set_debug_mb()'s division by num_possible_cpus(), which a dense walk&lt;br/&gt;
  over nr_cpu_ids would not.&lt;/p&gt;

&lt;p&gt;  Minor, same file: the "trace pages aren't empty" diagnostic at :1221&lt;br/&gt;
  prints CPU ids 0..num_possible_cpus()-1, so on a sparse mask it reports&lt;br/&gt;
  CPUs that do not exist and omits the ones that do.&lt;/p&gt;

&lt;p&gt;Supporting facts, all read from the kernel tree Lustre builds against&lt;br/&gt;
(RHEL 9.7, 5.14.0-611.13.1.el9_7):&lt;/p&gt;

&lt;p&gt;1. for_each_possible_cpu() makes no contiguity promise.&lt;br/&gt;
     include/linux/cpumask.h:1010&lt;br/&gt;
       #define for_each_possible_cpu(cpu) for_each_cpu((cpu), cpu_possible_mask)&lt;br/&gt;
     include/linux/cpumask.h:294&lt;br/&gt;
       #define for_each_cpu(cpu, mask) \&lt;br/&gt;
               for_each_set_bit(cpu, cpumask_bits(mask), small_cpumask_bits)&lt;br/&gt;
   It walks the set bits in increasing order, gaps included.&lt;/p&gt;

&lt;p&gt;2. Can the mask actually be sparse?&lt;/p&gt;
&lt;ul class="alternate" type="square"&gt;
	&lt;li&gt;x86: NO.  arch/x86/kernel/cpu/topology.c does&lt;br/&gt;
       init_cpu_possible(cpumask_of(0));&lt;br/&gt;
       for (cpu = 0; cpu &amp;lt; allowed; cpu++) set_cpu_possible(cpu, true);&lt;br/&gt;
     Ids are assigned densely; "possible_cpus="/"maxcpus=" only change&lt;br/&gt;
     "allowed", and rejected CPUs never get an id.  Xen PV&lt;br/&gt;
     (arch/x86/xen/smp_pv.c:218-221) clears bits from the top down, which&lt;br/&gt;
     also keeps the mask dense.&lt;/li&gt;
	&lt;li&gt;sparc (smp_fill_in_cpu_possible_map), mips (prefill_possible_map),&lt;br/&gt;
     PowerPC (assign_threads / smp_setup_cpu_maps): dense.&lt;/li&gt;
	&lt;li&gt;arm64: YES, possible.  arch/arm64/kernel/smp.c smp_init_cpus():
&lt;div class="preformatted panel" style="border-width: 1px;"&gt;&lt;div class="preformattedContent panelContent"&gt;
&lt;pre&gt;       for (i = 1; i &amp;lt; nr_cpu_ids; i++)
               if (cpu_logical_map(i) != INVALID_HWID)
                       if (smp_cpu_setup(i))
                               set_cpu_logical_map(i, INVALID_HWID);
&lt;/pre&gt;
&lt;/div&gt;&lt;/div&gt;
&lt;p&gt;     smp_cpu_setup() calls set_cpu_possible(cpu, true) only after&lt;br/&gt;
     init_cpu_ops() and ops-&amp;gt;cpu_init() both succeed.  A DT/ACPI-described&lt;br/&gt;
     CPU with an unsupported enable-method (or a failing cpu_init) is&lt;br/&gt;
     skipped while the next logical id still succeeds, leaving e.g. &lt;/p&gt;
{0,2,3}
&lt;p&gt;.&lt;/p&gt;&lt;/li&gt;
&lt;/ul&gt;


&lt;p&gt;3. What defect 3's unwind does to a zeroed struct cl_env (cl_env_percpu[] is&lt;br/&gt;
   static, so an unvisited slot is all zeroes):&lt;/p&gt;
&lt;ul class="alternate" type="square"&gt;
	&lt;li&gt;lu_context_exit() (lu_object.c:1816) is harmless on it.  LINVRNT() is&lt;br/&gt;
     compiled out unless --enable-invariants, and the lct_exit walk is&lt;br/&gt;
     guarded by "ctx-&amp;gt;lc_tags &amp;amp; LCT_HAS_EXIT &amp;amp;&amp;amp; ctx-&amp;gt;lc_value", both zero.&lt;/li&gt;
	&lt;li&gt;lu_context_fini() (lu_object.c:1790) is NOT harmless:
&lt;div class="preformatted panel" style="border-width: 1px;"&gt;&lt;div class="preformattedContent panelContent"&gt;
&lt;pre&gt;         if ((ctx-&amp;gt;lc_tags &amp;amp; LCT_REMEMBER) == 0)
                 LASSERT(list_empty(&amp;amp;ctx-&amp;gt;lc_remember));
&lt;/pre&gt;
&lt;/div&gt;&lt;/div&gt;
&lt;p&gt;     lc_tags is 0 so the LASSERT is evaluated, and list_empty() on a zeroed&lt;br/&gt;
     list_head is false (head-&amp;gt;next == NULL != head).  LASSERT is on by&lt;br/&gt;
     default (config/lustre-libcfs.m4 defines LIBCFS_DEBUG unless&lt;br/&gt;
     --disable-libcfs-assert), so this LBUGs.&lt;/p&gt;&lt;/li&gt;
	&lt;li&gt;lu_env_fini() (lu_object.c:1923) reaches the same lu_context_fini().&lt;br/&gt;
   So it is an LBUG, not a silent leak.  LBUG() does not return, so real&lt;br/&gt;
   entries after the first hole are never finalised either, but with&lt;br/&gt;
   panic_on_lbug that is moot.&lt;/li&gt;
&lt;/ul&gt;


&lt;p&gt;4. Reachability.  Defect 1 runs on every obdclass module load.  Defect 2 runs&lt;br/&gt;
   on the lprocfs stats path, i.e. continuously on a live system.  Defect 3&lt;br/&gt;
   runs only if lu_env_init() or lu_context_init() fails, i.e. under memory&lt;br/&gt;
   allocation failure, and only from cl_global_init() during module init.&lt;br/&gt;
   Defect 4 runs on every CDEBUG/CERROR from an affected CPU, i.e. from the&lt;br/&gt;
   moment libcfs.ko is loaded, before any Lustre target or client exists.&lt;/p&gt;

&lt;p&gt;5. Not a defect, checked and cleared: tracefile.c:386 and :409&lt;br/&gt;
   (debug_format_buffer_alloc_buffers/free_buffers) also walk&lt;br/&gt;
   for_each_possible_cpu(), but they iterate a genuine alloc_percpu()&lt;br/&gt;
   object via per_cpu_ptr(), which is id-safe by construction.&lt;br/&gt;
   cfs_trace_set_debug_mb()'s division by num_possible_cpus() (:1098) is a&lt;br/&gt;
   count, not an index, and stays correct once the walk is in id space&lt;br/&gt;
   because exactly num_possible_cpus() tcds are then initialised.&lt;/p&gt;

&lt;p&gt;Steps to reproduce:&lt;br/&gt;
  Not reproducible on x86 - the possible mask is dense there by&lt;br/&gt;
  construction.  On arm64 it needs a platform where one CPU's cpu_ops init&lt;br/&gt;
  fails while a higher-numbered one succeeds; defect 3 additionally needs an&lt;br/&gt;
  allocation failure during "modprobe obdclass".  Defects 1 and 3 can be&lt;br/&gt;
  demonstrated synthetically by making cl_env_percpu_init()/&lt;br/&gt;
  cl_env_percpu_fini() iterate a hardcoded sparse id list and forcing&lt;br/&gt;
  lu_env_init() to fail on the third iteration.  Defect 4 can be&lt;br/&gt;
  demonstrated synthetically by clearing a low bit of cpu_possible_mask&lt;br/&gt;
  before libcfs.ko is loaded (or by hardcoding a sparse id list in&lt;br/&gt;
  cfs_tcd_for_each) and issuing a CDEBUG from the highest CPU under KASAN.&lt;/p&gt;

&lt;p&gt;Actual result (on a sparse mask):&lt;/p&gt;
&lt;ul class="alternate" type="square"&gt;
	&lt;li&gt;cl_global_init() writes past the end of the cl_envs allocation, and the&lt;br/&gt;
    lprocfs stats path writes and reads past the end of ls_percpu[] (slab&lt;br/&gt;
    corruption; KASAN slab-out-of-bounds).&lt;/li&gt;
	&lt;li&gt;any CDEBUG/CERROR on a CPU whose id is &amp;gt;= the weight of&lt;br/&gt;
    cpu_possible_mask locks and appends pages to a tcd past the end of the&lt;br/&gt;
    cfs_trace_data allocation (KASAN slab-out-of-bounds in&lt;br/&gt;
    cfs_trace_get_tcd / cfs_trace_lock_tcd), and "lctl dk" and libcfs&lt;br/&gt;
    unload touch the same out-of-bounds slot.&lt;/li&gt;
	&lt;li&gt;and, under an allocation failure,&lt;br/&gt;
    LustreError: ... (lu_object.c:1794:lu_context_fini())&lt;br/&gt;
    ASSERTION( list_empty(&amp;amp;ctx-&amp;gt;lc_remember) ) failed -&amp;gt; LBUG.&lt;/li&gt;
&lt;/ul&gt;


&lt;p&gt;Expected result:&lt;br/&gt;
  Module load, debug logging and stats accounting behave correctly&lt;br/&gt;
  regardless of the shape of cpu_possible_mask.&lt;/p&gt;</description>
                <environment />
        <key id="99085">LU-20632</key>
            <summary>libcfs and obdclass size several CPU-id-indexed arrays by num_possible_cpus(), so a sparse cpu_possible_mask overflows them</summary>
                <type id="1" iconUrl="https://jira.whamcloud.com/secure/viewavatar?size=xsmall&amp;avatarId=11303&amp;avatarType=issuetype">Bug</type>
                                            <priority id="4" iconUrl="https://jira.whamcloud.com/images/icons/priorities/minor.svg">Minor</priority>
                        <status id="1" iconUrl="https://jira.whamcloud.com/images/icons/statuses/open.png" description="The issue is open and ready for the assignee to start work on it.">Open</status>
                    <statusCategory id="2" key="new" colorName="default" />
                                    <resolution id="-1">Unresolved</resolution>
                                        <assignee username="wc-triage">WC Triage</assignee>
                                    <reporter username="green">Oleg Drokin</reporter>
                        <labels>
                    </labels>
                <created>Mon, 24 Aug 2026 07:38:08 +0000</created>
                <updated>Mon, 24 Aug 2026 07:38:08 +0000</updated>
                                                                                <due />
                            <votes>0</votes>
                                    <watches>1</watches>
                                                                                    <attachments>
                    </attachments>
                <subtasks>
                    </subtasks>
                <customfields>
                                                                                                                                                                                            <customfield id="customfield_10890" key="com.atlassian.jira.plugins.jira-development-integration-plugin:devsummary">
                        <customfieldname>Development</customfieldname>
                        <customfieldvalues>
                            
                        </customfieldvalues>
                    </customfield>
                                                                                                                                                                                                                                                                                                                                                        <customfield id="customfield_10390" key="com.pyxis.greenhopper.jira:gh-lexo-rank">
                        <customfieldname>Rank</customfieldname>
                        <customfieldvalues>
                            <customfieldvalue>1|i06ys7:</customfieldvalue>

                        </customfieldvalues>
                    </customfield>
                                                                <customfield id="customfield_10090" key="com.pyxis.greenhopper.jira:gh-global-rank">
                        <customfieldname>Rank (Obsolete)</customfieldname>
                        <customfieldvalues>
                            <customfieldvalue>9223372036854775807</customfieldvalue>
                        </customfieldvalues>
                    </customfield>
                                                                                            <customfield id="customfield_10060" key="com.atlassian.jira.plugin.system.customfieldtypes:select">
                        <customfieldname>Severity</customfieldname>
                        <customfieldvalues>
                                <customfieldvalue key="10022">3</customfieldvalue>

                        </customfieldvalues>
                    </customfield>
                                                                                                                                                                                                                                                                                                                                                        </customfields>
    </item></channel></rss>
Change:     69133
Subject:    LU-15533 mdt: don't LBUG on getattr of non-MD object
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/69133
Change-Id:  Ic28079c9c0db6308b439263d631119bc4dec0815
Project:    fs/lustre-release
Branch:     master
Status:     NEW
Owner:      Max Wang
Created:    2026-09-22 23:54:20.000000000
Updated:    2026-09-25 09:31:57.000000000
Patch Set:  3
Insertions: +47
Deletions:  -4
Comments:   33 (2 unresolved)

--- Labels ---
  Code-Review: (no votes)
  Verified: jenkins: +1

--- Reviewers ---
  CC: Gerrit AI review for Lustre
  REVIEWER: Lai Siyao, Andreas Dilger, Timothy Day

--- Commit Message ---
LU-15533 mdt: don't LBUG on getattr of non-MD object

A client-supplied FID that passes fid_is_sane() can resolve to an
object with no metadata (MDD) layer -- e.g. an OSD-internal object
such as the OI scrub tracker, FID {FID_SEQ_IGIF_MAX, osd_index + 1,
0} (osd_scrub_setup()), "only visible inside the OSD".  Such a FID
can be named directly through the by-FID interfaces
(open_by_handle/NFS, .lustre/fid, llapi open-by-fid).

lu_object_find() resolves the FID at ld_site->ls_top_dev, not on the
device it is passed, so on an MDS it normally returns the MDT slice.
The OSD-only object comes back from the shared site hash instead:
osd_scrub_setup() instantiated its header while ls_top_dev was still
the OSD, so the header has no MD slice at all.  Once it is stored as
tsi_corpus, tsi2mdt_info() casts it with mdt_obj() and mdt_getattr()
-> mdt_attr_get_eabuf_size() -> mdt_object_child() -> lu2md() trips
  ASSERTION( o == NULL || IS_ERR(o) || lu_device_is_md(o->lo_dev) )
and LBUGs the MDS.  The request is a resend, so it replays on every
recovery and the MDS crash-loops.

Add lu_object_is_md() to test whether an object slice belongs to a
metadata device, and reject a non-MD object with -ENOENT:
 - tgt_mdt_body_unpack(): for every handler whose format carries an
   MDT_BODY, not only HAS_BODY ones, since e.g. MDS_RMFID also ends
   up in mdt_obj(tsi_corpus) via tsi2mdt_info();
 - mdt_object_find(): the other by-FID callers (fid2path, intents).

Testing the object's own device rather than the layer below it also
avoids calling lu_object_next() on a header with a single (OSD)
layer, where it returns a pointer derived from the list head.

Fixes: 89ead218eb ("LU-7585 zfs: OI scrub for ZFS")
Signed-off-by: Max Wang <wamax@amazon.com>
Change-Id: Ic28079c9c0db6308b439263d631119bc4dec0815

--- Review History ---

--- Message (Max Wang, 2026-09-22 23:54:20.000000000) ---
Uploaded patch set 1.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-09-22 23:57:15.000000000) ---
Patch Set 1:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-09-23 00:05:39.000000000) ---
Patch Set 1:

(5 comments)

5 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (Timothy Day, 2026-09-23 00:22:50.000000000) ---
Patch Set 1:

(2 comments)

--- Message (jenkins, 2026-09-23 04:02:25.000000000) ---
Patch Set 1: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/132166/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-09-23 18:02:47.000000000) ---
Patch Set 1:

(7 comments)

AI code review results below, please doublecheck

Some concerns, see inline comments. The non-MD test walks off the end of loh_layers for exactly the object class the patch targets, and the guard sits on a path the intent (LDLM_ENQUEUE) by-FID lookups do not take - including the one the new sanity 154k exercises - so `stat $MOUNT/.lustre/fid/<internal fid>` still looks able to LBUG the MDS.

--- Message (Max Wang, 2026-09-23 19:02:51.000000000) ---
Uploaded patch set 2.

Outdated Votes:
* Verified+1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (Timothy Day, 2026-09-23 19:11:24.000000000) ---
Patch Set 2: Code-Review+1

--- Message (Max Wang, 2026-09-23 19:16:45.000000000) ---
Patch Set 2:

(9 comments)

--- Message (wc-checkpatch, 2026-09-23 19:18:52.000000000) ---
Patch Set 2:

Looks good to me.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-09-23 19:20:21.000000000) ---
Patch Set 2:

Code-wise looks good to me.

--- Message (jenkins, 2026-09-23 21:54:31.000000000) ---
Patch Set 2: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/132234/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-09-24 20:52:21.000000000) ---
Patch Set 2:

(5 comments)

AI code review results below, please doublecheck

Mostly good; the two rejection points do close the crash and no functional defect turned up. One thing worth a look: sanity 154k reaches the MDS only through mdt_object_find() - the `.lustre/fid` stat arrives as an LDLM intent (mdt_intent_opc() -> mdt_unpack_req_pack_rep() -> mdt_body_unpack() -> mdt_object_find()) and `lfs fid2path` as MDS_GET_INFO -> mdt_fid2path() -> mdt_object_find(). Nothing in the test exercises the tgt_mdt_body_unpack() hunk, which is the path the commit message describes. A case driving an MDS_GETATTR, MDS_RMFID or MDS_BATCH at the same FID would cover it. Remaining comments are optional nits for whenever this is next refreshed.

--- Message (Max Wang, 2026-09-25 01:51:17.000000000) ---
Uploaded patch set 3.

Outdated Votes:
* Code-Review+1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE OR changekind:TRIVIAL_REBASE OR changekind:TRIVIAL_REBASE_WITH_MESSAGE_UPDATE OR is:MIN")
* Verified+1, Verified-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-09-25 01:54:22.000000000) ---
Patch Set 3:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-09-25 01:54:56.000000000) ---
Patch Set 3:

Looks good to me.

--- Message (Max Wang, 2026-09-25 01:57:55.000000000) ---
Patch Set 3:

(5 comments)

--- Message (jenkins, 2026-09-25 03:08:50.000000000) ---
Patch Set 3: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/132293/ : SUCCESS

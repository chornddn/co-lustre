Change:     68340
Subject:    LU-20643 utils: clear stale lmd fields on reuse
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/68340
Change-Id:  I6126e73bb120bd6a12a904563e13303d63e75c1c
Project:    fs/lustre-release
Branch:     master
Status:     NEW
Owner:      Hiroshi Nishida
Created:    2026-08-25 16:49:11.000000000
Updated:    2026-08-29 18:12:42.000000000
Patch Set:  3
Insertions: +18
Deletions:  -2
Comments:   12 (6 unresolved)

--- Labels ---
  Code-Review: (no votes)
  Verified: jenkins: +1; Maloo: -1

--- Reviewers ---
  CC: Lustre RISC-V Builder, Gerrit AI review for Lustre

--- Commit Message ---
LU-20643 utils: clear stale lmd fields on reuse

get_lmd_info_fd() reuses one struct lov_user_mds_data for every object
and convert_lmd_statx() writes only the fields lstat(2) supplies, so
stx_attributes, stx_attributes_mask, stx_btime and the mask bits above
STATX_BASIC_STATS keep whatever was there before.

Two ways that is not this object's data.  On the V1 ioctl path the
lstat_t is copied over the front of the record, so stx_attributes_mask
ends up holding st_atim.tv_sec -- never 0 in practice -- and stx_mask
picks up bits from st_nlink, which reads as STATX_BTIME on a directory
whose link count has 0x800 set.  It does not reach as far as it looks:
lmd_st is 144 bytes against 272 for lmd_fid and lmd_stx, so it stops
at the end of stx_mtime and stx_rdev_major onwards is still the
previous conversion's.  On the lstat fallback the buffer holds the
previous object's data outright, or the file name written in for the
ioctl.

So a walk that crosses onto a non-Lustre subtree can answer -btime and
the attribute predicates from a file name.

Clear what is about to be reused on both paths, with the same line:
everything up to lmd_lmm, which is what the record's own fields amount
to.  That takes lmd_fid, lmd_flags, lmd_lmmsize and lmd_padding with
it -- nothing in lustre/utils reads lmd_fid today, but a short name
there passes fid_is_sane() as an IGIF, and llapi_get_lum_file_fd()
copies by lmd_lmmsize.  On the V1 path it goes after the memmove, so
the layout it has just moved up is not touched.  The glimpse path is
left alone: there the MDT's answer is still wanted.

Fixes: 11aa7f8704c4 ("LU-11367 som: integrate LSOM with lfs find")
Signed-off-by: Hiroshi Nishida <hnishida@thelustrecollective.com>
Assisted-by: ClaudeCode:Opus-5 llm_code_and_review_tools
Change-Id: I6126e73bb120bd6a12a904563e13303d63e75c1c

--- Review History ---

--- Message (Hiroshi Nishida, 2026-08-25 16:49:11.000000000) ---
Uploaded patch set 1.

--- Message (wc-checkpatch, 2026-08-25 16:51:26.000000000) ---
Patch Set 1:

Looks good to me.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-26 07:40:09.000000000) ---
Patch Set 1:

Code-wise looks good to me.

--- Message (Lustre RISC-V Builder, 2026-08-26 07:55:45.000000000) ---
Patch Set 1:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-08-26 08:08:18.000000000) ---
Patch Set 1: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/130245/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-08-27 03:47:35.000000000) ---
Patch Set 1:

(6 comments)

AI code review results below, please doublecheck

Mostly good, comments inline. The stale-buffer effect is real and reproducible from `lfs find` (`llapi_get_lum_file_fd()` even hands `convert_lmd_statx()` an uninitialized stack buffer), but the two hunks clear different sets of fields and the commit message claims an impact the code does not seem to have. Since the symptom is user visible, could a regression test go with it - e.g. a sanity case that runs `lfs find -btime`/`-attrs` across a non-Lustre subtree (or `lfs find --newerbt` with a non-Lustre reference file) and checks the answer is EOPNOTSUPP rather than a match?

--- Message (Hiroshi Nishida, 2026-08-28 00:00:13.000000000) ---
Uploaded patch set 2.

Outdated Votes:
* Verified+1, Verified-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (wc-checkpatch, 2026-08-28 00:10:52.000000000) ---
Patch Set 2:

Looks good to me.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-28 00:46:09.000000000) ---
Patch Set 2:

Code-wise looks good to me.

--- Message (Lustre RISC-V Builder, 2026-08-28 01:09:45.000000000) ---
Patch Set 2:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-08-28 02:55:52.000000000) ---
Patch Set 2: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/130501/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-08-28 23:25:49.000000000) ---
Patch Set 2:

(3 comments)

AI code review results below, please doublecheck

Looks correct: both partial-write paths now clear the fields convert_lmd_statx() never fills, and the stat refresh in cb_find_init() is rightly left alone so the MDT's answer survives. Comments inline are documentation and an adjacent pre-existing issue, not objections to the fix.

LU-20643 is a wrong-answer bug in a user-facing tool, so could this carry a regression test? A sanity case that walks a tree containing non-Lustre objects with -btime (and -attrs) after a Lustre object, and checks the non-Lustre entries do not inherit the previous object's answer, would fail before this patch and pass after.

--- Message (Hiroshi Nishida, 2026-08-28 23:53:17.000000000) ---
Uploaded patch set 3.

Outdated Votes:
* Verified+1, Verified-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (Hiroshi Nishida, 2026-08-28 23:55:59.000000000) ---
Patch Set 3:

(3 comments)

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-28 23:56:47.000000000) ---
Patch Set 3:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-08-29 00:01:04.000000000) ---
Patch Set 3:

Looks good to me.

--- Message (Lustre RISC-V Builder, 2026-08-29 00:29:00.000000000) ---
Patch Set 3:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-08-29 03:47:49.000000000) ---
Patch Set 3: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/130562/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-08-29 18:12:42.000000000) ---
Patch Set 3:

AI code review results below, please doublecheck

Analysis found no correctness regression: both memsets are correctly bounded (offsetof(lov_user_mds_data, lmd_lmm) is 288, and every GET_LMD_INFO caller passes a buffer at least that large), the convert_lmdbuf_v1v2() one is correctly ordered after the memmove that reads the overlapping source region, and neither clears anything a consumer still needs -- cb_find_init() zeroes lmd_lmm.lmm_magic itself, and the V2 ioctl path in ll_dir_ioctl() writes lmd_fid/lmd_stx/lmd_flags/lmd_lmmsize in full so it is unaffected.

One thing missing: this is a user-visible wrong-answer fix with no test. sanity.sh already has test_56od for `lfs find -btime`; a companion subtest walking a Lustre path and a non-Lustre one in the same invocation would be deterministic and would fail before this patch. With $DIR walked first the non-Lustre entry inherited STATX_BTIME from the previous object and got matched; now `-btime` correctly reports EOPNOTSUPP for it. Could a case along those lines be added?

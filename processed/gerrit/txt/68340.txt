Change:     68340
Subject:    LU-20643 utils: do not reuse an lmd buffer across objects
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/68340
Change-Id:  I6126e73bb120bd6a12a904563e13303d63e75c1c
Project:    fs/lustre-release
Branch:     master
Status:     NEW
Owner:      Hiroshi Nishida
Created:    2026-08-25 16:49:11.000000000
Updated:    2026-08-27 03:47:35.000000000
Patch Set:  1
Insertions: +16
Deletions:  -0
Comments:   6 (6 unresolved)

--- Labels ---
  Code-Review: (no votes)
  Verified: jenkins: +1; Maloo: -1

--- Reviewers ---
  CC: Lustre RISC-V Builder, Gerrit AI review for Lustre

--- Commit Message ---
LU-20643 utils: do not reuse an lmd buffer across objects

get_lmd_info_fd() reuses one struct lov_user_mds_data for every object
and convert_lmd_statx() writes only the fields lstat(2) supplies, so
stx_attributes, stx_attributes_mask, stx_btime and the mask bits above
STATX_BASIC_STATS keep whatever was there before.

Two ways that is not this object's data.  On the V1 ioctl path lmd_st
covers lmd_stx, so stx_attributes_mask ends up holding st_atim.tv_sec
-- never 0 in practice -- and stx_mask picks up bits from st_nlink,
which reads as STATX_BTIME on a directory whose link count has 0x800
set.  On the lstat fallback the buffer still holds the file name that
was written into it for the ioctl: those bytes sit in lmd_fid, where
fid_is_sane() accepts a short name as an IGIF, and in the statx fields
convert_lmd_statx() does not write.

So a walk that crosses onto a non-Lustre subtree can answer -btime and
the attribute predicates from a file name, and report a FID made of
one.

Clear what is about to be reused on both paths.  The glimpse path is
left alone: there the MDT's answer is still wanted.

Signed-off-by: Hiroshi Nishida <hnishida@thelustrecollective.com>
Assisted-by: ClaudeCode:Opus-5 llm_code_and_review_tools
Change-Id: I6126e73bb120bd6a12a904563e13303d63e75c1c

--- Review History ---

--- Message (Hiroshi Nishida, 2026-08-25 16:49:11.000000000) ---
Uploaded patch set 1.

--- Message (wc-checkpatch, 2026-08-25 16:51:26.000000000) ---
Patch Set 1:

Looks good to me.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-26 07:40:09.000000000) ---
Patch Set 1:

Code-wise looks good to me.

--- Message (Lustre RISC-V Builder, 2026-08-26 07:55:45.000000000) ---
Patch Set 1:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-08-26 08:08:18.000000000) ---
Patch Set 1: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/130245/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-08-27 03:47:35.000000000) ---
Patch Set 1:

(6 comments)

AI code review results below, please doublecheck

Mostly good, comments inline. The stale-buffer effect is real and reproducible from `lfs find` (`llapi_get_lum_file_fd()` even hands `convert_lmd_statx()` an uninitialized stack buffer), but the two hunks clear different sets of fields and the commit message claims an impact the code does not seem to have. Since the symptom is user visible, could a regression test go with it - e.g. a sanity case that runs `lfs find -btime`/`-attrs` across a non-Lustre subtree (or `lfs find --newerbt` with a non-Lustre reference file) and checks the answer is EOPNOTSUPP rather than a match?

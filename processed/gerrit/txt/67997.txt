Change:     67997
Subject:    LU-20590 tests: inject layout lock reclaim during an IO
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/67997
Change-Id:  I4ab964a505bd979b45aceb2e0efa22cc158173d6
Project:    fs/lustre-release
Branch:     master
Status:     NEW
Owner:      Chris Horn
Created:    2026-08-14 04:20:45.000000000
Updated:    2026-08-14 14:38:35.000000000
Patch Set:  1
Insertions: +90
Deletions:  -3
Comments:   5 (4 unresolved)

--- Labels ---
  Code-Review: (no votes)
  Verified: jenkins: +1; Maloo: +1

--- Reviewers ---
  CC: Lustre RISC-V Builder, Gerrit AI review for Lustre

--- Commit Message ---
LU-20590 tests: inject layout lock reclaim during an IO

The client reclaims the layout lock of a file from the LRU.  An IO on
that file which is in flight then restarts.  Add a fault injection
which drops the unused locks of the MDC namespace inside the window
the IO uses.  The restart then needs no memory pressure and no racing
lru_size=clear.  The injection goes through the LRU cancel path, not
a direct invalidation of the object.  The condition under test is
therefore the real one.  The injection fires only after the layout
refresh succeeds.  A drop of the namespace LRU on the way out of a
failed refresh does no good.

The reclaim is best effort.  ldlm_cancel_lru() queues nothing while
another thread already drains the namespace.  A lock which somebody
holds a mode reference on is no candidate either.  The injection can
therefore miss, and the IO then completes with no restart at all.
The count of locks queued goes to the debug log.  The test skips
instead of fails when that log holds no restart.  A test which did
not run must not report a failure.

Add sanity test_251c.  The test checks that such an IO completes.
The test also checks that the pin held the layout lock out of the
LRU.

ldlm_cancel_lru() moves to a public header, and llite is a module of
its own, so the patch exports the symbol.  A test change which widens
the module ABI is unusual, so this message calls it out.

Test-Parameters: trivial
Assisted-by: Claude:claude-opus-5
Signed-off-by: Chris Horn <chorn@ddn.com>
Change-Id: I4ab964a505bd979b45aceb2e0efa22cc158173d6

--- Review History ---

--- Message (Chris Horn, 2026-08-14 04:20:45.000000000) ---
Uploaded patch set 1.

--- Message (wc-checkpatch, 2026-08-14 04:22:43.000000000) ---
Patch Set 1:

Looks good to me.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-14 04:30:18.000000000) ---
Patch Set 1:

Code-wise looks good to me.

--- Message (jenkins, 2026-08-14 05:52:56.000000000) ---
Patch Set 1: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/129337/ : SUCCESS

--- Message (Lustre RISC-V Builder, 2026-08-14 10:01:51.000000000) ---
Patch Set 1:

RISC-V Client Builder: build succesful

--- Message (Gerrit AI review for Lustre, 2026-08-14 14:38:35.000000000) ---
Patch Set 1:

(4 comments)

AI code review results below, please doublecheck

Mostly good, minor comments inline. The mechanism itself checks out: ll_layout_lock_set() drops the match reference so the layout lock really is on the MDC LRU, can_populate_pages() aborts before any bytes move so every injected restart counts toward LL_LAYOUT_PIN_RESTARTS, and ldlm_lock_decref_internal() skips the LRU for an LDLM_FL_NO_LRU lock so the injection stops biting once the pin is taken.

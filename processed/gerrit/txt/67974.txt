Change:     67974
Subject:    LU-20589 llite: fail IO when the restart budget runs out
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/67974
Change-Id:  Ica49c8469fabbf5e795fa40d4bfb583f8e21e800
Project:    fs/lustre-release
Branch:     master
Status:     ABANDONED
Owner:      Chris Horn
Created:    2026-08-13 18:18:11.000000000
Updated:    2026-08-14 01:15:56.000000000
Patch Set:  1
Insertions: +47
Deletions:  -1
Comments:   1 (0 unresolved)

--- Labels ---
  Code-Review: (no votes)
  Verified: (no votes)

--- Reviewers ---
  CC: Lustre RISC-V Builder

--- Commit Message ---
LU-20589 llite: fail IO when the restart budget runs out

ll_file_io_generic() restarts an IO when a lower layer sets
ci_need_restart, at most RETRY_ATTEMPTS times. One producer of
that request is can_populate_pages(), which compares the cached
layout generation of the inode with the generation the IO took a
copy of. On a mismatch vvp_io_write_start() returns 0, a success
which moves no bytes.

If the generation keeps changing, for example because the client
cancels its own layout locks from LRU reclaim, every attempt is
used. Control then reaches RETURN(result > 0 ? result : rc) with
result and rc both 0. The io->ci_result fallback does not help,
because each restart builds a fresh cl_io whose ci_result starts
at 0, and the layout mismatch path never sets it. write(2) then
returns 0 for a non-zero request and the data is dropped without
a message, and read(2) reports a false end of file.

Report -EIO when the budget is used up and nothing was moved, and
log the fsname, FID and IO range at a rate limit. A transfer
which moved some bytes is still returned as a short IO. -EIO is
not retryable, and the condition which causes it can be transient,
but an error the application can see is better than a silent loss
of the data.

Make the layout mismatch fault injection repeatable so that a test
can use up the budget. Add sanity test_251b, which checks that
both a write and a read fail instead of returning 0.

Test-Parameters: trivial
Fixes: 6b1e747ad5bf ("LU-14760 llite: restart clio for AIO if necessary")
Assisted-by: Claude:claude-opus-5
Signed-off-by: Chris Horn <chorn@ddn.com>
Change-Id: Ica49c8469fabbf5e795fa40d4bfb583f8e21e800

--- Review History ---

--- Message (Chris Horn, 2026-08-13 18:18:11.000000000) ---
Uploaded patch set 1.

--- Message (wc-checkpatch, 2026-08-13 18:19:02.000000000) ---
Patch Set 1:

Looks good to me.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-13 18:23:22.000000000) ---
Patch Set 1:

Code-wise looks good to me.

--- Message (Chris Horn, 2026-08-13 18:33:56.000000000) ---
Abandoned

Abandon duplicate change

--- Message (Lustre RISC-V Builder, 2026-08-13 19:54:29.000000000) ---
Patch Set 1:

RISC-V Client Builder: build succesful

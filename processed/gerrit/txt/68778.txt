Change:     68778
Subject:    LU-20703 llite: limit readahead window by the readahead budget
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/68778
Change-Id:  I01818568c4cba4ff40bee0b725f7e7dcf0bc9063
Project:    fs/lustre-release
Branch:     master
Status:     NEW
Owner:      Zhenyu Xu
Created:    2026-09-10 12:24:17.000000000
Updated:    2026-09-22 11:49:26.000000000
Patch Set:  2
Insertions: +84
Deletions:  -5
Comments:   24 (3 unresolved)

--- Labels ---
  Code-Review: Xiyang Wang: +1
  Verified: jenkins: +1; Maloo: +1

--- Reviewers ---
  CC: Lustre RISC-V Builder, Gerrit AI review for Lustre
  REVIEWER: Li Dongyang, Qian Yingjin, Xiyang Wang

--- Commit Message ---
LU-20703 llite: limit readahead window by the readahead budget

The per-file readahead window is capped by ra_max_pages_per_file,
independently of the global readahead budget ra_max_pages
(llite.*.max_read_ahead_mb).  When the budget is set below the
per-file window (e.g. sanity/101d sets max_read_ahead_mb=40 while
max_read_ahead_per_file_mb=64 on a 64K-page client), the readahead
window target exceeds the whole readahead budget, and then:

* the async readahead gate in kickoff_async_readahead() is never
  satisfied: the throttle defaults to ra_max_pages_per_file, so
  the gate only opens at the maximal window, but a cache miss
  runs ras_reset() and drops the window back to 0, and it takes
  ra_max_pages_per_file / ras_rpc_pages growth steps to climb
  back;
* every cache miss runs a synchronous readahead walk that reserves
  the whole remaining budget and holds it until its read RPCs
  complete;
* while the budget is in flight the reader catches up with the
  readahead frontier, and further misses can only reserve one page
  via the pages_min override, degenerating into a storm of one-page
  (64KB) read RPCs plus per-miss readahead state and lock-match
  overhead, making the readahead-enabled read slower than the
  readahead-disabled one: "readahead 22.8092s > no-readahead
  10.3606s (80M)".

Limit the effective per-file readahead window by the readahead
budget with a new helper ra_max_window_pages(), and use it where
the window growth is capped (ras_stride_increase_window(),
ras_increase_window()), where the whole-file-read window is
initialized (ll_ras_enter()) and in the async readahead gate
(kickoff_async_readahead()).  The stored tunables are unchanged,
so raising max_read_ahead_mb again restores the original behavior.

The window grows in ras_rpc_pages steps once it reaches one RPC
(ras_increase_window() aligns every step), so it tops out at
ra_max_window_pages() rounded down to the RPC size.  Round the
async readahead throttle down to the same granularity, otherwise
it can exceed every value the window can take and async readahead
never fires (e.g. brw_size=16 on a 64K-page client with
max_read_ahead_mb=40: window tops out at 512 pages, throttle is
640).  Throttles below one RPC are left unaligned, since such
windows are still reachable via the sub-RPC window in
ras_increase_window().

Also add opt-in (DEBUG_101D=1, or DEBUG_101D=trace to also capture
the D_READA trace) instrumentation to sanity/101d, which dumps the
readahead parameters, readahead stats and read RPC size histogram
around the measured reads, and restores the debug mask via
debugsave()/debugrestore() when tracing.

Assisted-by: Augment Agent:GLM 5.3 Flash
Test-Parameters: testlist=sanity.sh env=ONLY=101d,ONLY_REPEAT=50 clientdistro=rocky9.5 clientarch=aarch64
Fixes: c2791674260b ("LU-12043 llite: improve single-thread read performance")
Signed-off-by: Bobi Jam <bobijam@whamcloud.com>
Change-Id: I01818568c4cba4ff40bee0b725f7e7dcf0bc9063

--- Review History ---

--- Message (Zhenyu Xu, 2026-09-10 12:24:17.000000000) ---
Uploaded patch set 1.

--- Message (wc-checkpatch, 2026-09-10 12:26:22.000000000) ---
Patch Set 1:

(4 comments)

4 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-09-10 12:27:28.000000000) ---
Patch Set 1:

Code-wise looks good to me.

--- Message (jenkins, 2026-09-10 12:45:57.000000000) ---
Patch Set 1: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/131512/ : SUCCESS

--- Message (Lustre RISC-V Builder, 2026-09-10 12:54:52.000000000) ---
Patch Set 1:

RISC-V Client Builder: build succesful

--- Message (Gerrit AI review for Lustre, 2026-09-11 17:26:00.000000000) ---
Patch Set 1:

(5 comments)

AI code review results below, please doublecheck

Some concerns, see inline comments.  The default configuration is unaffected (ra_max_pages_per_file defaults to ra_max_pages/4), so this only bites when max_read_ahead_mb has been lowered below the per-file window, but in that case the new cap interacts badly with the RPC alignment done in ras_increase_window() and can leave the async gate less reachable than before the patch.

--- Message (Qian Yingjin, 2026-09-12 03:54:27.000000000) ---
Patch Set 1:

(1 comment)

--- Message (Xiyang Wang, 2026-09-12 08:01:21.000000000) ---
Patch Set 1:

(1 comment)

--- Message (Zhenyu Xu, 2026-09-15 05:52:00.000000000) ---
Patch Set 1:

(6 comments)

--- Message (Zhenyu Xu, 2026-09-15 06:27:35.000000000) ---
Uploaded patch set 2.

Outdated Votes:
* Verified+1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-09-15 06:30:48.000000000) ---
Patch Set 2:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-09-15 06:33:55.000000000) ---
Patch Set 2:

(4 comments)

4 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (Lustre RISC-V Builder, 2026-09-15 06:42:27.000000000) ---
Patch Set 2:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-09-15 06:49:41.000000000) ---
Patch Set 2: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/131763/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-09-16 07:36:05.000000000) ---
Patch Set 2:

(3 comments)

AI code review results below, please doublecheck

Mostly good, comments inline. One case looks like it needs another look before landing: capping the whole-file-read window by the budget leaves ras_whole_file_read set even when the window no longer covers the file, which turns off readahead for the rest of it.

--- Message (Xiyang Wang, 2026-09-22 11:49:26.000000000) ---
Patch Set 2: Code-Review+1

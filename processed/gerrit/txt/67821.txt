Change:     67821
Subject:    LU-20559 target: drain zombie exports before invalidating obt
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/67821
Change-Id:  I11f6f125f3bb679f4b7ff70560d3c12c9b54cf99
Project:    fs/lustre-release
Branch:     master
Status:     NEW
Owner:      Oleg Drokin
Created:    2026-08-07 16:51:05.000000000
Updated:    2026-08-24 08:08:49.000000000
Patch Set:  3
Insertions: +17
Deletions:  -0
Comments:   2 (1 unresolved)

--- Labels ---
  Code-Review: wc-checkpatch: -1; Andreas Dilger: +1
  Verified: jenkins: +1; Maloo: +1

--- Reviewers ---
  CC: Lustre RISC-V Builder, Gerrit AI review for Lustre
  REVIEWER: Andreas Dilger, Mikhail Pershin

--- Commit Message ---
LU-20559 target: drain zombie exports before invalidating obt

"don't LBUG destroying exports of a failed mount" guards the export-
teardown consumers and "keep client records when a mount fails" stops
the records being erased; neither serializes the teardown itself.

The exports tgt_clients_data_init() rebuilds from last_rcvd are
destroyed on the obd_zombid workqueue, so a worker can read a
still-valid obt_magic just before tgt_init()'s out_put label clears it
and go on to touch lut_client_bitmap after out_put has freed it.  Once
mdt_init0() unwinds to mdt_device_free(), the lu_target itself is
gone.  dt_object_put() sits between the obt_magic store and the frees
and can sleep, so the window is not instruction-scale.  No magic flag
can close it: the check and the use are separated by an unsynchronized
free.

Drain the queue at out_put while lut is still valid.
class_disconnect_exports() splices obd_exports into a work list, which
is what satisfies obd_exports_barrier()'s assertion that the list is
empty; obd_zombie_barrier() then waits for the culls to finish.  This
covers the tgt_init() error paths that mdt_init0()'s err_tgt label
never reaches, because tgt_init() failure jumps to err_free_ns.
OBDF_FAIL is set first, for the same reason tgt_server_data_init()'s
err_client label now sets it.

OBDF_FAIL is not cleared afterwards, so anything that reads it later
on the way out of the mount sees a failover-style teardown rather than
a normal one.  That is what mdt_init0()'s err_tgt has always left
behind for the failures it covers, so this makes a tgt_init() failure
unwind the same way rather than differently; the err_client site added
earlier in this series leaves the same state.

The consumer guards stay.  Not because a cull can outlive this - after
the barriers none can, obd_zombie_export_add() counts the export
before it leaves obd_unlinked_exports and obd_zombie_barrier() waits
that count to zero - but because they cover the teardown paths that
never reach tgt_init()'s out_put at all, and because they can be
backported on their own, which this ordering change should not be.

"LU-20559 target: drop an export reference on reply_data error" is a
hard prerequisite for this patch, not just an adjacent fix: without it
the tgt_set_reply_slot() failure path leaves an export reference
behind, the export never leaves obd_unlinked_exports, and the
obd_exports_barrier() added here - which has no timeout - leaves the
mount thread unkillable.

The drain is not conditional on obd_exports being non-empty, even
though only the failures arriving through out: can have anything on
that list. err_client has already spliced it empty while its exports
are still in flight on the workqueue, which is the case the reported
crash comes from, so a list_empty() gate would skip the drain exactly
where it is needed. The cost is that a mount failing earlier now waits
on obd_zombie_barrier() with nothing of its own to drain.

conf-sanity 138 does not reach the new disconnect: fail_loc 0x723
fails inside tgt_server_data_init(), whose err_client label has
already spliced obd_exports away, so only the barriers do any work
there.  A follow-on patch adds the injection and the case that does
reach it; it is kept separate because it exists only to exercise this
path, and nothing in it is needed for the fix.

Fixes: 0d3a07a8aa46 ("LU-7430 mdt: better handle MDT recovery error path")
Signed-off-by: Oleg Drokin <green@whamcloud.com>
Assisted-by: ClaudeCode:Opus-5 llm_code_and_review_tools
Change-Id: I11f6f125f3bb679f4b7ff70560d3c12c9b54cf99

--- Review History ---

--- Message (Oleg Drokin, 2026-08-07 16:51:05.000000000) ---
Uploaded patch set 1.

--- Message (wc-checkpatch, 2026-08-07 16:56:55.000000000) ---
Patch Set 1:

Looks good to me.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-07 17:03:09.000000000) ---
Patch Set 1:

Code-wise looks good to me.

--- Message (Lustre RISC-V Builder, 2026-08-07 18:04:35.000000000) ---
Patch Set 1:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-08-07 19:51:25.000000000) ---
Patch Set 1: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/128909/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-08-08 11:05:06.000000000) ---
Patch Set 1:

(1 comment)

AI code review results below, please doublecheck

Looks correct; the drain/barrier ordering checks out and no defects found. One process point: with 138 stopping at tgt_server_data_init(), nothing in the tree exercises the new disconnect with live exports, so the part that actually changes behaviour (mount thread running mdt_obd_disconnect() -> mdt_export_cleanup() against a half-built mdt_device, and the cull running against a still-valid lut) never runs in CI. Consider landing the follow-on injection patch together with this one.

--- Message (Oleg Drokin, 2026-08-08 15:56:39.000000000) ---
Uploaded patch set 2: Patch Set 1 was rebased.

Outdated Votes:
* Verified+1, Verified-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-08 16:08:40.000000000) ---
Patch Set 2:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-08-08 16:11:14.000000000) ---
Patch Set 2:

Looks good to me.

--- Message (Oleg Drokin, 2026-08-08 16:24:57.000000000) ---
Patch Set 2:

(1 comment)

--- Message (Lustre RISC-V Builder, 2026-08-08 16:39:11.000000000) ---
Patch Set 2:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-08-08 17:14:07.000000000) ---
Patch Set 2: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/128956/ : SUCCESS

--- Message (Oleg Drokin, 2026-08-09 19:01:14.000000000) ---
Uploaded patch set 3: Patch Set 2 was rebased.

Outdated Votes:
* Verified+1, Verified-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (wc-checkpatch, 2026-08-09 19:05:25.000000000) ---
Patch Set 3:

Looks good to me.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-09 19:24:02.000000000) ---
Patch Set 3:

Code-wise looks good to me.

--- Message (jenkins, 2026-08-09 20:22:08.000000000) ---
Patch Set 3: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/128986/ : SUCCESS

--- Message (Lustre RISC-V Builder, 2026-08-09 21:01:07.000000000) ---
Patch Set 3:

RISC-V Client Builder: build succesful

--- Message (Marc Vef, 2026-08-13 13:19:43.000000000) ---
Hashtag added: master-next

--- Message (Marc Vef, 2026-08-13 13:19:44.000000000) ---
Patch Set 3:

[Marc Bot] Note: This change has been cherry-picked to master-next and is considered for landing. Please avoid updates unless required while the master-next hashtag is set; doing so may cause the patch to be removed from master-next and not landed. Note: AI review comments should be addressed/responded to.

--- Message (Marc Vef, 2026-08-13 13:19:44.000000000) ---
Removed cc <GERRIT_ACCOUNT_1055>.

--- Message (Autotest, 2026-08-18 20:46:25.000000000) ---
Patch Set 3:

Trevis Autotest 2 retesting enforced review-dne-zfs-part-4 session on el10.1-x86_64/zfs servers and clients for build 128986 patch 3. Requested by Andreas Dilger because of LU-17077.

--- Message (Andreas Dilger, 2026-08-18 23:56:27.000000000) ---
Patch Set 3: Code-Review+1

--- Message (Autotest, 2026-08-19 00:06:40.000000000) ---
Patch Set 3:

Onyx Autotest 2 retesting enforced review-dne-zfs-part-4 session on el10.1-x86_64/zfs servers and clients for build 128986 patch 3. Requested by Andreas Dilger because of LU-17077.

--- Message (wc-checkpatch, 2026-08-23 04:39:49.000000000) ---
Patch Set 3: Code-Review-1

This change cannot be cherry-picked to master.
Please rebase the change locally and upload the rebased commit for review.

--- Message (Marc Vef, 2026-08-24 08:08:48.000000000) ---
Hashtag removed: master-next

--- Message (Marc Vef, 2026-08-24 08:08:48.000000000) ---
Patch Set 3:

[Marc Bot] Note: This change has been removed from master-next and is no longer considered for landing in its current form.

--- Message (Marc Vef, 2026-08-24 08:08:49.000000000) ---
Removed cc <GERRIT_ACCOUNT_1055>.

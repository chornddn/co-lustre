Change:     68417
Subject:    LU-20650 lfs: find --since, from the changelog
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/68417
Change-Id:  I13426ef1ed12cd08d78aa5a5da9f8ba4465690d2
Project:    fs/lustre-release
Branch:     master
Status:     NEW
Owner:      Hiroshi Nishida
Created:    2026-08-26 16:04:40.000000000
Updated:    2026-08-31 15:34:13.000000000
Patch Set:  6
Insertions: +766
Deletions:  -2
Comments:   37 (11 unresolved)

--- Labels ---
  Code-Review: (no votes)
  Verified: (no votes)

--- Reviewers ---
  CC: Lustre RISC-V Builder, Gerrit AI review for Lustre

--- Commit Message ---
LU-20650 lfs: find --since, from the changelog

The three pieces were already here.  llapi_scan_changelog() says which
objects had an event, llapi_scan_fid() fills each one's record the way
a walk fills it, and find_decide() judges it with the predicates a
walk uses.  --since composes them, so it answers exactly what a plain
find answers over fewer objects: a strict subset, narrowed and never
different.  No new predicates and no second copy of the vocabulary.

The argument is never guessed.  A bare integer is an index, which is
what "lfs changelog MDT <startrec>" already takes; a single-unit
duration (30m, 2h, 7d) is that far back from now; a timestamp or
@<seconds> is that time; anything else is an error naming the four
spellings.  A leading sign is refused rather than wrapped through
strtoull(), and a duration reaching past the epoch is a typo rather
than a window.

cr_index belongs to one MDT, so a bare index is refused on a
filesystem with more than one and the message says which time
spellings do work.  That count has to be right, and the obvious source
is not: LL_IOC_GETOBDCOUNT answers with the size of the LMV's target
array, so a single-MDT filesystem reports 64.  It is used as an upper
bound and each index confirmed with llapi_search_tgt() before its
changelog is read.

A changelog covers the whole filesystem, so the subtree the caller
named is enforced against the pathname llapi_scan_fid() resolved, and
the depth the walk would have counted is recovered from it for
--maxdepth and --mindepth.  Without that, a find rooted at one
directory would answer for its siblings.

Against every name the object has, not just the resolved one.
llapi_scan_fid() resolves linkno 0, so a hardlinked file arrives under
whichever name fid2path answered with first: with /a/f and /b/f the
same inode, "lfs find /a --since 2h" would be handed b/f, reject it,
and lose a file a walk of /a prints.  -name has the same shape, which
is why a target scan walks every linkea name.  The names come from
fid2path here, one ioctl each, and only a file whose link count says
it has another name pays for any of them.

A time anchor is cut on the record rather than at the start index,
because a time cannot be turned into a per-MDT index without reading
the log; coalescing has already reduced an object to its latest event,
so an object whose latest event is older than the window did not
change in it.  An object unlinked between its event and now cannot be
verified, so it is not in the answer -- that is what the flag gives up
-- and the run says how many were dropped that way.

--since is refused for a target scan rather than ignored.  The option
table is compiled into lfind as well, so `lfind --local --since 2h`
parsed and then scanned the whole target: llapi_find_device() has no
changelog to anchor on and never reads fp_since_kind.  It joins the
list find_device_supported() already keeps for --maxdepth, --threads
and -printf.

The mount point is found from the resolved root and not from the
spelling, which are the same string only when the caller already gave
a canonical absolute path.  /home/user/data -> /mnt/lustre/data
answered -ENODEV, llapi_search_mounts() matching mnt_dir literally,
and a relative path went through the fsname to the *first* mount of
that filesystem -- so under $MOUNT2 every object fell outside the
subtree and the search printed nothing and exited 0.

What is printed is then the caller's spelling of that root with the
resolved remainder appended, because a walk prints the path as it was
given -- param_callback() copies the argument and
llapi_semantic_traverse() appends to it.  Named from the mount every
time, "lfs find . --since 2h" would answer /mnt/lustre/sub/f where
"lfs find ." answers ./sub/f, and --since promises the same answer as
the search without it.

ONLY=56 is the regression check that the shared paths still answer as
they did; the subtests for the new options arrive with the tests
patch at the end of this series.

Test-Parameters: testlist=sanity env=ONLY=56
Signed-off-by: Hiroshi Nishida <hnishida@thelustrecollective.com>
Assisted-by: ClaudeCode:Opus-5 llm_code_and_review_tools
Change-Id: I13426ef1ed12cd08d78aa5a5da9f8ba4465690d2

--- Review History ---

--- Message (Hiroshi Nishida, 2026-08-26 16:04:40.000000000) ---
Uploaded patch set 1.

--- Message (wc-checkpatch, 2026-08-26 16:13:09.000000000) ---
Patch Set 1:

(1 comment)

1 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-26 16:32:22.000000000) ---
Patch Set 1:

Code-wise looks good to me.

--- Message (jenkins, 2026-08-27 04:24:36.000000000) ---
Patch Set 1: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/130408/ : SUCCESS

--- Message (Lustre RISC-V Builder, 2026-08-27 16:04:45.000000000) ---
Patch Set 1:

RISC-V Client Builder: build succesful

--- Message (Gerrit AI review for Lustre, 2026-08-27 21:51:23.000000000) ---
Patch Set 1:

(11 comments)

AI code review results below, please doublecheck

Some concerns, see inline comments. The main one is that this path skips find_prefilter(), so --since answers a different question than a plain find for -name and --mdt-count. There is also no test: the patch adds a user-visible option but nothing under lustre/tests/ exercises it, and Test-Parameters only re-runs sanity test_56 as it already was; a case that creates files, reads them back with 'lfs find <dir> --since <n>m' and compares against the same find without --since would have caught the -name gap.

--- Message (Hiroshi Nishida, 2026-08-28 00:00:00.000000000) ---
Uploaded patch set 2.

Outdated Votes:
* Verified+1, Verified-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (wc-checkpatch, 2026-08-28 00:09:14.000000000) ---
Patch Set 2:

(1 comment)

1 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-28 00:38:32.000000000) ---
Patch Set 2:

Code-wise looks good to me.

--- Message (Lustre RISC-V Builder, 2026-08-28 02:04:05.000000000) ---
Patch Set 2:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-08-28 02:35:24.000000000) ---
Patch Set 2: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/130498/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-08-28 20:26:50.000000000) ---
Patch Set 2:

(4 comments)

AI code review results below, please doublecheck

Some concerns, see inline comments. The rest of this series lands its user-visible surface with coverage (sanity.sh for llapi_scan_namespace, conf-sanity.sh for the device scans, a C test for llapi_scan_changelog), but nothing here exercises --since: no sanity 56 subtest builds a changelog and checks that `lfs find <dir> --since <t>` returns the same set as the plain find restricted to what changed, and none of the four argument spellings or the multi-MDT index refusal is tested. Could a test_56 case be added?

--- Message (Hiroshi Nishida, 2026-08-29 13:32:34.000000000) ---
Uploaded patch set 3.

Outdated Votes:
* Verified+1, Verified-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (wc-checkpatch, 2026-08-29 13:44:30.000000000) ---
Patch Set 3:

(1 comment)

1 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-29 14:07:58.000000000) ---
Patch Set 3:

Code-wise looks good to me.

--- Message (Hiroshi Nishida, 2026-08-29 14:48:00.000000000) ---
Patch Set 3:

(4 comments)

--- Message (Lustre RISC-V Builder, 2026-08-29 15:12:12.000000000) ---
Patch Set 3:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-08-29 15:13:04.000000000) ---
Patch Set 3: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/130580/ : SUCCESS

--- Message (Hiroshi Nishida, 2026-08-29 18:09:55.000000000) ---
Uploaded patch set 4: Patch Set 3 was rebased.

Outdated Votes:
* Verified+1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (wc-checkpatch, 2026-08-29 18:23:47.000000000) ---
Patch Set 4:

(1 comment)

1 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-29 18:38:21.000000000) ---
Patch Set 4:

Code-wise looks good to me.

--- Message (Lustre RISC-V Builder, 2026-08-29 19:12:06.000000000) ---
Patch Set 4:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-08-29 19:54:16.000000000) ---
Patch Set 4: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/130599/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-08-30 10:12:08.000000000) ---
Patch Set 4:

(2 comments)

AI code review results below, please doublecheck

Mostly good, comments inline. One thing that looks worth fixing before landing: lfs_find_parse.c is compiled into lfind as well, so lfind now accepts --since and ignores it. The new behaviour also appears untested -- Test-Parameters names sanity ONLY=56 but no test_56* is added, and neither the index form, the four --since spellings, the subtree/maxdepth bound nor the hardlink case is exercised anywhere.

--- Message (Hiroshi Nishida, 2026-08-30 14:47:27.000000000) ---
Uploaded patch set 5.

Outdated Votes:
* Verified+1, Verified-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (Hiroshi Nishida, 2026-08-30 14:48:21.000000000) ---
Patch Set 5:

(2 comments)

--- Message (wc-checkpatch, 2026-08-30 15:01:45.000000000) ---
Patch Set 5:

(1 comment)

1 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-30 15:16:18.000000000) ---
Patch Set 5:

Code-wise looks good to me.

--- Message (Lustre RISC-V Builder, 2026-08-30 15:47:41.000000000) ---
Patch Set 5:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-08-30 16:45:10.000000000) ---
Patch Set 5: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/130632/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-08-31 08:08:09.000000000) ---
Patch Set 5:

(4 comments)

AI code review results below, please doublecheck

Mostly good, comments inline. The new option is user-visible and there is no test for it anywhere under lustre/tests/ -- would a sanity case that writes a file, then checks `lfs find $DIR --since <window>` finds it and `--since <newer window>` does not, be worth adding here?

--- Message (Hiroshi Nishida, 2026-08-31 14:07:31.000000000) ---
Uploaded patch set 6.

Outdated Votes:
* Verified+1, Verified-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (Hiroshi Nishida, 2026-08-31 14:08:42.000000000) ---
Patch Set 5:

(4 comments)

--- Message (wc-checkpatch, 2026-08-31 14:20:45.000000000) ---
Patch Set 6:

(1 comment)

1 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-31 14:39:03.000000000) ---
Patch Set 6:

Code-wise looks good to me.

--- Message (Lustre RISC-V Builder, 2026-08-31 15:34:13.000000000) ---
Patch Set 6:

RISC-V Client Builder: build succesful

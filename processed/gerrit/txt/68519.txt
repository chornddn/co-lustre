Change:     68519
Subject:    LU-17000 ofd: do not use a list iterator after the loop
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/68519
Change-Id:  Ia666b7198ba22b2c0f7a89c21b2d42473d7daf5c
Project:    fs/lustre-release
Branch:     master
Status:     NEW
Owner:      Oleg Drokin
Created:    2026-08-31 05:11:56.000000000
Updated:    2026-09-01 10:37:36.000000000
Patch Set:  2
Insertions: +3
Deletions:  -2
Comments:   3 (0 unresolved)

--- Labels ---
  Code-Review: Arshad Hussain: +1; Andreas Dilger: +1
  Verified: jenkins: +1; Maloo: +1

--- Reviewers ---
  CC: Gerrit AI review for Lustre
  REVIEWER: Oleg Drokin, Andreas Dilger, Arshad Hussain

--- Commit Message ---
LU-17000 ofd: do not use a list iterator after the loop

ofd_intent_cb() decides "found it" from the list_for_each_entry()
iterator after the loop.  When the loop runs to completion the iterator
holds a pointer computed from the list head, which is not NULL, so the
later test succeeds and the code works on a bogus object.

The OBD_FAIL_OST_GL_WORK_ALLOC injection deliberately skips the
allocation and relies on gl_work still being NULL, but the preceding
loop has already overwritten it, so the simulated ENOMEM path is never
taken.  A real allocation failure assigns NULL and is unaffected.

gl_list sits at offset 8 of struct ldlm_glimpse_work and is the first
member of struct ofd_intent_args, so the stale iterator resolves to
(char *)&arg - 8 in ofd_intent_policy()'s frame.  The gl_lock store
lands 8 bytes below arg, outside the object, and list_add_tail() then
links arg.gl_list to itself, leaving the list empty, so the cleanup
loop in ofd_intent_policy() never runs and the ldlm_lock_get()
reference is leaked.  The gl_desc and gl_flags stores that follow would
overwrite arg.no_glimpse_ast, arg.error and arg.size; on a
CONFIG_BUG_ON_DATA_CORRUPTION kernel the list_add_tail() kills the
thread first, and without CONFIG_DEBUG_LIST none of it is caught at
all.

Walk the list with a separate variable so the result only ever holds a
real list entry.

Reproduced on an OSS with fail_loc=0x240: without this patch the node
panics 20us later with "list_add double add: new=X, prev=X, next=X" at
lib/list_debug.c:29, in ofd_intent_cb().  With it the injection returns
-ENOMEM as intended and the OSS stays up.

Test-Parameters: trivial
Fixes: a8dcf372f430 ("LU-6179 llite: Implement ladvise lockahead")
Signed-off-by: Oleg Drokin <green@whamcloud.com>
Assisted-by: ClaudeCode:Opus-5 llm_code_and_review_tools
Change-Id: Ia666b7198ba22b2c0f7a89c21b2d42473d7daf5c

--- Review History ---

--- Message (Oleg Drokin, 2026-08-31 05:11:56.000000000) ---
Uploaded patch set 1.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-31 05:14:42.000000000) ---
Patch Set 1:

Code-wise looks good to me, 1 warnings less than parent.

--- Message (wc-checkpatch, 2026-08-31 05:21:56.000000000) ---
Patch Set 1:

Looks good to me.

--- Message (Oleg Drokin, 2026-08-31 05:34:05.000000000) ---
Patch Set 1: Code-Review-1

(1 comment)

--- Message (jenkins, 2026-08-31 05:46:25.000000000) ---
Patch Set 1: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/130691/ : SUCCESS

--- Message (Oleg Drokin, 2026-08-31 05:53:43.000000000) ---
Uploaded patch set 2.

Outdated Votes:
* Code-Review-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE OR changekind:TRIVIAL_REBASE OR changekind:TRIVIAL_REBASE_WITH_MESSAGE_UPDATE OR is:MIN")
* Verified+1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-31 05:56:22.000000000) ---
Patch Set 2:

Code-wise looks good to me, 1 warnings less than parent.

--- Message (wc-checkpatch, 2026-08-31 06:07:56.000000000) ---
Patch Set 2:

Looks good to me.

--- Message (jenkins, 2026-08-31 06:18:57.000000000) ---
Patch Set 2: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/130694/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-08-31 12:54:03.000000000) ---
Patch Set 2:

AI code review results below, please doublecheck

The fix looks correct - the iterator can now only ever hold a real list entry, and the OBD_FAIL_OST_GL_WORK_ALLOC path reaches the -ENOMEM handling again. The offsets, the empty-list list_add_tail() behaviour and the leaked ldlm_lock_get() reference described in the commit message all check out against the current code, and the Fixes: sha and subject are accurate.

One thought: the broken path was only reachable through fail_loc 0x240, and nothing under lustre/tests/ has ever set that value since the injection point was added in 2017. Would it be worth adding a small test that sets fail_loc=0x240 on the OSS and then glimpses a file with an outstanding PW lock beyond the LVB size, checking that the OSS survives and the client sees the error? That would stop this injection path from silently rotting again.

--- Message (Andreas Dilger, 2026-08-31 17:51:27.000000000) ---
Patch Set 2: Code-Review+1

--- Message (Arshad Hussain, 2026-09-01 10:37:36.000000000) ---
Patch Set 2: Code-Review+1

Change:     68453
Subject:    LU-12669 ec: create lock placeholder for reconstructed pages
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/68453
Change-Id:  I57453459ec3aee4de72c012ffa5258153cf4b408
Project:    fs/lustre-release
Branch:     master
Status:     NEW
Owner:      Zhenyu Xu
Created:    2026-08-28 12:04:27.000000000
Updated:    2026-08-29 10:48:11.000000000
Patch Set:  1
Insertions: +292
Deletions:  -24
Comments:   21 (16 unresolved)

--- Labels ---
  Code-Review: (no votes)
  Verified: jenkins: +1; Maloo: -1

--- Reviewers ---
  CC: Lustre RISC-V Builder, Gerrit AI review for Lustre

--- Commit Message ---
LU-12669 ec: create lock placeholder for reconstructed pages

Create client-only ldlm_lock placeholders for sub-locks on
unavailable OSTs (LSS_READ_ERR) during CIT_EC_RD, so reconstructed
pages stay cached and Uptodate across reads instead of being
discarded after each read.  The ldlm_lock is granted locally without
an RPC (ldlm_lock_enqueue with LDLM_FL_BLOCK_GRANTED) and attached to
the osc_lock via osc_lock_granted.

osc_lock_cancel() only detaches the ldlm_lock (osc_lock_detach)
without cancelling it, so the ldlm_lock survives the EC read loop and
keeps pages cached for concurrent readers.  When the OST recovers,
osc_import_event(IMP_EVENT_ACTIVE) calls ldlm_cli_cancel_unused()
which cancels locks with 0 readers/writers, the blocking-AST path
(osc_ldlm_blocking_ast -> __osc_dlm_blocking_ast -> osc_lock_flush),
flushing stale pages.  LRU pressure cancels the same way via
osc_ldlm_weigh_ast.

Hoist cli in osc_import_event before the switch. Add cl_lock_cancel()
before cl_lock_fini() in the lov_lock_init error path so initialized
sub-locks are properly cleaned up.

Add a test_49a second-read and OST-reactivation check to verify pages
stay cached and are invalidated when the OST comes back.

Assisted-by: Augment Agent:GLM 5.2
Fixes: f240b09615b2 ("LU-12669 ec: fix reader deadlock, stale cache, EAGAIN leak")
Signed-off-by: Bobi Jam <bobijam@whamcloud.com>
Change-Id: I57453459ec3aee4de72c012ffa5258153cf4b408

--- Review History ---

--- Message (Zhenyu Xu, 2026-08-28 12:04:27.000000000) ---
Uploaded patch set 1.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-28 12:07:02.000000000) ---
Patch Set 1:

(2 comments)

2 code warning(s).
If you believe the warnings are incorrect, please leave a comment with your explanations as a reply to the warning.

--- Message (wc-checkpatch, 2026-08-28 12:17:48.000000000) ---
Patch Set 1:

(5 comments)

5 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (jenkins, 2026-08-28 13:37:18.000000000) ---
Patch Set 1: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/130538/ : SUCCESS

--- Message (Lustre RISC-V Builder, 2026-08-28 14:40:51.000000000) ---
Patch Set 1:

RISC-V Client Builder: build succesful

--- Message (Gerrit AI review for Lustre, 2026-08-29 08:08:05.000000000) ---
Patch Set 1:

(14 comments)

AI code review results below, please doublecheck

Problematic, see inline comments. The core mechanism does not appear to work as described: on a client namespace ldlm_lock_enqueue() with LDLM_FL_BLOCK_GRANTED puts the lock on lr_waiting rather than granting it, the lock has no l_conn_export and no extent policy, and LDLM has an explicit LASSERTF against locally-created locks in a client namespace (ldlm_cli_enqueue_local()). Since the placeholder never functions, the ClearPageUptodate() removal in vvp_io_ec_rd_end() leaves reconstructed pages Uptodate with nothing able to invalidate them. It would help to build this on ldlm_cli_enqueue_local()'s pattern (policy assignment, addref, ldlm_set_local(), the final ldlm_lock_put()) rather than open-coding it, and to exercise the recovery path with a real OSC import deactivate/reactivate so IMP_EVENT_ACTIVE is actually delivered.

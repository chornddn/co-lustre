Change:     67323
Subject:    LU-19955 osd: flush the fallocate range even when it fails
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/67323
Change-Id:  I46629c6a3b91ce7f703a32148fd1beef1a0869be
Project:    fs/lustre-release
Branch:     master
Status:     NEW
Owner:      Oleg Drokin
Created:    2026-07-17 04:11:01.000000000
Updated:    2026-07-17 10:48:52.000000000
Patch Set:  2
Insertions: +48
Deletions:  -10
Comments:   6 (0 unresolved)

--- Labels ---
  Code-Review: (no votes)
  Verified: jenkins: +1

--- Reviewers ---
  CC: Lustre RISC-V Builder

--- Commit Message ---
LU-19955 osd: flush the fallocate range even when it fails

osd_execute_fallocate() only flushes the range it punched back out of
the page cache when the fallocate itself returned success:

	rc = file->f_op->fallocate(file, mode, start, end - start);
	if (rc == 0)
		osd_partial_page_flush_punch(d, inode, start, end - 1);

ldiskfs dirties as it goes and can fail afterwards.  ldiskfs_punch_hole()
zeroes the partial blocks at the edges of the range first, and only then
removes the extents:

	ret = ldiskfs_zero_partial_blocks(handle, inode, offset, length);
	if (ret)
		goto out_stop;
	...
	if (stop_block > first_block) {
		...
		ret = ldiskfs_ext_remove_space(inode, first_block,
					       stop_block - 1);

so an error from the extent removal returns from a fallocate that has
already left dirty pages in the page cache.  The caller ignores the
error by design and only logs it, so the object stays in use with those
pages still dirty and never written back.

That is not only the LU-19955 assert.  A partial write landing on such a
page makes osd_write_prep() read the block back from disk to complete
it, which overwrites the zeroing the page was still holding, so the
zeroed range silently reverts.  With the error injected, fsx reports
data corruption long before it reaches the assert:

  READ BAD DATA: offset = 0x16991, size = 0x6bf2

The flush was unconditional when it was introduced by LU-14640, and
became conditional while the surrounding code was reworked to use
alloc_file_pseudo().  Restore the unconditional flush; it is harmless
on a range that was not touched.

Add OBD_FAIL_OSD_FALLOCATE_ERR to report an error out of a fallocate
that already did its zeroing, which reproduces the corruption on demand:
sanity/150id fails in 14s without this patch and passes with it.

Fixes: b0f150eba4 ("LU-13783 osd-ldiskfs: use alloc_file_pseudo to create fake files")
Test-Parameters: testlist=sanity,sanityn,sanity-benchmark
Signed-off-by: Oleg Drokin <green@whamcloud.com>
Assisted-by: ClaudeCode:Opus-4.8 llm_code_and_review_tools
Change-Id: I46629c6a3b91ce7f703a32148fd1beef1a0869be

--- Review History ---

--- Message (Oleg Drokin, 2026-07-17 04:11:01.000000000) ---
Uploaded patch set 1.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-07-17 04:14:12.000000000) ---
Patch Set 1:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-07-17 04:19:30.000000000) ---
Patch Set 1:

(2 comments)

2 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (Lustre RISC-V Builder, 2026-07-17 04:24:48.000000000) ---
Patch Set 1:

RISC-V Client Builder: build succesful

--- Message (Oleg Drokin, 2026-07-17 04:38:00.000000000) ---
Patch Set 1:

(1 comment)

--- Message (Oleg Drokin, 2026-07-17 04:39:45.000000000) ---
Uploaded patch set 2.

--- Message (jenkins, 2026-07-17 04:39:59.000000000) ---
Patch Set 1: Verified-1

Build Failed 

https://build.whamcloud.com/job/lustre-reviews/127713/ : ABORTED

--- Message (Oleg Drokin, 2026-07-17 04:41:03.000000000) ---
Patch Set 2:

(1 comment)

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-07-17 04:42:57.000000000) ---
Patch Set 2:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-07-17 04:49:59.000000000) ---
Patch Set 2:

(2 comments)

2 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (Lustre RISC-V Builder, 2026-07-17 04:52:15.000000000) ---
Patch Set 2:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-07-17 05:27:52.000000000) ---
Patch Set 2: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/127714/ : SUCCESS

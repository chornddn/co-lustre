Change:     67533
Subject:    LU-20523 target: don't LBUG when the device shrinks
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/67533
Change-Id:  I3df1560b83bb3c76f89dc11c9281ed1f7d4116bf
Project:    fs/lustre-release
Branch:     master
Status:     NEW
Owner:      Oleg Drokin
Created:    2026-07-25 21:30:28.000000000
Updated:    2026-08-05 20:03:59.000000000
Patch Set:  2
Insertions: +89
Deletions:  -10
Comments:   11 (6 unresolved)

--- Labels ---
  Code-Review: (no votes)
  Verified: jenkins: +1; Maloo: +1

--- Reviewers ---
  CC: Lustre RISC-V Builder, Gerrit AI review for Lustre
  REVIEWER: Andreas Dilger

--- Commit Message ---
LU-20523 target: don't LBUG when the device shrinks

tgt_grant_sanity_check() panics the server whenever the space granted
to a client exceeds the size of the backing device, assuming that such
a value can only come from counter corruption.  The device size is not
an invariant: a ZFS dataset quota can be lowered at runtime, and
os_blocks is (used + avail), so the reported total collapses to little
more than the used space.  Space granted while the device was large is
still legitimately outstanding, and the next statfs takes the server
down:

  lustre-MDT0000: cli 2665b580.../... ted_grant(29622272) +
                  ted_pending(0) > maxsize(6946816)
  (tgt_grant.c:209:tgt_grant_sanity_check()) LBUG

sanity test_805a does exactly this to the MDT: it lowers the MDT dataset
quota to (used + 5MB) to reach ENOSPC quickly, so any client holding
DoM grant from earlier tests panics the MDS - in test_805a itself, or in
a later test whose statfs runs the check (test_807a).

This is not new: the same panic has been recorded in test_805a (and in a
later statfs of test_806/807a) since 2022-01, on master, b2_15 and in
review runs, with per-export grants from 3MB to 355MB against a maxsize
of 1MB to 64MB.

Compare against the largest device size seen since mount rather than
the current cached one, so grants that were legitimate when they were
issued are not mistaken for corruption, while corrupt counters are
still caught.

Only the fatal check moves: the aggregate warnings at the end of
tgt_grant_sanity_check() keep comparing against the size the device
reports now, so an over-committed target still says so in the log
instead of going quiet - they just repeat now, rather than being cut
short by the LBUG() on the first statfs.  CERROR() is CDEBUG_LIMIT() on
D_ERROR, so repeating does not flood the console.

The sample is scaled by the shift that came with it, which is what the
preceding patch made tgt_grant_space_left() use as well, so the recorded
size and the space the allocator is willing to hand out stay in the same
units across a ZFS recordsize change.

The check stays fatal.  What was wrong is the threshold, not the
reaction to crossing it: gating these LBUG() calls on
lbug_on_grant_miscount would also stop catching the genuine counter
corruption the check exists for, which is a separate decision.

Add sanity test_805c, which takes DoM grant on the MDT and then shrinks
the size reported to the grant code with OBD_FAIL_TGT_STATFS_SHRINK.

Fixes: 71d68757c76c ("LU-1406 ofd: grant support")
Signed-off-by: Oleg Drokin <green@whamcloud.com>
Assisted-by: ClaudeCode:Opus-5 llm_code_and_review_tools
Change-Id: I3df1560b83bb3c76f89dc11c9281ed1f7d4116bf

--- Review History ---

--- Message (Oleg Drokin, 2026-07-25 21:30:28.000000000) ---
Uploaded patch set 1.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-07-25 21:33:01.000000000) ---
Patch Set 1:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-07-25 21:40:27.000000000) ---
Patch Set 1:

Looks good to me.

--- Message (Lustre RISC-V Builder, 2026-07-25 21:45:45.000000000) ---
Patch Set 1:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-07-25 21:56:13.000000000) ---
Patch Set 1: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/128099/ : SUCCESS

--- Message (Andreas Dilger, 2026-07-26 06:33:37.000000000) ---
Patch Set 1: Code-Review+1

(2 comments)

--- Message (Oleg Drokin, 2026-07-26 07:16:17.000000000) ---
Patch Set 1:

(1 comment)

--- Message (Gerrit AI review for Lustre, 2026-07-27 20:07:33.000000000) ---
Patch Set 1:

(3 comments)

AI code review results below, please doublecheck

Experimental feature testing in progress, please pay attention to the comments

Mostly good, comments inline. The approach is sound and the Fixes: tag checks out, but the mount-time sample of the new maxsize is recorded before tgd_blockbits is known, and the new test can pass without ever reaching the shrunk cached statfs.

--- Message (Oleg Drokin, 2026-08-04 01:39:19.000000000) ---
Uploaded patch set 2.

Outdated Votes:
* Code-Review+1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE OR changekind:TRIVIAL_REBASE OR changekind:TRIVIAL_REBASE_WITH_MESSAGE_UPDATE OR is:MIN")
* Verified+1, Verified-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-04 01:45:03.000000000) ---
Patch Set 2:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-08-04 01:51:51.000000000) ---
Patch Set 2:

(2 comments)

2 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (jenkins, 2026-08-04 02:11:38.000000000) ---
Patch Set 2: Verified-1

Build Failed 

https://build.whamcloud.com/job/lustre-reviews/128622/ : FAILURE

--- Message (Lustre RISC-V Builder, 2026-08-04 02:13:24.000000000) ---
Patch Set 2:

RISC-V Client Builder: build succesful

--- Message (Oleg Drokin, 2026-08-05 04:52:55.000000000) ---
Patch Set 2:

(1 comment)

--- Message (jenkins, 2026-08-05 07:13:19.000000000) ---
Patch Set 2: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/128704/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-08-05 20:03:59.000000000) ---
Patch Set 2:

(2 comments)

AI code review results below, please doublecheck

Mostly good, minor comments inline. The peak-vs-current split holds up: tgd_osfs_peaksize is written in the same tgd_osfs_lock section that publishes tgd_osfs, so peaksize >= maxsize is an invariant and the fatal check can never become stricter than before, while tgt_grant_alloc() bounds every allocation by the cached os_bavail of the statfs that produced that peak, so a legitimate ted_grant + ted_pending stays under it.

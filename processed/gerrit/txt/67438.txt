Change:     67438
Subject:    LU-20501 osd-zfs: fix stale FID on generated ".." entries
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/67438
Change-Id:  I0e999b877991e3c784d3f38a8d43ada0d99e7c44
Project:    fs/lustre-release
Branch:     master
Status:     NEW
Owner:      Oleg Drokin
Created:    2026-07-22 02:24:37.000000000
Updated:    2026-08-05 14:06:07.000000000
Patch Set:  5
Insertions: +134
Deletions:  -2
Comments:   9 (6 unresolved)
Hashtags:   testfix, master-next

--- Labels ---
  Code-Review: Lustre Gerrit Janitor: -1
  Verified: jenkins: +1

--- Reviewers ---
  CC: Lustre RISC-V Builder, Gerrit AI review for Lustre
  REVIEWER: Andreas Dilger, Alex Zhuravlev, James Simmons

--- Commit Message ---
LU-20501 osd-zfs: fix stale FID on generated ".." entries

ZFS does not store "." and ".." on disk, so osd-zfs generates them in
osd_dir_it_rec() and gets the ".." FID from osd_find_parent_fid(): from
the directory's linkEA, or failing that from the FID of the dnode named
by SA_ZPL_PARENT.  For the objects created directly under the OSD root
neither works.  They have no linkEA, and because Lustre objects live in
the OI ZAPs rather than in a POSIX tree, their SA_ZPL_PARENT names the
OI container that indexes them rather than the OSD root; that container
carries no LMA, so osd_get_fid_by_oid() returns -ENOENT for it.  On a
freshly formatted MDT here the ROOT directory is dnode 525, its
SA_ZPL_PARENT is 24 (an oi.N ZAP), and the OSD root is dnode 34.

That branch only skipped setting LUDA_FID and left lde_fid untouched, so
".." kept whatever the recycled dirent page happened to contain.
Instrumenting the value showed it varying between the correct FID, zero,
wild values such as [0xfcaae7467e00fcaa:0xc834a400:0x450008], and real
FIDs of unrelated objects.  That is how LU-20501 handed ll_get_name() a
".." entry whose FID matched the child being looked up.

Report the OSD root.  It is the real parent -- ROOT and update_log_dir
are entries in its ZAP -- and it is what osd-ldiskfs already reports for
the same directories, since osd_iget_fid() maps an LMA-less root inode
to lu_local_obj_fid(OSD_FS_ROOT_OID).  osd_scrub.c performs the same
mapping.  That FID is in FID_SEQ_LOCAL_FILE, so
lfsck_find_mdt_idx_by_fid() always resolves it to the local target and
the LFSCK takes the idx == lfsck_dev_idx() path, where ".." is a no-op.

Tracing every -ENOENT through a full sanity-lfsck run on a 2-MDT DNE
filesystem finds exactly three objects reaching this branch, all direct
children of the OSD root:

  lustre-MDT0000  [0x200000007:0x1:0x0]  FID_SEQ_ROOT, the fs root
  lustre-MDT0000  [0x20000000a:0x0:0x0]  FID_SEQ_UPDATE_LOG_DIR
  lustre-MDT0001  [0x20000000a:0x1:0x0]  FID_SEQ_UPDATE_LOG_DIR

.lustre/fid reaches it too, but only through a client readdir: the LFSCK
never opens it, since FID_SEQ_DOT_LUSTRE is below FID_SEQ_NORMAL and
lfsck_master_oit_engine() drops objects that are neither norm nor igif.

Gate that answer on the iterated object's own FID.  osd_find_parent_fid()
returns -ENOENT for any object whose linkEA is unreadable and whose
SA_ZPL_PARENT names an LMA-less dnode, and an object created with no
local parent hint stores the OI ZAP there -- that covers DNE remote
directories and striped shards.  The OSD root is not their parent, so
only ROOT and update_log_dir take it.

Everything else reaching this branch reports the object itself.  That is
not a claim about the parent; it is the value that leaks nothing, asserts
nothing false, and stays local, so lfsck_find_mdt_idx_by_fid() resolves
to this target and the LFSCK dotdot check is a no-op.  It is marked
LUDA_UNKNOWN to document that, the same way the sibling
osd_get_fid_by_oid() failure below is marked; nothing in the tree tests
that flag.  The absent LUDA_FID is what makes
ll_get_name() skip the entry, via the companion llite patch; readdir does
not gate on it at all -- ll_dir_read() builds the d_ino from lde_fid
whatever the flags say, and mdd_dir_page_build() only consults LUDA_FID
to spot .lustre and packs the entry either way.
The LFSCK reads lde_fid regardless of any flag, which is why the value
has to be sane and local rather than merely unmarked.

Zeroing lde_fid is not usable here.  A zero FID on ".." is a defined
value: lfsck_namespace_assistant_handler_p1() reads it as "parent
unknown" and flags LNTF_CHECK_PARENT.  The fs root is scanned by the
namespace LFSCK -- lfsck_master_oit_engine() lets li_global_root_fid
through the filter that drops purely local objects -- so a zeroing
scheme would raise that flag on the root on every scan.  An earlier
attempt to zero it did exactly that and regressed sanity-lfsck.

The gate keys on the iterated object's own FID rather than on the parent
dnode: SA_ZPL_PARENT does not name the OSD root for these objects, it
names the OI ZAP that indexes them, so testing the dnode against
osd->od_root never matches.

osd_dir_lookup() still answers -ENODATA for these same objects, so
dt_lookup(dir, "..") and the iterator disagree where osd-ldiskfs agrees.
That is deliberate here: no current consumer hits the divergence
(mdd_is_parent() short-circuits on mdd_is_root(), and the LFSCK's
dt_lookup_dir(child, dotdot) tolerates -ENODATA), and changing the lookup
would feed the LFSCK a parent FID where it currently gets nothing, which
wants its own patch and its own validation.

The sibling case is left alone here: when osd_get_fid_by_oid() fails for
an ordinary entry further down, lde_fid is also left unwritten but the
entry is marked LUDA_UNKNOWN.  A zero FID there would be worse than a
stale one, because for a name other than ".." the LFSCK treats an insane
FID as LNIT_BAD_DIRENT and removes the name entry.  The companion llite
patch makes the client ignore any entry without LUDA_FID, which covers
that path from the client side.

sanity test_912 covers it: it reads the ".." d_ino of the mount root
through getdents64() ten times over dropped caches and requires a stable,
non-zero value that differs from ".", so a gate that stopped matching the
root and let it fall to the self-FID arm would be caught too.  On an
unpatched ZFS MDT that value is 0 half the time and
5188147320583553024 the rest; with the patch it is 144115188092633101
every time.  The new dirent_ino helper is needed because stat(2) -- and
so ls(1), find(1) and $LFS path2fid -- resolves ".." through the VFS,
which at a mount root answers with the mountpoint's parent instead of
what the OSD put in the entry.

Validated on a ZFS DNE cluster (2 MDT, 2 OST) with the companion llite
patches, tracing both arms of the gate: the fs root and both
update_log_dir objects take the OSD root, .lustre/fid reports itself, and
no zero FID is emitted.  sanityn test_102a fails 5/30 without the patches;
with them full sanity-lfsck passes 73/73, sanity test_154f passes, and
test_102a passes 29/30 -- the one failure being the unrelated "stat data
mismatch" (LU-15842), not an ESTALE.

Fixes: 0780fde4d415 ("LU-3573 osd-zfs: Only advance zap cursor as needed")
Test-Parameters: fstype=zfs mdscount=2 mdtcount=4 testlist=sanityn env=ONLY=102,ONLY_REPEAT=200
Signed-off-by: Oleg Drokin <green@whamcloud.com>
Assisted-by: ClaudeCode:Opus-5 llm_code_and_review_tools
Change-Id: I0e999b877991e3c784d3f38a8d43ada0d99e7c44

--- Review History ---

--- Message (Oleg Drokin, 2026-07-22 02:24:37.000000000) ---
Uploaded patch set 1.

--- Message (wc-checkpatch, 2026-07-22 02:28:25.000000000) ---
Patch Set 1:

Looks good to me.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-07-22 02:30:22.000000000) ---
Patch Set 1:

Code-wise looks good to me.

--- Message (Lustre RISC-V Builder, 2026-07-22 02:52:25.000000000) ---
Patch Set 1:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-07-22 02:56:22.000000000) ---
Patch Set 1: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/127893/ : SUCCESS

--- Message (Andreas Dilger, 2026-07-22 10:33:12.000000000) ---
Patch Set 1: Code-Review+1

(2 comments)

--- Message (Andreas Dilger, 2026-07-22 17:01:37.000000000) ---
Hashtag added: testfix

--- Message (James Simmons, 2026-07-22 18:14:08.000000000) ---
Patch Set 1: Code-Review+1

--- Message (Alex Zhuravlev, 2026-07-23 09:46:49.000000000) ---
Patch Set 1: Code-Review+1

--- Message (Marc Vef, 2026-07-25 16:10:56.000000000) ---
Hashtag added: master-next

--- Message (Marc Vef, 2026-07-25 16:10:57.000000000) ---
Patch Set 1:

[Marc Bot] Note: This change has been cherry-picked to master-next and is considered for landing. Please avoid updates unless required while the master-next hashtag is set; doing so may cause the patch to be removed from master-next and not landed. Note: AI review comments should be addressed/responded to.

--- Message (Marc Vef, 2026-07-25 16:10:57.000000000) ---
Removed cc <GERRIT_ACCOUNT_1055>.

--- Message (Oleg Drokin, 2026-08-02 21:27:49.000000000) ---
Patch Set 1:

(1 comment)

--- Message (Oleg Drokin, 2026-08-03 05:53:49.000000000) ---
Uploaded patch set 2.

Outdated Votes:
* Code-Review+1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE OR changekind:TRIVIAL_REBASE OR changekind:TRIVIAL_REBASE_WITH_MESSAGE_UPDATE OR is:MIN")
* Verified+1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (wc-checkpatch, 2026-08-03 05:57:12.000000000) ---
Patch Set 2:

Looks good to me.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-03 05:59:10.000000000) ---
Patch Set 2:

Code-wise looks good to me.

--- Message (Oleg Drokin, 2026-08-03 06:05:45.000000000) ---
Uploaded patch set 3: Patch Set 2 was rebased.

--- Message (jenkins, 2026-08-03 06:05:52.000000000) ---
Patch Set 2: Verified-1

Build Failed 

https://build.whamcloud.com/job/lustre-reviews/128571/ : ABORTED

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-03 06:11:39.000000000) ---
Patch Set 3:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-08-03 06:12:42.000000000) ---
Patch Set 3:

Looks good to me.

--- Message (Lustre RISC-V Builder, 2026-08-03 06:23:13.000000000) ---
Patch Set 3:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-08-03 06:37:50.000000000) ---
Patch Set 3: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/128575/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-08-03 18:26:54.000000000) ---
Patch Set 3:

(3 comments)

AI code review results below, please doublecheck

Mostly good, minor comments inline. The two arms both trace out safe for the consumers that matter today (mdd_dir_page_build() -> llite, lfsck_master_dir_engine() -> lfsck_namespace_assistant_handler_p1(), lod_update_log_dir_gc()); the comments below are about the gate being wider than the reasoning behind it.

--- Message (Oleg Drokin, 2026-08-04 04:38:00.000000000) ---
Uploaded patch set 4.

Outdated Votes:
* Verified+1, Verified-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (wc-checkpatch, 2026-08-04 04:40:23.000000000) ---
Patch Set 4:

Looks good to me.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-04 04:43:49.000000000) ---
Patch Set 4:

Code-wise looks good to me.

--- Message (Lustre RISC-V Builder, 2026-08-04 04:58:21.000000000) ---
Patch Set 4:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-08-04 05:42:26.000000000) ---
Patch Set 4: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/128629/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-08-04 17:57:16.000000000) ---
Patch Set 4:

(3 comments)

AI code review results below, please doublecheck

Mostly good. The osd-zfs change itself traces out cleanly: both arms produce a sane, local FID, lfsck_find_mdt_idx_by_fid() resolves either one to this target so the dotdot check stays a no-op, and the Fixes: commit is the right one. Comments inline are about the test gating and two commit-message points.

--- Message (Oleg Drokin, 2026-08-05 04:30:02.000000000) ---
Uploaded patch set 5: Patch Set 4 was rebased. Commit message was updated.

Copied Votes:
* Code-Review-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE OR changekind:TRIVIAL_REBASE OR **changekind:TRIVIAL_REBASE_WITH_MESSAGE_UPDATE** OR is:MIN")

Outdated Votes:
* Verified+1, Verified-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-05 04:37:41.000000000) ---
Patch Set 5:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-08-05 04:40:47.000000000) ---
Patch Set 5:

Looks good to me.

--- Message (jenkins, 2026-08-05 05:18:00.000000000) ---
Patch Set 5: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/128699/ : SUCCESS

--- Message (Lustre RISC-V Builder, 2026-08-05 07:31:53.000000000) ---
Patch Set 5:

RISC-V Client Builder: build succesful

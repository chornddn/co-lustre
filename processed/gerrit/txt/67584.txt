Change:     67584
Subject:    LU-20527 osp: don't trust OST LAST_ID past precreate window
Link:       https://review.whamcloud.com/c/fs/lustre-release/+/67584
Change-Id:  I9a646bf7f8d13d92f73668729e68925bbe21a6a5
Project:    fs/lustre-release
Branch:     master
Status:     NEW
Owner:      Oleg Drokin
Created:    2026-07-28 04:47:22.000000000
Updated:    2026-08-14 14:17:18.000000000
Patch Set:  3
Insertions: +78
Deletions:  -1
Comments:   5 (3 unresolved)

--- Labels ---
  Code-Review: (no votes)
  Verified: jenkins: +1; Maloo: -1

--- Reviewers ---
  CC: Lustre RISC-V Builder, Gerrit AI review for Lustre
  REVIEWER: Oleg Drokin, Alex Zhuravlev

--- Commit Message ---
LU-20527 osp: don't trust OST LAST_ID past precreate window

ofd_create_hdl() answers a precreate request whose last OID is below
the OST LAST_ID with the OST's own LAST_ID + 1 ("trust the OST") and
precreates nothing.  osp_precreate_send() stores that reply into
opd_pre_last_created_fid without touching opd_pre_used_fid, so the
OSP then advertises every object in between as precreated, although
the OST created none of them for this request.  Once the jump exceeds
OST_MAX_PRECREATE * 2 the next lod_statfs_and_check() trips

  ASSERTION( sfs->os_fprecreated <= OST_MAX_PRECREATE * 2 ) failed:
  last_created [0x280000401:0xff02:0x0],
  next_fid [0x280000401:0x1:0x0], reserved 0

LU-15009 introduced that reply, but only for a gap below
OST_MAX_PRECREATE; a larger one was still refused with -EINVAL, so
os_fprecreated stayed under the assertion.  LU-17987 dropped that
refusal and an arbitrarily large jump now reaches it.

Treat a reply beyond the range we asked for as a resync: move
opd_pre_used_fid up together with opd_pre_last_created_fid, leaving
an empty precreate window, the same way that
osp_precreate_cleanup_orphans() handles the equivalent delorphan
reply.  LAST_ID + 1 is a "start over here" marker in both: the object
it names has not been created either.

Do this only while the window is already empty, which is the state
the reply is answered in here - it follows a sequence rollover, and
the crash above reports "reserved 0".  It is also the only state in
which moving opd_pre_used_fid is safe: osp_precreate_reserve() grants
a reservation only while opd_pre_reserved is below the number of
precreated objects, so an empty window has nothing reserved from it,
while emptying a window which has would leave opd_pre_reserved above
it and underflow the __u32 os_fprecreated in osp_statfs() - the same
assertion, from the other side.  A non-empty window keeps behaving as
before.

Whatever emptied the window - a rollover, a delorphan, or consumers
outrunning precreation - nothing above opd_pre_used_fid has ever been
handed out, so restarting there loses nothing that is in use.  What
it can give up is objects the OST really did create, when the reply
announcing them was lost rather than never sent.  Those are left to
LFSCK: as soon as the MDT consumes from the restarted window
opd_last_used_fid is above them, and ofd_orphans_destroy() only walks
the OST LAST_ID down to that.  Leaving them still beats handing out
objects the OST never created, which is what the whole jump would be
after a rollover or a delorphan.

conf-sanity test_122a reaches this whenever an OSP rolls over to a
fresh sequence while OBD_FAIL_OFD_SET_OID is still set: the OST
stamps the new sequence's LAST_ID at seq_width - 255 and reports it
back, and MDT0000's OSP always rolls over since it starts on the
IDIF sequence.  Add test_122d, which holds the fail_loc until that
OSP has really been given the inflated LAST_ID rather than racing
its rollover, then creates through MDT0000 and checks the window it
was left with - the os_fprecreated assertion is only reached while
seq_width, which the test framework sets to OSTSEQWIDTH / OSTCOUNT,
is above OST_MAX_PRECREATE * 2.  A new subtest rather than an
extension of test_122a, because holding the fail_loc that long turns
the LBUG from a race into a certainty and test_122a runs against an
MDS of any version.  The window check reads osp.*.prealloc_next_id,
which only reports the id the OSP will really hand out next with the
preceding patch in this series.

Fixes: 53940ff3eee8 ("LU-17987 ofd: always trust OST last_id")
Signed-off-by: Oleg Drokin <green@whamcloud.com>
Assisted-by: ClaudeCode:Opus-5 llm_code_and_review_tools
Change-Id: I9a646bf7f8d13d92f73668729e68925bbe21a6a5

--- Review History ---

--- Message (Oleg Drokin, 2026-07-28 04:47:22.000000000) ---
Uploaded patch set 1.

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-07-28 04:50:41.000000000) ---
Patch Set 1:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-07-28 04:53:20.000000000) ---
Patch Set 1:

Looks good to me.

--- Message (Lustre RISC-V Builder, 2026-07-28 05:01:46.000000000) ---
Patch Set 1:

RISC-V Client Builder: build succesful

--- Message (jenkins, 2026-07-28 05:12:10.000000000) ---
Patch Set 1: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/128200/ : SUCCESS

--- Message (Gerrit AI review for Lustre, 2026-07-28 19:19:32.000000000) ---
Patch Set 1:

(2 comments)

AI code review results below, please doublecheck

Some concerns, see inline comments. The resync itself looks like the right shape for the LU-20527 case, but it empties the precreate window without the reservation drain that the delorphan path it is modelled on performs.

--- Message (Oleg Drokin, 2026-08-13 03:11:59.000000000) ---
Patch Set 1: Code-Review-1

(1 comment)

--- Message (Oleg Drokin, 2026-08-13 13:46:20.000000000) ---
Uploaded patch set 2.

Outdated Votes:
* Code-Review-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE OR changekind:TRIVIAL_REBASE OR changekind:TRIVIAL_REBASE_WITH_MESSAGE_UPDATE OR is:MIN")
* Verified+1, Verified-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-13 13:50:49.000000000) ---
Patch Set 2:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-08-13 13:56:22.000000000) ---
Patch Set 2:

Looks good to me.

--- Message (jenkins, 2026-08-13 14:59:42.000000000) ---
Patch Set 2: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/129253/ : SUCCESS

--- Message (Lustre RISC-V Builder, 2026-08-13 17:04:12.000000000) ---
Patch Set 2:

RISC-V Client Builder: build succesful

--- Message (Oleg Drokin, 2026-08-14 01:41:25.000000000) ---
Uploaded patch set 3.

Outdated Votes:
* Code-Review-1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE OR changekind:TRIVIAL_REBASE OR changekind:TRIVIAL_REBASE_WITH_MESSAGE_UPDATE OR is:MIN")
* Verified+1 (copy condition: "changekind:NO_CHANGE OR changekind:NO_CODE_CHANGE")

--- Message (Misc Code Checks Robot (Gatekeeper helper), 2026-08-14 01:45:38.000000000) ---
Patch Set 3:

Code-wise looks good to me.

--- Message (wc-checkpatch, 2026-08-14 01:48:59.000000000) ---
Patch Set 3:

(1 comment)

1 style warning(s).
For more details please see https://wiki.lustre.org/Lustre_Coding_Style_Guidelines

--- Message (jenkins, 2026-08-14 04:30:34.000000000) ---
Patch Set 3: Verified+1

Build Successful 

https://build.whamcloud.com/job/lustre-reviews/129321/ : SUCCESS

--- Message (Lustre RISC-V Builder, 2026-08-14 06:29:26.000000000) ---
Patch Set 3:

RISC-V Client Builder: build succesful

--- Message (Gerrit AI review for Lustre, 2026-08-14 14:17:18.000000000) ---
Patch Set 3:

(1 comment)

AI code review results below, please doublecheck

Mostly good, one minor comment inline. The restart-while-empty condition holds up: osp_precreate_reserve() only grants a reservation while opd_pre_reserved is below osp_objs_precreated_nolock(), so an empty window really does have nothing reserved, and osp_precreate_get_fid() decrements both together under opd_pre_lock.

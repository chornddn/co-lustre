Key:        LU-20132
Summary:    sanity-lnet test cleanup: NULL deref in __wake_up_common via tcp_child_process on lnet reload
Link:       https://jira.whamcloud.com/browse/LU-20132
Type:       Bug
Priority:   Critical
Status:     In Progress
Resolution: Unresolved
Assignee:   Chris Horn
Reporter:   Maloo
Created:    Tue, 14 Apr 2026 07:42:40 +0000
Updated:    Wed, 8 Jul 2026 16:28:17 +0000
Links:      Duplicate: is duplicated by LU-19489

--- Description ---
This issue was created by maloo for Marc Vef 

This issue relates to the following test suite run: https://testing.whamcloud.com/test_sets/0598b105-002b-4ab2-a6dd-39b194f4ce91

Test session details:

clients: https://build.whamcloud.com/job/lustre-reviews/123835 - 4.18.0-553.111.1.el8_10.x86_64

servers: https://build.whamcloud.com/job/lustre-reviews/123835 - 4.18.0-553.111.1.el8_lustre.x86_64

Kernel NULL pointer dereference on client 0.8s after LNet/ksocklnd module reload between test sessions.

Timeline (dmesg seconds):

13831.132 — LNet: Removed LNI 10.240.23.92@tcp1 (prior session teardown; lnet_acceptor_remove_socket: Interface eth0 not found)
14093.834 — libcfs/ksocklnd/LNet modules reloaded
14094.658 — LNet: Added LNI 10.240.23.92@tcp
14094.659 — Oops

BUG: unable to handle kernel NULL pointer dereference at 0000000000000000
RIP: __wake_up_common+0x4c/0x190
Call Trace: 
  __wake_up_common_lock+0x81
  tcp_child_process+0x181
  tcp_v4_rcv+0xa47
  ip_local_deliver_finish → ip_rcv → napi_gro_receive → virtnet_poll
Modules: ksocklnd ptlrpc obdclass lnet libcfs ... [last unloaded: libcfs]

--- Comment (gerrit, Wed, 24 Jun 2026 03:56:56 +0000) ---
"Chris Horn " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/66829

Subject: LU-20132 lnet: statically initialize acceptor wait queue

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: fb7ac81d665fe105aa2b46be8f1475f44dceb19e

--- Comment (hornc, Sat, 27 Jun 2026 15:26:30 +0000) ---
Forgot to post the RCA here.

This is a latent bug in one of the LU-18812 patches.

When each socklnd NI comes up, lnet_acceptor_add_socket() opens a listening socket and replaces its sk->sk_data_ready with lnet_acceptor_ready.

        /* Setup socket callback properly */
        lnet_acceptor_state.pta_odata = lsock->liss_sock->sk->sk_data_ready;
        lsock->liss_sock->sk->sk_data_ready = lnet_acceptor_ready;

lnet_acceptor_ready() does an unconditional wake_up_interruptible()

        wake_up_interruptible(&lnet_acceptor_state.pta_waitq);

pta_waitq is a field of the file-static lnet_acceptor_state struct, which is zero. The static initializer only sets .pta_shutdown = 1, not pta_waitq. It is only made valid by init_waitqueue_head() inside the acceptor kthread

static struct {
        int                     pta_shutdown;
        struct socket           *pta_sock;
        struct completion       pta_signal;
        struct net              *pta_ns;
        wait_queue_head_t       pta_waitq;
        atomic_t                pta_ready;
#ifdef HAVE_SK_DATA_READY_ONE_ARG
        void                    (*pta_odata)(struct sock *);
#else
        void                    (*pta_odata)(struct sock *, int);
#endif
} lnet_acceptor_state = {
        .pta_shutdown = 1
};

...

static int
lnet_acceptor(void *arg)
{
...
        init_waitqueue_head(&lnet_acceptor_state.pta_waitq);

When LNet is started, all listeners are hooked in lnet_startup_lndnets() before lnet_acceptor_start() spawns the kthread. So there is a window where the listening sockets are live and hooked but pta_waitq.head.next == NULL.

An inbound TCP connection completing its 3-way handshake in that window goes tcp_v4_rcv -> tcp_child_process -> parent->sk_data_ready (=lnet_acceptor_ready) -> wake_up_interruptible(&pta_waitq) and dereferences the NULL list head -> __wake_up_common+0x4c NULL deref in softirq -> panic.

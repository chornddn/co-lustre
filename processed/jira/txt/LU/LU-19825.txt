Key:        LU-19825
Summary:    use io_schedule() instead of schedule when waiting for IO
Link:       https://jira.whamcloud.com/browse/LU-19825
Type:       Improvement
Priority:   Medium
Status:     Resolved
Resolution: Fixed
Assignee:   Nikos Papakonstantinou
Reporter:   Andreas Dilger
Created:    Fri, 23 Jan 2026 22:51:14 +0000
Updated:    Fri, 10 Jul 2026 19:54:24 +0000
Resolved:   Fri, 10 Jul 2026 19:54:24 +0000
Labels:     easy, upstream
Links:      Related: is related to  LU-5566; Related: is related to  LU-12511

--- Description ---
In upstream kernel code, it is common to use io_schedule() (and related family of functions) to sleep when processes are waiting for IO completion, rather than schedule() when they are just idle or waiting to on other processes, memory, CPU, etc. This allows the kernel to account sleeping processes waiting for IO rather than waiting for the task to be runnable. That helps with vmstat, top and other commands that distinguish how processes are sleeping.

There are a number of places that Lustre waits for completion that should be updated:

	schedule() -> io_schedule() - with review to ensure it is actually waiting on IO

	schedule_timeout() -> io_schedule_timeout() - with review

	wait_event() -> io_wait_event() - with review

	l_wait_abortable*() - should be unrolled to use __wait_event() and calls io_schedule() at the bottom level, until such a time that [__]io_wait_event_interruptible*() is available in the kernel, possibly renamed to io_wait_event_abortable() to better match other functions

--- Comment (adilger, Sat, 24 Jan 2026 22:58:49 +0000) ---
A submission to linux-fsdevel today:

Add io_wait_event_killable(), a variant of wait_event_killable() that
uses io_schedule() instead of schedule(). This is to be used in
situation where waiting time is to be accounted as IO wait time.

Signed-off-by: Remi Pommarel 
Acked-by: Peter Zijlstra (Intel) 
---
include/linux/wait.h | 15 +++++++++++++++
1 file changed, 15 insertions(+)

diff --git a/include/linux/wait.h b/include/linux/wait.h
index f648044466d5..dce055e6add3 100644
--- a/include/linux/wait.h
+++ b/include/linux/wait.h
@@ -937,6 +937,21 @@ extern int do_wait_intr_irq(wait_queue_head_t *, wait_queue_entry_t *);
	__ret;									\
})

+#define __io_wait_event_killable(wq, condition)					\
+	___wait_event(wq, condition, TASK_KILLABLE, 0, 0, io_schedule())
+
+/*
+ * wait_event_killable() - link wait_event_killable but with io_schedule()
+ */
+#define io_wait_event_killable(wq_head, condition)				\
+({										\
+	int __ret = 0;								\
+	might_sleep();								\
+	if (!(condition))							\
+		__ret = __io_wait_event_killable(wq_head, condition);		\
+	__ret;									\
+})
+
#define __wait_event_state(wq, condition, state)				\
	___wait_event(wq, condition, state, 0, 0, schedule())

--- Comment (JIRAUSER20216, Fri, 13 Mar 2026 15:56:35 +0000) ---
This is a summary of schedule, schedule_timeout and wait_event call in the source. For some of them it is not clear to me if they are I/O related or not.

schedule() calls:

	lustre/mdd/mdd_device.c - Not waiting for I/O

	lustre/llite/statahead.c - Not waiting for I/O

	lustre/target/update_trans.c - Seems not waiting for I/O (Not sure)

	lustre/kunit/kinode.c - Not waiting for I/O

	lustre/ofd/ofd_io.c - Not waiting for I/O

	lustre/mdt/mdt_restripe.c - Seems not waiting for I/O (Not sure)

	lustre/ptlrpc/service.c - Not waiting for I/O

	lustre/quota/qmt_lock.c - Seems not waiting for I/O (Not sure)

	lustre/quota/qsd_reint.c - Seems not waiting for I/O (Not sure)

	lustre/obdclass/range_lock.c - Probably waiting for I/O (Not sure)

	lustre/obdclass/page_pools.c - Not waiting for I/O

	lustre/osd-ldiskfs/osd_iam.c - Seems not waiting for I/O (Not sure)

schedule_timeout():

	lustre/ldlm/ldlm_lockd.c - Not waiting for I/O

	lustre/osc/osc_lock.c - Seems not waiting for I/O (Not sure)

	lustre/llite/statahead.c - Not waiting for I/O

	lustre/target/update_trans.c - Not waiting for I/O

	lustre/ptlrpc/pinger.c - Not waiting for I/O

	lustre/quota/qsd_writeback.c - Probably waiting for I/O (Not sure)

	lustre/obdclass/upcall_cache.c - Not waiting for I/O

wait_event():

	lustre/osp/osp_dev.c - Waiting for I/O

	lustre/osd-zfs/osd_handler.c - Waiting for I/O

	lustre/lov/lov_object.c - Probably waiting for I/O (Not sure)

	lustre/ldlm/ldlm_lockd.c - Not waiting for I/O

	lustre/llite/lcommon_cl.c - Not waiting for I/O

	lustre/mdt/mdt_coordinator.c- Not waiting for I/O

	lustre/osd-ldiskfs/osd_io.c - Waiting for I/O

	lustre/osd-ldiskfs/osd_dynlocks.c - Waiting for I/O

	lustre/osd-ldiskfs/osd_handler.c - Waiting for I/O

Can you please check if my findings are correct or they are need some adjustments?

PS: There are also some wait_event_* variants which probably related to I/O. Should we have to replace them too?

--- Comment (adilger, Fri, 13 Mar 2026 16:34:38 +0000) ---
To be clear, in many cases, "waiting for IO" in a Lustre client is the same as "waiting for RPC completion" because the IO is happening on the server.

--- Comment (JIRAUSER20216, Mon, 30 Mar 2026 14:04:37 +0000) ---
Are you referring to some of the cases I wrote in the previous comment or to other calls which maybe I missed them? Can you elaborate on that please?

--- Comment (adilger, Mon, 30 Mar 2026 16:39:16 +0000) ---
Sorry for the confusion.  I was just commenting that "wait for I/O" is a broad term in a network filesystem. I looked through the sites you mentioned and your analysis appears generally correct.  There are a few places were wait/schedule is used for debugging/fault injection, so they are a bit of a grey area and I don't have a strong opinion on whether they should be converted or not.  Maybe best to start with the clear cases, and then there can be a separate patch for the less obvious ones (in case there is a discussion needed that may delay the patch landing).

--- Comment (gerrit, Fri, 24 Apr 2026 13:41:33 +0000) ---
"Nikos Papakonstantinou " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/65509

Subject: LU-19825 lustre: Use io_wait_event() instead of wait_event()

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: b15ee863470222fb3ee4526f0b3b8d4607d2038c

--- Comment (JIRAUSER20216, Fri, 24 Apr 2026 13:44:42 +0000) ---
I just pushed a patch which replaces the clear (not debugging/fault injection) case of wait_event() with io_wait_event(). I 'll continue checking the rest case to see which of then can also be replaced.

--- Comment (gerrit, Fri, 10 Jul 2026 17:49:44 +0000) ---
"Oleg Drokin " merged in patch https://review.whamcloud.com/c/fs/lustre-release/+/65509/

Subject: LU-19825 lustre: Use io_wait_event() instead of wait_event()

Project: fs/lustre-release

Branch: master

Current Patch Set: 

Commit: 1367874b59387011c688a3e155ae95cedafffbcb

--- Comment (pjo, Fri, 10 Jul 2026 19:54:24 +0000) ---
Merged for 2.18

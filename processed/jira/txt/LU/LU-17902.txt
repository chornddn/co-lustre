Key:        LU-17902
Summary:    add NRS TBF policy for nodemap
Link:       https://jira.whamcloud.com/browse/LU-17902
Type:       Improvement
Priority:   Major
Status:     Resolved
Resolution: Fixed
Assignee:   Qian Yingjin
Reporter:   Andreas Dilger
Created:    Tue, 4 Jun 2024 04:23:51 +0000
Updated:    Sat, 11 Jul 2026 13:11:26 +0000
Resolved:   Sat, 20 Jun 2026 12:49:30 +0000
Labels:     TBF, medium, nodemap
Links:      Duplicate: is duplicated by LU-18089; Related: is related to  LU-18179; Related: is related to  LU-14501; Related: is related to  LU-17166; Related: is related to LU-18269; Related: is related to LU-19077; Related: is related to LU-20090
Attachments: Screenshot 2026-03-27 145739-1.png, Screenshot 2026-03-27 145739.png, Screenshot 2026-03-27 151853.png, Screenshot 2026-03-27 153630.png, Screenshot 2026-03-27 153647.png

--- Description ---
For isolation of workloads across multiple sub-tenants of a filesystem, it would be useful to allow registering an NRS TBF rule for a nodemap.  This can be proxied to some extent by setting a TBF rule for a project ID, but this doesn't work if there are multiple project IDs used by a single nodemap.

--- Comment (adilger, Tue, 4 Jun 2024 04:34:58 +0000) ---
sebastien, eaujames, qian_wc, what do you think of using TBF for a nodemap?  That would allow tagging all the incoming RPCs with the nodemap that the export is using, and directly managing the RPCs of a sub-tenant  instead of using projid as a proxy for this.  I think the TBF rule would specify the nodemap name, but could internally match based on a pointer to the nodemap directly, if that was more efficient.  It would also simplify balancing performance across multiple tenants by using proportional rule (e.g. 5000-2500-2500 based on nodemaps).

--- Comment (sebastien, Tue, 4 Jun 2024 08:21:46 +0000) ---
It looks fine to me, as NRS arbitration is carried out on server side, just as nodemap's. I can see that NRS TBF currently supports providing uids/gids, jobids, opcodes or nids. So I think it would make sense to have the nodemap  NRS association defined on NRS side, by introducing a new parameter: a nodemap name. As Andreas mentioned, the export has a reference to the nodemap being used, so that would make it easy to find the matching entries I guess.

--- Comment (eaujames, Wed, 5 Jun 2024 18:09:54 +0000) ---
I agree with Sebastien, we do not need to tag any requests, we can just "ask" if the RPC NID is in range of a nodemap via the existing functions. This makes the implementation of a new "nodemap" policy relatively easy.

The default rate to apply to a "nodemap" class can be tricky. Unlike the "nid" policy, the rate will apply to a range of nodes instead of one node. So, maybe the default rate for a class can be determined like this:

default_nodmap_rate = nbr_of_node_in_nodemap * tbf_default_rate

If a nodemap is bigger than another one, it should have more BW.

Note, that we could achieve something like this manually with "tbf nid": we can declare tbf-rules with the same NID ranges as for the nodemaps and compute a rate to apply.

--- Comment (adilger, Wed, 5 Jun 2024 21:37:22 +0000) ---
I agree with Sebastien, we do not need to tag any requests, we can just "ask" if the RPC NID is in range of a nodemap via the existing functions. 

I don't read Sebastien's comment as implying that?  Since the RPC handling has already looked up the nodemap I don't think it should be doing another nodemap NID lookup when NRS is processing the request again later?  I think the NID->nodemap lookup is the most expensive part of having a nodemap, so it should only be done once if possible.

The default rate to apply to a "nodemap" class can be tricky. Unlike the "nid" policy, the rate will apply to a range of nodes instead of one node.

That is fine, and that rate is up to the administrator to define?  I don't think we should be messing with the rates internally:

	this is confusing for the admin, if they set a rate of "5000" and then it is silently converted to "500000" if there are 1000 nodes in the nodemap

	the number of nodes in the nodemap will change over time, which would mean the TBF rate would change as clients are added and removed

I don't think this can or should be emulated with TBF NID rules, both because the NIDs in a nodemap may change over time (especially with Sebastien's dynamic nodemap code) as well as adding complexity to manage/specify complex TBF rules for many NIDs in the nodemap.  Specifying the rule with the nodemap name is very clear and directly ties the rates to all nodes that the "tenant" is using in the nodemap, even if the nodes change over time.

--- Comment (qian_wc, Thu, 6 Jun 2024 01:51:43 +0000) ---
The default rate to apply to a "nodemap" class can be tricky. Unlike the "nid" policy, the rate will apply to a range of nodes instead of one node. So, maybe the default rate for a class can be determined like this:

default_nodmap_rate = nbr_of_node_in_nodemap * tbf_default_rate

If a nodemap is bigger than another one, it should have more BW.

Can we directly use NID TBF? and each NID in the "nodemap" with its own rate limit (same value for all NIDs in a "nodemap")?

Otherwise, all NID in a nodemap will share the IOPS bandwidth and some will exceed the limit for each NID and some will not...

--- Comment (sebastien, Thu, 6 Jun 2024 06:25:40 +0000) ---
I don't read Sebastien's comment as implying that? Since the RPC handling has already looked up the nodemap I don't think it should be doing another nodemap NID lookup when NRS is processing the request again later? I think the NID->nodemap lookup is the most expensive part of having a nodemap, so it should only be done once if possible.

Sure, after the nodemap lookup has been done, the export holds a reference to the nodemap. So NRS TBF would just have to use this info from the export, instead of doing a whole nodemap lookup again.

--- Comment (eaujames, Thu, 6 Jun 2024 21:04:32 +0000) ---
Sure, after the nodemap lookup has been done, the export holds a reference to the nodemap. So NRS TBF would just have to use this info from the export, instead of doing a whole nodemap lookup again.

Ok, my bad. So there is no overhead to check if an RPC is in a nodemap.

That is fine, and that rate is up to the administrator to define? I don't think we should be messing with the rates internally:

Ok, I see the issue here. But, at least, can we increase the default rate for the nodemap policy (if the default is not changed via module parameter)?

10000 RPC/s default rate is adjusted for 1 node.

--- Comment (qian_wc, Thu, 28 Nov 2024 16:25:52 +0000) ---
I am currently working on TBF for project ID.

 If " This can be proxied to some extent by setting a TBF rule for a project ID" is the direction, then we can borrow the implementation of PCC code for aggregate project ID range and all requests in these project ID range have a TBF shared rate.

We have a patch for PCC implementing the similar functionality:

) compare operator support for PCC rule" class="issue-link" data-issue-key="LU-13881">LU-13881 pcc: comparator support for PCC rules

https://review.whamcloud.com/c/fs/lustre-release/+/39585

TBF rule can borrow the code from PCC to define a rule with a range of PROJID values with '' comparator.

 

We also have a patch for aggregate shared rate limiting for TBF:

https://review.whamcloud.com/#/c/fs/lustre-release/+/56351/

 

i.e. 

lctl set_param ost.OSS.ost_io.nrs_tbf_rule="start sharerate projid>{100}&projid 

If we need TBF supporting node map directly, I need some time to learn the background knowledge about node map.

--- Comment (qian_wc, Tue, 3 Dec 2024 03:02:54 +0000) ---
Please note that a Lustre client should not have rate control with two different TBF class:

See LU-7982 for details:

When using JOBD-based TBF rules, if multiple jobs run on the same client, the RPC rates of those jobs will be affected by each other. More precisely, the job that has high RPC rate limitation might get slow RPC rate actually. The reason of that is, the job that has slower RPC rate limitations might exaust the max-in-flight-RPC-number limitation, or the max-cache-pages limitation.

--- Comment (qian_wc, Tue, 3 Dec 2024 04:46:13 +0000) ---
improved "fair share" balancing between buckets

What's the "faire share" meaning?

 

I have a question for node map TBF support:

Should we use @lu_nodemap.nm_id or @lu_nodemap.nm_name as the key for TBF scheduling class?

I think both can be used as key to identify a nodemap.

--- Comment (sebastien, Tue, 3 Dec 2024 09:16:00 +0000) ---
I have a question for node map TBF support:

Should we use @lu_nodemap.nm_id or @lu_nodemap.nm_name as the key for TBF scheduling class?

I think both can be used as key to identify a nodemap. 

Good question. nm_id is unique and could be used as the key. However, if a nodemap is removed and then recreated with the same name, its id will be different, but maybe admins would expect it to be treated as before? In this case, it would make sense to use nm_name as the key for TBF: even if the nodemap is recreated, the same TBF rules apply, and do not need to be fixed.

--- Comment (gerrit, Tue, 3 Dec 2024 09:50:10 +0000) ---
"Qian Yingjin " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/57254

Subject: LU-17902 nrs: add NRS TBF policy for nodemap

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: bae391a8d8a698c7b4fc7d193785b5ef55cb7af9

--- Comment (qian_wc, Thu, 3 Apr 2025 02:08:11 +0000) ---
I will rebase it later.

For your question, 

To make NRS TBF so smart to detect the node map changing (adding and removing), maybe we should both name and id of node map into the key of a TBF class. Each matching will compare both name and id of the node map? In the current version and previous version, we only compare one of them (either name, or id), not both.

And the adding/removing node map will change the id, I think. Thus NRS TBF can detect the change during the matching.

However, this means more memory used for each TBF class.

--- Comment (gerrit, Wed, 21 Jan 2026 05:13:30 +0000) ---
"Andreas Dilger " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/63491

Subject: LU-17902 nodemap: add nodemap_lookup_unlocked()

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: 72f84e2b55e43dbb4778c8e8b6994ffda300747f

--- Comment (gerrit, Fri, 13 Feb 2026 01:21:49 +0000) ---
"Oleg Drokin " merged in patch https://review.whamcloud.com/c/fs/lustre-release/+/63491/

Subject: LU-17902 nodemap: add nodemap_lookup_unlocked()

Project: fs/lustre-release

Branch: master

Current Patch Set: 

Commit: b00c34cf5adaee6beb58c5d21d915abcc4aba33d

--- Comment (mrasobarnett, Fri, 27 Mar 2026 14:57:23 +0000) ---
All, I wanted to check my understanding of the patch in this ticket, https://review.whamcloud.com/c/fs/lustre-release/+/57254

I tested this patch today and in my limited testing, the way this feature works is similar to other rules such as a NID range, in that every client that is a member of the nodemap, will get the specified rate, rather than all clients, collectively, sharing the rate set.

For example, in my test environment I have 3 clients, and 2 servers. I have configured the following TBF rules:

[root@server1 ~]# lctl get_param ost.OSS.ost_io.nrs_tbf_rule
ost.OSS.ost_io.nrs_tbf_rule=
regular_requests:
CPT 0:
T1_rule nodemap={tenant1} 10, ref 1
T2_rule nodemap={tenant2} 20, ref 1
T3_rule nodemap={tenant3} 30, ref 0
default * 10000, ref 0
high_priority_requests:
CPT 0:
T1_rule nodemap={tenant1} 10, ref 0
T2_rule nodemap={tenant2} 20, ref 0
T3_rule nodemap={tenant3} 30, ref 0
default * 10000, ref 0

My nodemap configuration is setup such that 2 of the clients are in tenant1, and 1 client is in tenant2:

[root@server1 ~]# lctl get_param nodemap.tenant*.ranges
nodemap.tenant1.ranges=
[
 { id: 27, start_nid: 10.100.103.123@tcp103, end_nid: 10.100.103.123@tcp103 },
 { id: 26, start_nid: 10.100.103.23@tcp103, end_nid: 10.100.103.23@tcp103 },
 { id: 18, start_nid: 10.100.101.121@tcp101, end_nid: 10.100.101.121@tcp101 },
 { id: 17, start_nid: 10.100.101.21@tcp101, end_nid: 10.100.101.21@tcp101 }
]
nodemap.tenant2.ranges=
[
 { id: 20, start_nid: 10.100.102.122@tcp102, end_nid: 10.100.102.122@tcp102 },
 { id: 19, start_nid: 10.100.102.22@tcp102, end_nid: 10.100.102.22@tcp102 }
]
nodemap.tenant3.ranges=[]

(Note: each client has two NIDs, client1 has IPs on 10.10.101..., client2 on 10.100.102...., client3 on 10.100.103....)

If I run a simple single-thread fio job, with 1M RPC size on all three clients, I see client1 gets 10 IOPS, client3 gets 10 IOPS and client2 gets 20 IOPS:

Is this the expected behaviour?

I was hoping to use this policy as an aggregate limit on all members of the nodemap. Is the intention that to achieve an aggregate limit on this policy, or any policy, to use the 'shared' property in https://jira.whamcloud.com/browse/LU-18183?

--- Comment (mrasobarnett, Fri, 27 Mar 2026 15:38:36 +0000) ---
Sorry - ignore the above comment, this was a testing problem. In my test Client1 and Client3 were writing to files on different OSS servers, and thus were both getting 10 RPC/s from each server.

Re-configuring the test so that client1 & client3 (both members of nodemap tenant1) both write to files striped onto OST0000 on a single server, gives the expected aggregate limit of 10 RPC/s for both clients in the nodemap.

 

So this patch works as I originally expected it to. Thanks!

--- Comment (qian_wc, Fri, 27 Mar 2026 15:53:51 +0000) ---
What's the nrs TBF policy you are using? just nodemap or combined with other kinds such as NID, gid, uid, jobid?

Or what's the server configuration? do you make the file stripping over more than 1 OSTs on two OSSes? What's the CPT (it seems 1 in your case)?

In theory, all clients belonged to a nodemap should classify into a same NRS TBF class and all I/O requests will be enqueued and serviced in FIFO order with the shared rate limiting.

--- Comment (adilger, Sat, 28 Mar 2026 07:39:51 +0000) ---
mrasobarnett, good to hear it worked out in the end. But it exposes the other significant issue with TBF that the TBF limits are per-CPT and per-target, so it is difficult for an administrator to set them, and difficult for users to achieve their maximum limit without a perfectly balanced workload across targets. 

It would be useful to have a PtlRPC-level mechanism to share credits between all of the buckets in a filesystem. That wouldn't have to be "real time", but a few times per second shouldn't be too much overhead. That can be some kind of decaying average exchanged with a central coordinator, so that there aren't huge swings in global rate limit split across peers.

--- Comment (mrasobarnett, Thu, 2 Apr 2026 11:09:29 +0000) ---
@Yingjin - I was only using nodemap policy at the moment, so not combined with anything. The server configuration is very basic, this is just a small VM environment for learning (two servers, three clients). Each server has a single CPT. The files I was using were not striped, so each client was writing to a single OST - the confusion I had was that I was unexpectedly writing to two OSTs, one on each server, so I was seeing the aggregate rate-limit of both servers.

@Andreas - yes, per-CPT and per-Server (rather than per-target) is my understanding. As you say, if we could move towards a model where there is a single TBF rate set by the administrator centrally on the object (nodemap, user, projID etc...), that is then distributed by some mechanism across the servers, but with the ability to automatically shift unused tokens from idle servers towards busier servers so that a client could theoretically saturate their TBF limit through a single server, this would both make the TBF mechanism easier to administer, and also more flexible. I believe GPFS has this sort of dynamic redistribution in it's implementation: https://www.ibm.com/docs/en/storage-scale/6.0.0?topic=reference-mmqos-command#mmqos__dqr_description__title__1

Also - removing the per-CPT limitation within a single server would also be a good initial improvement, so that you set a rate limit for a server, which could be automatically distributed amongst CPTs, however again, with some mechanism whereby unused tokens in an inactive CPT could be reallocated towards another CPT that is reaching it's limit - this way a single client again could theoretically saturate it's per-server TBF limit through a single CPT.

--- Comment (gerrit, Sat, 20 Jun 2026 06:23:38 +0000) ---
"Oleg Drokin " merged in patch https://review.whamcloud.com/c/fs/lustre-release/+/57254/

Subject: LU-17902 nrs: add NRS TBF policy for nodemap

Project: fs/lustre-release

Branch: master

Current Patch Set: 

Commit: 9f81b0730bda29d7c4689fef42a6db44ffa0281c

--- Comment (pjo, Sat, 20 Jun 2026 12:49:30 +0000) ---
Merged for 2.18

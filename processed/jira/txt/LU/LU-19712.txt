Key:        LU-19712
Summary:    Add ability for admins to pin files into Pool
Link:       https://jira.whamcloud.com/browse/LU-19712
Type:       New Feature
Priority:   Medium
Status:     In Progress
Resolution: Unresolved
Assignee:   Nikos Papakonstantinou
Reporter:   Sebastien Buisson
Created:    Tue, 16 Dec 2025 17:37:52 +0000
Updated:    Wed, 8 Jul 2026 16:27:21 +0000
Labels:     llapi, pool
Links:      Related: is related to  LU-20239; Related: is related to  LU-10499; Related: is related to  LU-13171; Related: is related to  LU-17049

--- Description ---
Admins or users would want to pin files to a given pool, as a way of staging files before use, or to prevent any mirroring or purging to/from a given pool. One way to accomplish this would be to designate an xattr that external tools would check for and honor.

A pin xattr, lustre.pin, has already been introduced for PCC purpose. It currently supports PCC info, and is only used by PCC for now, but the spec already plans to include other information such as pool names in this xattr. The content of this xattr is in JSON format, and is like:

lustre.pin=[{"hsm":2},{"hsm":3},{"pool":"ssd"}]

If a pool is specified (multiple are possible), we would consider the file is pinned to this pool, and a mirror stored on this pool cannot be deleted. We could also introduce the "only_pool" property, so that the file is pinned exclusively to this pool. This means no mirror to a different pool would be created in that case.

That would give us the following possible values as examples:

lustre.pin=[{"pool":"ssd"},{"pool":"hdd"}]

or

lustre.pin=[{"only_pool":"ssd"}]

"pool" and "only_pool" would be mutually exclusive given their meaning.

Using this already existing xattr lustre.pin provides the ability to control which GIDs can pin/unpin files, as this feature was already added for PCC by patch "LU-10499 pcc: tunable GID to pin/unpin files". This patch adds a tunable "mdt.*.enable_pin_gid" that controls the GID to pin/unpin files. Default is gid=0, so it is limited to root, gid=-1 means any user can pin/unpin, and gid= is used to allow a specific GID (e.g. wheel).

To support the new pool pin/unpin capability on Lustre, we would need to extend the llapi with dedicated functions, e.g:

int llapi_pool_pin_fd(int fd, char* pool);
int llapi_pool_pin_file(const char *path, char* pool);
int llapi_pool_pin_fid(const char *lustre_dir, const struct lu_fid *fid, char* pool);
int llapi_pool_unpin_file(const char *path, char* pool);
bool llapi_pool_is_pinned_fd(int fd, char* pool);
bool llapi_pool_is_pinned_file(const char *path, char* pool);
bool llapi_pool_is_pinned_fid(const char *lustre_dir, const struct lu_fid *fid, char* pool);

These could be inspired from their PCC equivalents, see all the llapi_pcc_*pin* functions.

We would also need dedicated lfs commands, such as lfs pool pin/unpin/get. pin/unpin would take a path or FID and optionally a pool name. If no pool is specified, we would internally pin to the pool that is configured in the file layout as the primary mirror.

Apart from the userspace side of things, it is important for the lustre.pin xattr to be automatically inherited for files and subdirectories when set on the parent directory. That would allow setting it for instance once at the project directory or group directory level, and then automatically benefit from it for all new files and directories created in there. We do inherit layout already, which is basically an xattr we process and then write, so we would have to implement a similar behavior for the lustre.pin xattr.

--- Comment (adilger, Wed, 18 Mar 2026 17:53:16 +0000) ---
npapakonstantinou, please include man3 pages for the new llapi_pool*_pin*() functions.  I've already written a man4 page for mdt.*.enable_pin_gid for LU-17049 that I can submit shortly.

sebastien, it doesn't appear that there is a Nodemap RBAC for enable_pin_gid or any way to virtualize these GID values per tenant.  Some enable/disable parameters (e.g. enable_remote_dir) fall under existing dne_ops RBAC, but no way to specify a different GID per tenant. This is generic issue, unrelated to just enable_pin_gid or enable_remote_dir_gid.  I think the only solution today would be to set *_gid=-1 on the MDTs so that this functionality is available to all tenants.

I was wondering whether it would be possible to have a single enable_pin_gid=99995 and then map this ID for every nodemap outside of the Nodemap offset range?  This might allow tenants to potentially "escape" their container using this GID unless it had a quota limit of "1" or similar to prevent space usage outside of their Nodemap LQA/PROJID limit (AFAIK, a quota limit of "0" means "unlimited" or "use default", so is not suitable to restrict usage).

It doesn't appear that pinning files itself needs its own RBAC to enable/disable, since it is only managing files within the capacity that a tenant can use, so it doesn't appear to present any security/administrative concern for MTFL.

--- Comment (JIRAUSER20216, Thu, 19 Mar 2026 14:50:11 +0000) ---
I am planning to split this into two parts. The first part will include llapi_pool_*pin_*() functions and all the needed adjustments along with the man pages and a simple test for them and the second part will include the implementation of lfs part.

--- Comment (sebastien, Fri, 20 Mar 2026 08:53:56 +0000) ---
Hi adilger,

mdt.*.enable_pin_gid is a server side tuning, so I am not sure how a nodemap rbac role could help, as clients do not have access to this tuning?

I agree we could want to have nodemap specific enable_pin_gid, i.e. per tenant. We could have a dedicated property for that, but I think you are proposing a workaround with enable_pin_gid=99995, and I am not entirely sure I get the idea and how that would work.

--- Comment (adilger, Sat, 21 Mar 2026 08:17:11 +0000) ---
My idea about using a fixed GID for enable_pin_gid=GID (eg. 99995, and similar GIDs for other enable_*gid parameters) was to have an explicit mapping for each nodemap where pin is enabled, that maps the tenant-offset _GID within each nodemap to the filesystem-side GID that allows pinning on the MDS, rather than having it offset to the normal tenant range. This would allow it to perform the pin action, but has the drawback of being potentially complex to configure, and I'm not sure if it is possible to map IDs outside the offset range or not. 

Alternately, each nodemap would get a set of enable_pin_gid=GID, enable_remote_dir_gid=GID, enable_chprojid_gid=GID settings that can be checked in the new mdt_enable_gid_deny() from my patch https://review.whamcloud.com/52063 ("LU-17049 mdt: 'lfs mkdir' check supplementary groups").  That is more straight forward to understand and configure, but needs additional code for each enable_*_gid parameter.

I've wondered for a while if it makes sense to move all MDT and OST parameters off the main device and into the default nodemap, so that they are consistent for all users?  The stats code could be modified to still display the parameters under mdt.*.PARAM, but they would really be managed under the nodemap?

--- Comment (gerrit, Sun, 10 May 2026 22:48:11 +0000) ---
"Oleg Drokin " merged in patch https://review.whamcloud.com/c/fs/lustre-release/+/64941/

Subject: LU-19712 mdd: Inherit pin xattr from parent

Project: fs/lustre-release

Branch: master

Current Patch Set: 

Commit: bbe23406f6041490d00e2bda7d7ad998186a0d09

--- Comment (gerrit, Sun, 10 May 2026 22:48:33 +0000) ---
"Oleg Drokin " merged in patch https://review.whamcloud.com/c/fs/lustre-release/+/64751/

Subject: LU-19712 llapi: Introduce pool pinning API

Project: fs/lustre-release

Branch: master

Current Patch Set: 

Commit: 86b0c90666bf069e8db125bb2f06cb4d1ffe8b17

--- Comment (bzzz, Mon, 11 May 2026 11:30:41 +0000) ---
patch "LU-19712 mdd: Inherit pin xattr from parent" breaks Lustre checker for memleaks, please check the details in LU-20239

I guess you can introduce one lu_buf to track allocation/release and another one to use as a "argument" for other functions holding actual EA size.

--- Comment (JIRAUSER20216, Mon, 11 May 2026 13:08:47 +0000) ---
bzzz That was the first design, but I changed it to avoid calling mdo_xattr_get() twice. We can do what you wrote or set the proper allocation size in lb_len (lb_len = buf_size), and thus we will free the correct length of bytes.

Actually, no need to do this at all, since {{lu_buf_alloc() }}sets the proper length, we can just remove the line 

pin_buf->lb_len = rc;

--- Comment (adilger, Mon, 11 May 2026 19:52:26 +0000) ---
we can just remove the line 

pin_buf->lb_len = rc;

It looks like that would cause setxattr to write out the xattr with the full buffer size each time, rather than the size of the actually-used pin xattr?  That would also be bad...

Rather than calling mdo_xattr_get() multiple times, it looks like the most efficient solution is to allocate a new buffer with the exact xattr size, maybe with a new lu_buf_check_and_shrink() helper function.

--- Comment (JIRAUSER20216, Tue, 12 May 2026 10:23:43 +0000) ---
I have already pushed the proposed fix in https://review.whamcloud.com/c/fs/lustre-release/+/65818

Feel free to make a pass.

--- Comment (JIRAUSER20216, Wed, 20 May 2026 06:46:51 +0000) ---
Since the last patch is ready for review, we probably need to add some special handling to the lfs mirror extend and lfs migrate commands (and maybe to more commands). For example, if a file is pinned in a pool we probably should prevent migration or a new mirror. What do you think about this?

--- Comment (sebastien, Wed, 20 May 2026 13:31:50 +0000) ---
Yes we should modify the commands that handle mirrors so that they can interpret the pool pin information. I can think of:

	lfs mirror extend

	lfs mirror split

	lfs mirror create

	lfs mirror delete

	lfs migrate

So these commands would return an error and do nothing if the pool pin info contradicts the requested action.

But for scripts that do massive mirror operations, this behavior might not be optimal, as it implies the Lustre command internally fetches an xattr for each file, and then takes an educated decision. One way to avoid that could be to introduce a new semantic for the pool pin attribute, such as [\{defaultpool: \\}]. When defaultpool (or whatever better name) is specified on a directory, all files in this directory (let's consider for now we do not go beyond the first level in the tree hierarchy) would implicitly apply the pool pinning of the parent. That would allow the aforementioned scripts to read only the pin xattr of the parent with lfs pool pin_info, and decide not to call mirror primitives for the files in this directory if it has a default pool that contradicts the desired mirror action.

In any case, defaultpool would be a second step optimization, and at first we need to update the lfs commands listed above to interpret the already defined pool pin xattr.

--- Comment (adilger, Thu, 21 May 2026 02:08:47 +0000) ---
Niko, before you get into the implementation, please describe the semantics you intend for each of the commands.  From my POV, in most cases when a user requests to do something, that is what they want to do, unless it is forbidden by the administrator, so I'm reluctant to put roadblocks in their way. It would just mean more inconvenience when trying to take some action (eg. add a new mirror to a file first requires removing the pool pin, adding the mirror, then re-pinning the file).

The original proposal for "pool pin" in LU-13171 was to store this pinning attribute in the file layout in a specific component/mirror, and it would be that mirror that is pinned,  it wasn't necessarily intended to prevent the addition or removal of other mirrors. Having to modify the file layout during pinning was considered too complex, so storing the pin attribute in a separate xattr was implemented, where the file is pinned to a named pool (or in the case of PCC-RO a specific HSM archive ID). 

If we want to change the semantics of the pinning so that it isn't possible to create another mirror on the file, then that should be stored explicitly in the pin xattr in some manner, rather than the intent just being inferred. Essentially, that would be a "pin the non-existence of a mirror on a specific pool", or "pin the non-existence of a file on any other pool", or possibly even "pin the non-existence of any other mirror on this file, even if it is on the same pool", though the latter would prevent any other mirror creation and may be quite undesirable. 

Since xattr space inside the inode is at a premium, this should be expressed in a compact manner. I could imagine "{{ pool: hdd,!nvme }}" or "{{ pool: hdd,-nvme }}" (or whatever is best suited to YAML), preventing creation on the nvme pool. Maybe "{{ pool: +hdd,-any }}" (or similar) would restrict new mirrors to only the hdd pool.

One concern about storing state on the parent directory that affects the files themselves is that this has the chance to become inconsistent, either due to file rename from one parent directory into another where the parents have different policies, or if the parent xattr is changed after the files are created. I'm not against it, just that care is needed to have sane semantics, or at least document what the limitations are (though typically users don't read documentation so the semantics should preferably be sane from the beginning).

--- Comment (JIRAUSER20216, Fri, 22 May 2026 15:55:23 +0000) ---
It seems that I hadn't paid enough attention back then to the "only_pool" reference in the description.

I think that syntax [pool: pool1,!pool2] is a very goof option. That means that an object is pinned to pool1 and it is forbidden to be pinned in pool2. And with any keyword we can define the exclusivity to a pool, like [pool: pool1,!any].

So, assuming that the patch with lfs pool *pin commands will remain as it is now, 

first, we have to add -exclusive option to pin an object to a pool exclusively and a -deny option to prevent an object to be pinned in that pool. This can be implemented in the liblustreapi_pool by doing some changes in cYaml code to support the new syntax. Thus we can have a command like:

lfs pool pin --pool poo11 --exclusive file1

which translated as:

[pool: pool1,!any] 

or a command like:

lfs pool pin --pool poo11 --deny pool2 file1 

which translated as:

[pool: poll1,!pool2]

About the lfs mirror {create, extend, split, delete} and lfs migrate commands, after checking the lustre.pin and if there is any exclusive pool set then we can abort them with a warning.

--- Comment (adilger, Sun, 24 May 2026 05:56:02 +0000) ---
I think "--exclusive" is longer than necessary. It could be "--only", which is easier to type?  And maybe "--not[\-pool] POOL" is more clear than --deny.  Note that getopt_long() will allow abbreviated long options.

It should also be possible to use --pin and --not-pool POOL for "lfs setstripe" when creating a new file or set on a directory default layout.

--- Comment (JIRAUSER20216, Mon, 25 May 2026 11:29:09 +0000) ---
Since the diff would be big enough to put them all together, I suggest to make all the changes to separate patches, eg. one to introduce the new options in lfs pool pin commands, one for the new syntax of the xattr, and one for setstripe, mirror and migrate commands changes.

--- Comment (gerrit, Thu, 28 May 2026 14:05:33 +0000) ---
"Nikos Papakonstantinou " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/66252

Subject: LU-19712 llapi: Add new xattr syntax for lustre.pin

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: d6ee1f71fae8d37378232263bafca41f96e7b15b

--- Comment (gerrit, Thu, 28 May 2026 14:05:34 +0000) ---
"Nikos Papakonstantinou " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/66253

Subject: LU-19712 lfs: Add only and not-pool options to lfs pool pin

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: 3696feff5c1a840068f190cf6b6788d8adcf0177

--- Comment (gerrit, Thu, 28 May 2026 14:05:35 +0000) ---
"Nikos Papakonstantinou " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/66254

Subject: LU-19712 lfs: Add pin and not-pool options in setstripe

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: 2efdfab03302e580fa65ae7f9817421659db7a63

--- Comment (gerrit, Thu, 28 May 2026 14:05:35 +0000) ---
"Nikos Papakonstantinou " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/66255

Subject: LU-19712 lfs: Prevent migrate/mirror on exclusive pinned files

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: 5a042ec0c9989a26a434f8c867f1c62948e15d92

--- Comment (adilger, Thu, 28 May 2026 20:13:45 +0000) ---
I'm getting errors when entering this into the YAML validator that I use, so it makes sense to have this reviewed by someone who knows that side of things.

It looks like ! is YAML unfriendly, but - would be OK. Also, the parsing of pool: pool1,pool2,-pool3 looks like it has pool: pool1 as one item, and pool2 and -pool3 as two separate items.

If it is a single pool (the most common case), then pool: pool1 would be fine (no quotes is cleaner), otherwise, pool: "pool1,pool2,-pool3" is needed. Similarly, for hsm/PCC pins, if a single value (most common) them hsm: 1 is OK (no quotes), but hsm: 1,2 would be needed.

https://yaml-online-parser.appspot.com/?yaml=%5B%20pool%3A%20%22pool1%2Cpool2%2C-pool3%22%20%5D&type=json

--- Comment (adilger, Tue, 2 Jun 2026 07:06:17 +0000) ---
npapakonstantinou, could you please look at the interop failure in the new sanity test_157/157a added in patch https://review.whamcloud.com/64751 ("LU-19712 llapi: Introduce pool pinning API").

https://testing.whamcloud.com/test_sets/57126e83-a562-4520-88a9-c3024604dbe0

It looks like a master client (with the new test_157 subtests) is failing against a 2.15 server, because the pool pin functionality was only added to master in commit v2_16_50-187-gaae909cf8f.

Please also fix the numbering of these two subtests to be "test_157a" and "test_157b".  Having "157" and "157a" means that it is impossible to run ONLY=157 and only run the first test, since that is used as a wildcard to mean "all test_157* subtests". I don't think the tests have been landed in master long enough that changing the names slightly now will matter in the long run.

== sanity test 157: llapi pool pinning API tests ========= 11:30:40 (1779967840)
llapi_pool_test: llapi_pool_test.c:66: create_empty_file: assertion '0' failed: lremovexattr(/mnt/lustre/d157.sanity/pool_t0) failed: Operation not supported
 test0 @1779967841: llapi_pool_pin_file/llapi_pool_unpin_file basic behavior ... fail (exit status 1)
llapi_pool_test: llapi_pool_test.c:66: create_empty_file: assertion '0' failed: lremovexattr(/mnt/lustre/d157.sanity/pool_t1) failed: Operation not supported
 test1 @1779967841: llapi_pool_pin_file already pinned ......................... fail (exit status 1)
llapi_pool_test: llapi_pool_test.c:66: create_empty_file: assertion '0' failed: lremovexattr(/mnt/lustre/d157.sanity/pool_t2) failed: Operation not supported
 test2 @1779967841: pool pins coexist with PCC hsm pins ........................ fail (exit status 1)
llapi_pool_test: llapi_pool_test.c:66: create_empty_file: assertion '0' failed: lremovexattr(/mnt/lustre/d157.sanity/pool_t3) failed: Operation not supported
 test3 @1779967841: llapi_pool_pin_fd, llapi_pool_unpin_fd and llapi_pool_is_pinned_fd  fail (exit status 1)
llapi_pool_test: llapi_pool_test.c:66: create_empty_file: assertion '0' failed: lremovexattr(/mnt/lustre/d157.sanity/pool_t4) failed: Operation not supported
 test4 @1779967841: llapi_pool_pin_fid, llapi_pool_unpin_fid and llapi_pool_is_pinned_fid  fail (exit status 1)

--- Comment (JIRAUSER20216, Tue, 2 Jun 2026 07:57:51 +0000) ---
The interop checks have been added in https://review.whamcloud.com/c/fs/lustre-release/+/64918. Do you want them in a separate commit? I can fix the test numbering also in the same commit.

I think the problem is that in version 2.15 there is not the lustre.pin. Probably it is trusted.pin, so I think the correct version is v2_16_51-30-g84100d7f7d in which lustre.pin introduced. In test test_157a we use plain setfattr command which failing because of the missing name (-n luster.pin).

--- Comment (adilger, Tue, 2 Jun 2026 15:21:04 +0000) ---
It doesns't look like 64918 is ready to land, so it probably makes sense to put those interop checks and subtest renaming into a separate patch (with appropriate Fixes: 84100d7f7d ("LU-10499 pcc: tunable GID to pin/unpin files") reference) so it can be reviewed and landed independently.  If 64918 becomes ready to land independently then we can -1/abandon the separate patch...

--- Comment (JIRAUSER20216, Wed, 3 Jun 2026 09:07:23 +0000) ---
I think the Fixes tag should include `86b0c90666bf069e8db125bb2f06cb4d1ffe8b17` since this commit introduced the problem.

--- Comment (gerrit, Thu, 4 Jun 2026 12:48:25 +0000) ---
"Nikos Papakonstantinou " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/66454

Subject: LU-19712 tests: add interop checks in pool pin tests

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: 82fa4f9d10feb95e38e62bfc53b291a5b19946d6

--- Comment (gerrit, Fri, 12 Jun 2026 22:24:41 +0000) ---
"Oleg Drokin " merged in patch https://review.whamcloud.com/c/fs/lustre-release/+/66454/

Subject: LU-19712 tests: add interop checks in pool pin tests

Project: fs/lustre-release

Branch: master

Current Patch Set: 

Commit: cc3c2298f2fddb13cefcfd87e60c88d11412d347

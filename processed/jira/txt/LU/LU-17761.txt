Key:        LU-17761
Summary:    sanity-compr test_cleanup: FAIL: test_sanity failed with 1
Link:       https://jira.whamcloud.com/browse/LU-17761
Type:       Bug
Priority:   Minor
Status:     Reopened
Resolution: Unresolved
Assignee:   Jian Yu
Reporter:   Jian Yu
Created:    Fri, 19 Apr 2024 19:05:42 +0000
Updated:    Mon, 27 Jul 2026 21:03:59 +0000
Links:      Related: is related to  LU-10026; Related: is related to LU-18713

--- Description ---
While running sanity-compr test_sanity() and test_sanityn(), if there was sub-subtest failure, then the test_cleanup would be incorrectly marked as FAIL.

We should leave it to the individual sanity/sanityn subtests to mark their failures, test_sanity() and test_sanityn() should not also return an error.

--- Comment (gerrit, Fri, 19 Apr 2024 19:15:12 +0000) ---
"Jian Yu " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/54855

Subject: LU-17761 tests: make sanity-compr sanity/sanityn return 0

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: 96767ff8af44b5dac0677db759634515de1d1802

--- Comment (gerrit, Tue, 21 May 2024 18:40:15 +0000) ---
"Oleg Drokin " merged in patch https://review.whamcloud.com/c/fs/lustre-release/+/54855/

Subject: LU-17761 tests: make sanity-compr sanity/sanityn return 0

Project: fs/lustre-release

Branch: master

Current Patch Set: 

Commit: 1fcfd6e62e80673ecadda43e22892867bd60fd02

--- Comment (pjones, Tue, 21 May 2024 20:08:42 +0000) ---
Merged for 2.16

--- Comment (gerrit, Thu, 21 May 2026 20:36:29 +0000) ---
"Andreas Dilger " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/66072

Subject: Revert "LU-17761 tests: make sanity-compr sanity/sanityn return 0"

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: 423b62e896032f11629f41b8ca44fd40914d3165

--- Comment (gerrit, Sat, 23 May 2026 16:51:48 +0000) ---
"Andreas Dilger " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/66095

Subject: LU-17761 tests: fix sanity-compr/1000 extent ratio

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: cde267eab1d764b183014b47a1a13c5ce9c95e00

--- Comment (adilger, Thu, 28 May 2026 20:56:31 +0000) ---
Still seeing this failing in sanity-compr test_1000.

--- Comment (adilger, Thu, 28 May 2026 21:06:58 +0000) ---
bzzz I was looking at the allocations for one of the test failures (review-dne-subtest-change) which is being run in isolation:

https://testing.whamcloud.com/test_sets/a82ba2c7-7cd6-43c6-af7e-09dee55969d1

It appears that the dense=0 file starts by allocating a bunch of blocks (60) contiguously, and then starts allocating them discontiguously (66), while the reverse is true for the dense=1 file (about 30 discont + 17 cont), so the test fails with "dense (48) should have fewer extents (67)".  In my test patch there are consistently fewer fragments in the dense=1 case (around 40), while the dense=0 case varies between 80-120 so it isn't hitting the 3x ratio consistently, with a few just below 2x ratio.

I might be able to make the test pass more consistently by allocating more blocks to each file, but I'm wondering if this is exposing some kind of bug in the mballoc code?  Is there some kind of "allocation history" or "use up current allocations" (either at the block or object level, for either the dense or sparse allocator) that needs to be done until the new setting takes effect?  This should be handled immediately on a per-file basis depending on whether the client is sending compressed/sparse writes, but it seems like something is not quite right.

--- Comment (bzzz, Sat, 30 May 2026 17:41:58 +0000) ---
adilger there is a diff between exa6 and master branch:

@@ -3718,6 +3730,7 @@ int ldiskfs_mb_init(struct super_block *
 	sbi->s_mb_max_to_scan = MB_DEFAULT_MAX_TO_SCAN;
 	sbi->s_mb_min_to_scan = MB_DEFAULT_MIN_TO_SCAN;
 	sbi->s_mb_stats = MB_DEFAULT_STATS;
+	sbi->s_mb_stream_request = MB_DEFAULT_STREAM_THRESHOLD;
 	sbi->s_mb_order2_reqs = MB_DEFAULT_ORDER2_REQS;
 	if (!sbi->s_mb_c1_blocks)
 		sbi->s_mb_c1_blocks =
@@ -5543,8 +5566,8 @@ static void ldiskfs_mb_group_or_file(str
 		inode_pa_eligible = false;
 
 	size = max(size, isize);
-	if ((ac->ac_o_ex.fe_len >= sbi->s_mb_small_req) ||
-	    (size >= sbi->s_mb_large_req))
+	/* Don't use group allocation for large files */
+	if (size > sbi->s_mb_stream_request)
 		group_pa_eligible = false;
 
 	if (!group_pa_eligible) {
--- ldiskfs/ldiskfs.h	2026-05-30 20:04:23.626242991 +0300
+++ ../head1/ldiskfs/ldiskfs.h	2026-05-30 20:34:01.380466202 +0300
@@ -1530,6 +1531,7 @@ struct ldiskfs_sb_info {
 	/* tunables */
 	unsigned long s_stripe;
 	unsigned int s_mb_max_linear_groups;
+	unsigned int s_mb_stream_request;
 	unsigned long s_mb_small_req;
 	unsigned long s_mb_large_req;
 	unsigned int s_mb_max_to_scan;

which explains why exa6 passes the test.

--- Comment (adilger, Tue, 2 Jun 2026 20:55:35 +0000) ---
bzzz, is this missing from some patch ported between the branches?  It looks like ext4-prealloc.patch is explicitly removing the usage of s_mb_stream_request and replacing them with s_mb_small_req and s_mb_large_req, but maybe it is time to get rid of that patch?  I don't think we have every used that functionality.

--- Comment (adilger, Mon, 27 Jul 2026 21:03:59 +0000) ---
It appears that the ext4-mballoc-dense.patch is missing from newer kernel series like el9, el10, ubuntu and the sanity-compr.sh test_1000 subtest is skipped in this case.

Key:        LU-19199
Summary:    Ensure all fast recovery features are enabled and working
Link:       https://jira.whamcloud.com/browse/LU-19199
Type:       Task
Priority:   Medium
Status:     Open
Resolution: Unresolved
Assignee:   Colin Faber - TLC
Reporter:   Colin Faber - TLC
Created:    Tue, 22 Jul 2025 16:50:23 +0000
Updated:    Tue, 18 Aug 2026 20:03:05 +0000
Labels:     fast_recovery
Links:      Related: is related to  LU-13643; Related: is related to  LU-10911; Related: is related to  LU-17819; Related: is related to  LU-13932; Related: is related to LU-20135; Related: is related to LU-20359; Related: is related to LU-18681

--- Description ---
We should verify that all known fast recovery type features are enabled and working correctly.  There are a number of existing Lustre features that could be used (and further improved) to reduce recovery time.

Depending on the IO model of the applications running on the cluster (e.g. shared file writers in a monolithic MPI application vs. independent "ensemble" processes working on their own files and directories) it should be possible to "tune" recovery to be more responsive, and potentially avoid waiting for unresponsive clients if they are not using directories or files of interest to the recovered clients.  The complex part is to automatically determine if clients have overlapping domains of interest or not.

--- Comment (adilger, Wed, 23 Jul 2025 19:13:33 +0000) ---
Candidates in this list would include:

	better stats for recovery times so we can measure improvements (client reconnect time histogram, RPC recovery/sec, LDLM lock recovery, waiting for client eviction) - LU-18681

	reduce ldiskfs MMP recovery time - LU-13932

	COS (Commit-on-Share) to ensure that there are no dependencies between clients during recovery (improve recovery concurrency, reduce evictions), but may impact runtime performance (which may be preferable to some customers over a longer recovery window)

	VBR (Version Based Recovery) to allow clients & servers to recover filesystem modifying operations independently of each other, instead of depending on the RPC transno to sequence client recovery

	parallelize RPC recovery/resend with VBR and multiple server threads (AFAIK RPC recovery is currently single-threaded on the server)

	start RPC recovery and LDLM lock replay as soon as client connects (as much as possible, taking dependencies into account but maybe none with COS) while waiting for other clients to reconnect (related to COS, VBR and parallel replay above)

	active server->client reconnect once initial client reconnection is done and server is waiting for clients

	tune client reconnect backoff time so that they retry connections to servers more aggressively after MGS notification

	fault tolerant MGS would allow IR to always be used even when "primary MGS" would be offline - LU-17819

--- Comment (jinshan, Sat, 15 Aug 2026 19:15:23 +0000) ---
adilger How likely would this be implemented without  sacrificing performance?

We're thinking about a feature called 'graceful recovery' that will be useful to make Lustre recover fast when we know we're going to restart the servers for various reasons. We'd like to share a design doc once we finish it. I am calling it out here to avoid duplicate effort.

--- Comment (adilger, Tue, 18 Aug 2026 20:03:05 +0000) ---
Hi Jinshan, we already have code for LU-20359 internally, just haven't pushed it to master yet due to the backlog for landing other patches, so I would recommend not spending time on "safe recovery during planned failover".  There are definitely other areas in unplanned failover that need investigation, as enumerated above (VBR, COS, parallel recovery replay, server->client reconnect, etc.).

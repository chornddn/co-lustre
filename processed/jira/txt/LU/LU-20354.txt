Key:        LU-20354
Summary:    FLR-ECRO: cache quota usage on QMT0000
Link:       https://jira.whamcloud.com/browse/LU-20354
Type:       Improvement
Priority:   Medium
Status:     Open
Resolution: Unresolved
Assignee:   Keguang Xu
Reporter:   Andreas Dilger
Created:    Thu, 11 Jun 2026 01:29:01 +0000
Updated:    Mon, 10 Aug 2026 06:14:37 +0000
Labels:     FLR-EC-RO, LMR
Links:      Related: is related to  LU-17820; Related: is related to  LU-18823; Related: is related to  LU-20212; Related: is related to  LU-15419

--- Description ---
The "lfs quota" operations are blocked when an OST is offline.  I see this regularly with my home Lustre system where the OSTs are on HDDs that have spun down to save energy.  The "lfs quota" operation fetches the quota usage information from each MDT and OST directly upon request, and the command does not return until the HDDs are spun up, which means there is no cache of the usage information on the QMT.

Having offline OSTs is expected to be a more common occurrence with FLR-ECRO deployments, and to the largest extent possible the filesystem operations should not block in this case.

For proper operation when an OST is (temporarily) offline, it would be useful for QMT0000 to maintain a (lazy) cache of the quota usage of each target.  This has two benefits:

	it maintains filesystem accessibility for "lfs quota" commands when an OST is undergoing failover or is permanently offline.

	it allows estimating how much quota usage was on that OST before it went offline for purposes of future quota grants (i.e. the OST's quota usage is not just "lost" when it is inaccessible).

In most cases the OST quota usage does not need to be totally uptodate, since the quota enforcement is itself not exact.  If the OST is online, then the QMT and client should prefer quota usage from the actual target.  If the OST is offline, then the cached quota usage should be "good enough", since the space used by that OST cannot change while it is offline (excluding OST reformat or e2fsck correcting errors).

This means that having a cache of the OST usage that is asynchronously written to the QMT0000 for each ID should be enough.  It does not need to be synchronous or recovered in case of failure, since it can be updated the next time the OST is online, or if the OST is permanently removed from the filesystem.

--- Comment (sergey, Mon, 15 Jun 2026 09:54:08 +0000) ---
When print_obd_quota goes through the list of the targets and prints an error if target is not available. In a such case it could get a cached usage for this target from the QMT.

Simple approach that could be used as "phase 1" is to get target granted space from the QMT. It is already implemented as the QMT stores space/inodes granted by quota IDs for each target separately. The drawback back is that sometimes granted space could be much larger than the actual usage.

In fact, there is already QUOTA_DQACQ_FL_REPORT request used by targets to notify QMT of quota overruns. Quota usage is sent in qb_usage field in this case. In other cases(acquire/release), qb_usage is not used. In theory, this field could be used in all quota requests sent to the QMT. This would allow us to maintain usage information at the QMT.

Slave target index files are already at the QMT to store granted space per target. These index files could be used to maintain the usage as well:

struct lquota_slv_rec { /* 8 bytes */
        __u64 qsr_granted; /* space granted to the slave for the key=ID,
                            * in #inodes or kbytes
                            */
++++    __u64 qsr_usage;
};

--- Comment (JIRAUSER19404, Wed, 17 Jun 2026 13:06:17 +0000) ---
Hi, following is the procedure of `lfs quota`,

1. RPC to MDS/QMT: Fetches global quota limits and grace time.

2. Iterate OSTs: Issues Q_GETOQUOTA to collect individual block usage, aggregating them into dqb_curspace on the client side.

3. Iterate MDTs: Issues Q_GETOQUOTA to collect and aggregate inode usage on the client side.

To cope with EC-RDONLY OST offline issue, we could just add another step

4. Consult QMT for Offline OSTs's granted space. Just as scherementsev "phase 1" suggested.

To further refine the accuracy of the offline OST's usage (since granted space can be much larger than actual usage), we could implement a heuristic optimization: calculate the average Usage-to-Grant ratio sampling from some currently online OSTs, and apply this ratio to the offline OST's granted space to extrapolate a more realistic estimate.

I am not a fan of maintaining a persistent usage cache directly on the QMT by piggybacking qb_usage on all quota requests (QUOTA_DQACQ_FL_REPORT, ACQ, and RELEASE) and serializing it into struct lquota_slv_rec.

Aside from the fact that this usage data is not up-to-date, this approach introduces additional architectural complexity: modifying lquota_slv_rec alters the on-disk layout, which demands rigorous  backward compatibility handling.

Given that our primary goal under FLR-ECRO is simply to provide a "good enough" estimate to prevent lfs quota from blocking or returning incomplete data, adding Step 4 offers a much simpler, low-risk, and cost-effective alternative.

--- Comment (adilger, Thu, 18 Jun 2026 07:41:43 +0000) ---
Hi Keguang, I think in the case of not-yet-inactive OSTs that have just failed, we want to make sure that the GETQUOTA RPCs do not hang for a long time. As such, these RPCs should be allowed to time out but not retry these RPCs. 

I'm not exactly sure of the quota implementation details, but my thought would be to have the fallback to the quota grant usage to happen immediately after the OST RPC fails/times out. That localizes the error handling, and does not require storing some extra state about which OSTs to retry. However, that is just a high-level opinion and maybe does not match the way the code is implemented. 

On the one hand, it may not be ideal to store a persistent record of the quota usage on QMT0000 since it could become stale, however it might also have some advantages. This cache on QMT0000 would allow the client to avoid potentially hundreds of extra RPCs for statfs() calls when project quota is enabled on a directory tree (which is basically always for multi-tenant systems that are increasingly common today. In some cases, applications are calling statfs() frequently, and while we have a client-side per-ID cache, it still means GETQUOTA calls to all MDTs and OSTs every few seconds from every client calling statfs(). If the quota master cached the aggregated quota usage per ID then the clients only need to do a single GETQUOTA call to get the full limits, reducing the RPC count by a factor of hundreds.

--- Comment (sergey, Thu, 18 Jun 2026 11:20:49 +0000) ---
I am not a fan of maintaining a persistent usage cache directly on the QMT by piggybacking qb_usage on all quota requests (QUOTA_DQACQ_FL_REPORT, ACQ, and RELEASE) and serializing it into struct lquota_slv_rec.

As said before there is already a field qb_usage. It is always used for QUOTA_DQACQ_FL_REPORT. In case of other requests(ACQ, RELEASE), it is set with zeroes. It's pretty easy to always set it before sending ACQ and RELEASE as well.

Aside from the fact that this usage data is not up-to-date, this approach introduces additional architectural complexity: modifying lquota_slv_rec alters the on-disk layout, which demands rigorous  backward compatibility handling.

I suggested to modify lquota_slv_rec for better optimisation: qmt_dqacq0 would modify one file instead of two. It could be stored in a separate index file if there is a problem with the backward compatibility. This could be even stored in a memory.

Note, since we start storing the actual usage at the QMT the usage will be always more accurate than the granted space: It's impossible to change granted space without a request from the quota slave.

If it is required for FLR, we could use the granted as I suggested but as "phase 1". Keeping in mind, that there is a better solution.

adilger , I would agree that having the usage on the QMT gives us a serious advantage especially for the case of statfs. Part of this task is already implemented in https://review.whamcloud.com/c/fs/lustre-release/+/65511. Now it returns granted space only for the LQA, but it could return usage for regular quota IDs if it will be implemented.

--- Comment (JIRAUSER19404, Thu, 18 Jun 2026 12:15:35 +0000) ---
Hi scherementsev , I generally would avoid serializing(store) somewhat stale data (usage) into disk, especially when we could inference it through its neighbours. However, if we just store them in memory, that sounds more reasonable.

Another concern here is, how fresh could we provide from the Cache, given that qb_usage is piggybacked through QUOTA_DQACQ_FL_REPORT/ACQ/RELEASE, there might be a balance between correctness and performance ?

--- Comment (sergey, Thu, 18 Jun 2026 15:58:49 +0000) ---
Hi squalfof , 

I think this needs some kind of small HLD or good explanation how it is going to be implemented.

I generally would avoid serializing(store) somewhat stale data (usage) into disk, especially when we could inference it through its neighbours. However, if we just store them in memory, that sounds more reasonable.

Having that only in memory causes another problem. When QMT starts, it wouldn't know the usage.

Another concern here is, how fresh could we provide from the Cache, given that qb_usage is piggybacked through QUOTA_DQACQ_FL_REPORT/ACQ/RELEASE, there might be a balance between correctness and performance ?

The usage freshness would depend on the latest ACQ/REL/REPORT request.

 

The both approaches with usage and granted space have own benefits and drawbacks. Note, that granted space is 0 if limits are set to 0, i.e. the usage could be > 0, while granted space is 0.

I think a lot of things need to be discussed before starting implementation.

--- Comment (adilger, Thu, 18 Jun 2026 16:33:06 +0000) ---
For FLR-EC/mirror, the cached quota data will always be more accurate than not having any information at all if the OST is unavailable. I suspect it will still be fairly accurate, within one qunit per inaccessible OST because that is the granularity of OSTs requesting more qunits from QMT. Also, the quota usage cannot become too outdated if the OST is offline, so I'm not very worried about the accuracy of the cache. 

This is especially true since the quota usage reported to users is not "binding" in any sense. It is just a snapshot of the real usage and will be immediately outdated if the user is actively writing or deleting files. If the user is not active, then the cached usage should be fairly accurate.

--- Comment (sergey, Thu, 18 Jun 2026 17:19:53 +0000) ---
Following our previous discussion about the difference between granted and usage. In some cases the difference could be pretty high. This happens when the real usage is significantly below the limit:

[root@vm3 tests]# lfs quota -u quota_usr -vh /mnt/lustre
Disk quotas for usr quota_usr (uid 1000):
      Filesystem    used   bquota  blimit  bgrace   files   iquota  ilimit  igrace
     /mnt/lustre   30.3M       0k    100G       -       2        0       0       -
  lustre-MDT0000      0k        -      0k       -       2        -       0       -
  lustre-OST0000      0k        -      0k       -       -        -       -       -
  lustre-OST0001   30.3M        -     16G       -       -        -       -       -
Total allocated inode limit: 0, total allocated block limit:    16G

16G here is OST0001 granted space vs 30.3M actual usage.

 That's why I suggested to consider using qb_usage instead of granted space.

--- Comment (adilger, Thu, 18 Jun 2026 19:12:01 +0000) ---
I'm definitely more in favor of using and caching the actual usage instead of the grant. I suspect that given the large difference between usage and granted, the cached usage is always going to be more accurate than the grant or an estimate based on the grant. Again, since this is just a reported value and not anything to do with quota enforcement, I don't think it is critical that this be totally accurate if an OST is inaccessible.

--- Comment (JIRAUSER19404, Wed, 24 Jun 2026 05:39:16 +0000) ---
Hi adilger  scherementsev  hongchao.zhang, a simple HLD about the cache,

TL;DR: Add quota-usage cache (not serialize to disk) in QMT where clients consult for quota usage, cache itself needs warmup and may face cache-miss; in that case, QMT proxies RPCs to OSTs to fetch usage and populate the cache on behalf of clients.

PRE: we already have a workable mechnism to fulfill the lfs quota command as mentioned above.

Startup: At startup, QMT will read all index files into Cache, {qid, osts[grants, usage(missing at startup)], mdts[...]}. During this phase, query from clients are rejected and redirect to the legacy mech (to keep the design simple and consistent, we do not perform proxy RPCs at this stage). (should we continue serving ACQ/REL at this stage?).

Warmup: QSD piggybacks the usage field in their ACQ/REL/REPORT, which helps to warmup the cache.

Serve clients: On each query with UID/GID, QMT return the corresponding OSTs/MDTs like { osts:[grants, usage], mdts:[...] }. If usage is missing at this stage, QMT proxies RPCs to OSTs to retrieve quota usage on behalf of the clients, the populated cache avoids repeat RPCs for subsequent callers. Based on prior experience with stats(), the extra RPC load on MDS0 is acceptable here.

With this cache, we could significantly reduce quota queries to OSTs/MDTs and improve client query performance as suggested in the description of this ticket.

 

Open: about Cache, there's already a global Cache in QMT. It's indexed through ID. We may reuse it with a new field like 'ost_maps'  under each lqe.

--- Comment (adilger, Wed, 24 Jun 2026 05:59:20 +0000) ---
Rather than ignore requests before the cache is populated, it would be possible for QMT to do proxy RPCs to the other targets to populate its cache and return that to the client. That is the same RPC count, but will populate the cache for the next caller.

--- Comment (JIRAUSER19404, Thu, 25 Jun 2026 01:42:02 +0000) ---
Rather than ignore requests before the cache is populated, it would be possible for QMT to do proxy RPCs to the other targets to populate its cache and return that to the client. That is the same RPC count, but will populate the cache for the next caller.

In terms of the same RPC count, yes, it helps. However, this is a bit tricky: should we shift the burden from clients to QMT on MDS0, given that MDS0 already has heavy duties?

During startup, QMT does not yet have the full mapping relationship between quota IDs and OSTs. In that case, a quota query may fan out to all OSTs, which could mean thousands of RPCs in a large cluster.

After startup, when the system is in steady state, the fanout should be controllable in most cases. At that point, we may use proxy RPCs. Still, we probably need a threshold: if the number of missing quota entries is below this threshold, QMT should do the proxy RPCs on behalf of clients; otherwise, clients should still take the responsibility for querying the OSTs. (In the latter case, maybe we could maintain a specific thread on QMT, recording the missing ones and fetching them in the backgroud asynchonously).

--- Comment (adilger, Thu, 25 Jun 2026 12:19:45 +0000) ---
In general, every ID will have objects on every OST, so the famous is exactly the same in each case. We already have STATFS proxy from the clients to an MDT and it does the RPCs to the OSTs, so I don't think this is a huge increase in load. Also, there is the goal of splitting QMT0000 to be distributed across all MDTs in the future, and that would naturally distribute the load.

--- Comment (gerrit, Thu, 23 Jul 2026 06:05:48 +0000) ---
"Keguang Xu " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/67482

Subject: LU-20354 qmt: add per-slave quota usage cache for GETQUOTA

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: e7e77d2f712d55c1a1823a8647c4497aa2f15834

--- Comment (gerrit, Thu, 30 Jul 2026 12:21:17 +0000) ---
"Keguang Xu " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/67642

Subject: LU-20354 qmt: reshape qmt_sarray for MD/DT membership

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: f3b5d50495321af7fd8a202cc1d04b0720f3ed85

--- Comment (gerrit, Mon, 3 Aug 2026 09:49:59 +0000) ---
"Keguang Xu " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/67700

Subject: LU-20354 qmt: add per-slave DT usage cache for GETQUOTA

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: 2256a4140b285e50df33a6a9469e13df80905916

--- Comment (gerrit, Fri, 7 Aug 2026 12:19:12 +0000) ---
"Keguang Xu " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/67810

Subject: LU-20354 quota: parallelize usage-cache GETOQUOTA

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: 1bb2b157c999da0b6a74f6525e0249fc4c1a4869

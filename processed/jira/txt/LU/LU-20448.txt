Key:        LU-20448
Summary:    posix_acl leak in llite DEFAULT-ACL fetch path
Link:       https://jira.whamcloud.com/browse/LU-20448
Type:       Bug
Priority:   Minor
Status:     Open
Resolution: Unresolved
Assignee:   Sohei Koyama
Reporter:   Sohei Koyama
Created:    Fri, 3 Jul 2026 06:16:56 +0000
Updated:    Mon, 6 Jul 2026 04:25:45 +0000

--- Description ---
ll_get_acl_common() in lustre/llite/acl.c leaks one posix_acl object per default-ACL fetch that reaches the get path. The ACL_TYPE_DEFAULT branch calls posix_acl_dup() on a fresh acl that already has refcount 1 and no other owner; the caller releases only one reference, orphaning the object.

 

Reproduction

D=/mnt/lustre/acltest; mkdir -p $D
setfacl -d -m u:1000:rwx $D          # default ACL
loop(){ for i in $(seq $1); do
          lctl set_param ldlm.namespaces.*mdc*.lru_size=clear >/dev/null
          getfacl $2 $D >/dev/null; done; }
# measure kmalloc-96 (grep '^kmalloc-96 ' /proc/slabinfo) around each,
# with `echo 3 > /proc/sys/vm/drop_caches` before/after to free legit caches
loop 20000 -d          # DEFAULT (leaking path)
loop 20000 --access    # ACCESS  (control)

Result

 

 

 Loop (N=20000)
kmalloc-96 delta after drop_caches

DEFAULT (getfacl -d)
+19989 (~1 leak/call, ~1.9 MB)

ACCESS (getfacl --access)
+16 (noise, no leak)

found by commit-hunter-v1

--- Comment (gerrit, Fri, 3 Jul 2026 07:37:47 +0000) ---
"Sohei Koyama " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/67040

Subject: LU-20448 llite: fix posix_acl leak in default ACL fetch

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: 39157d3257f33b873b35698a73d9f8915dcbc6f4

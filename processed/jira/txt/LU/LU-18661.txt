Key:        LU-18661
Summary:    allow configuration of projname<->username mapping
Link:       https://jira.whamcloud.com/browse/LU-18661
Type:       Improvement
Priority:   Minor
Status:     Reopened
Resolution: Unresolved
Assignee:   WC Triage
Reporter:   Andreas Dilger
Created:    Tue, 21 Jan 2025 20:43:39 +0000
Updated:    Sat, 13 Jun 2026 04:16:07 +0000
Labels:     easy
Links:      Related: is related to  LU-18640; Related: is related to LU-13335

--- Description ---
It is common practice to use project quotas instead of user quotas in a Lustre filesystem to allow having different quotas for different parts of the filesystem and to allow the project quota accounting quickly determine space usage for users in different subdirectory trees (e.g. "/lustre/home/adilger", "/lustre/project/adilger", "/lustre/scratch/adilger"). In such configurations, it is common that the "/lustre/home" subtree map projid == userid to simplify ID management, while "/lustre/scratch/adilger" might be UID+100000 and "/lustre/project/adilger" might be UID+200000.

It should be possible to configure "lfs quota --projid PROJNAME", "lfs project --projid PROJNAME", and "lfs find --projid PROJNAME" and related commands to fall back to looking up the numeric UID for PROJNAME when a project name is not found in /etc/projid. This reduces configuration overhead for the project IDs, and would allow using a network database (e.g. LDAP or sssd in /etc/resolv.conf) for doing the "projid" lookups using the username.  This should be configurable with a directive in /etc/projid.

Options that have been discussed:

	store some directive as a comment in the file, like "# config: projname=passwd" or "# config: projname=group" that would redirect all future projname or projid lookups to use getpwnam()/getpwuid() or getgrnam()/getgrgid() as appropriate to look up the supplied name/ID. This has the advantage that it maintains compatibility with XFS, but means that all of the comment lines need to be parsed.

	store some directive as a "special" entry, like "*:passwd:use passwd entry for projid mapping" or "*:group:use group entry for projid mapping" (comments are optional and for reference only).

--- Comment (adilger, Wed, 21 Jan 2026 05:39:15 +0000) ---
Patch https://review.whamcloud.com/46144 ("LU-13335 utils: allow projid lookup by name") implemented username and group name lookups for project IDs using the "u:USERNAME" and "g:GROUPNAME" or "user:USERNAME" and "group:GROUPNAME" prefixes when specifying the project name to "lfs find", "lfs setquota", "lfs project", and "lfs quota".

That is likely both more flexible and clear for users.

--- Comment (gerrit, Tue, 26 May 2026 18:20:06 +0000) ---
"Andreas Dilger " uploaded a new patch: https://review.whamcloud.com/c/fs/lustre-release/+/66155

Subject: username mapping" class="issue-link" data-issue-key="LU-18661">LU-18661 docs: add user: and group: to project man pages

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: 34210f360bb20e6280edd112e6cbceabb95395f4

--- Comment (adilger, Tue, 26 May 2026 18:56:59 +0000) ---
The ability to use u:USERNAME or user.USERNAME for the PROJID mapping is useful.  

However, the current implementation does not allow lookup of the numeric PROJID->u:USERNAME mapping in tools like lfs quota.  It would be useful to have some kind of setting in the /etc/projid file that allows the reverse mapping for PROJIDs that cannot be found in the /etc/projid file, as previously described.  

It would be desirable to also allow "user.USERNAME" and "group.GROUPNAME" to match the behavior of Solaris.

--- Comment (adilger, Tue, 26 May 2026 19:01:42 +0000) ---
It would also be useful to allow a PROJID:USERNAME mapping with a constant offset (e.g. 1M or 2M):

*:passwd:use getpwuid()/getpwnam() for /home
*+1000000:passwd:passwd with 1M offset for /scratch
*+2000000:passwd:passwd with 2M offset for /project

--- Comment (adilger, Tue, 26 May 2026 19:08:51 +0000) ---
It would also be useful to allow a PROJID:USERNAME mapping with a constant offset (e.g. 1M or 2M):

*:passwd:use getpwuid()/getpwnam() for home/
*+1000000:passwd+S:passwd with 1M offset for scratch/
*+2000000:passwd+P:passwd with 2M offset for project/

There should be some way to disinguish those names with offsets during both get and set operations.  The existing u:USERNAME mapping works for the first case (direct mapping), but there should be some way to show these entries separately.  In the example above I've proposed +S and +P to indicate that u+S:USERNAME would map into the passwd+10000000 entry for "scratch/", and similarly with +P for project/.  This may need to be refined during implementation to confirm that this is working in a reasonable manner...

--- Comment (gerrit, Fri, 12 Jun 2026 22:31:05 +0000) ---
"Oleg Drokin " merged in patch https://review.whamcloud.com/c/fs/lustre-release/+/66155/

Subject: username mapping" class="issue-link" data-issue-key="LU-18661">LU-18661 docs: add user: and group: to project man pages

Project: fs/lustre-release

Branch: master

Current Patch Set: 

Commit: 5b96b37d7723e5cf4b7749988305e1bec904ac0f

--- Comment (pjo, Fri, 12 Jun 2026 23:48:54 +0000) ---
Merged for 2.18

--- Comment (adilger, Sat, 13 Jun 2026 04:16:07 +0000) ---
The man page update was only a small cleanup patch. This patch still needs the ability to do projid->name lookups for "u:username" PROJID values.

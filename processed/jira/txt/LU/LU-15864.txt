Key:        LU-15864
Summary:    OST_SETATTR replay can hit missing object
Link:       https://jira.whamcloud.com/browse/LU-15864
Type:       Bug
Priority:   Minor
Status:     Open
Resolution: Unresolved
Assignee:   Sonia Sharma
Reporter:   Alex Zhuravlev
Created:    Tue, 17 May 2022 09:38:11 +0000
Updated:    Mon, 22 Jun 2026 22:21:22 +0000

--- Description ---
the object OST_SETATTR replay operates on can be a subject to the orphan cleanup procedure - it's recreated when OST recovery is over. this way OST_SETATTR replay can get lost.

the similar problem can happen to OST_WRITE, but it's handled by explicit sync object recreation in ofd_preprw_write().

it makes sense to address a race when OST_WRITE arrives when OST recovery is over, but ophan cleanup is not (similar to LU-15863)

--- Comment (JIRAUSER19573, Mon, 22 Jun 2026 22:09:37 +0000) ---
The issue for OST_WRITE described in LU-15863is already taken care of by this below patch - 

https://review.whamcloud.com/c/fs/lustre-release/+/45459

But this fix looks like wouldn't cover the race for OST_SETATTR.

static int ofd_setattr_hdl() {
....
....
    fo = ofd_object_find_exists(tsi->tsi_env, ofd, &tsi->tsi_fid);
    if (IS_ERR(fo))
	GOTO(out, rc = PTR_ERR(fo));

    la_from_obdo(&fti->fti_attr, &body->oa, body->oa.o_valid);
    fti->fti_attr.la_valid &= ~LA_TYPE;    /* setting objects attributes (including owner/group) */

    rc = ofd_attr_set(tsi->tsi_env, fo, &fti->fti_attr, &body->oa);
    if (rc != 0)
        GOTO(out_put, rc);
...
....
}

In OST_SETATTR case, if the object is missing during replay, it can return -ENOENT before ofd_attr_set() is ever called. And there is no guarantee that this request will first go through the LVB init path that has the EINPROGRESS retry logic.

OST_SETATTR doesn't have an explicit recreate/retry logic like ofd_prepwr_write() that forces the write replay and thus leverages the fix through ofd_lvb change in the above patch.

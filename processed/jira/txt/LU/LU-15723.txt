Key:        LU-15723
Summary:    Add 'llapi_projid_fget()' and 'llapi_projid_fset()' to llapi
Link:       https://jira.whamcloud.com/browse/LU-15723
Type:       Improvement
Priority:   Minor
Status:     Open
Resolution: Unresolved
Assignee:   WC Triage
Reporter:   Aurelien Rausch
Created:    Wed, 6 Apr 2022 09:13:32 +0000
Updated:    Mon, 1 Jun 2026 18:55:49 +0000
Labels:     easy, patch
Links:      Duplicate: is duplicated by LU-20286; Related: is related to  LU-12480; Related: is related to  LU-19125; Related: is related to  LU-13335; Related: is related to LU-20207

--- Description ---
There are no simple interfaces for getting and setting the numeric projid of a file (either by pathname or by open file descriptor).  The lfs_projid_set() and lfs_projid_list() commands are implemented in lustre/utils/lfs_project.c and are geared toward mass updates, and the project_set_one() command calls ioctl(fd, FS_IOC_FSSETXATTR, fsx) and ioctl(fd, FS_IOC_FSGETXATTR, fsx) directly.   It would be useful to have simple llapi_proj*() interfaces to get and set the numeric projid on a file.

On Function Naming
There are already llapi_project_[f]getprojid() interfaces that are used to perform projnameprojid lookups from the /etc/projid file, so potentially llapi_projid_[f]{get,set}() seem reasonable, or possibly llapi_projid_{get,set}_at() would be better (to allow operating by relative filename against an open parent directory handle).

API usage/Testing
Once added, these new llapi_projid_*() functions should be used by lfs_project.c::project_{get,clear,list}_one(), liblustreapi_pfind.c::get_projid(), checkstat.c, and createmany.c code.  This should provide sufficient test coverage through existing subtests that dedicated test cases are not needed.

The liblustreapi_pfind.c::get_projid() usage would ideally be replaced by statx(STATX_PROJID) so that there is no need for the extra overhead to open(), ioctl(FS_IOC_FSGETXATTR), close() on each file scanned when --projid is requested.

Documentation
There should be new man3/llapi_projid_*()}} pages added to describe the new APIs.

--- Comment (adilger, Sat, 2 May 2026 22:02:28 +0000) ---
rausch, would you be able to rebase this patch. I'm currently unable to comment on Gerrit due to the Ubuntu/Launchpad service being offline. 

It would make sense to move this function over to liblustreapi_project.c to keep this code together.

--- Comment (gerrit, Thu, 14 Apr 2022 15:46:12 +0000) ---
"Aurélien Rausch " uploaded a new patch: https://review.whamcloud.com/47071

Subject: LU-15723 utils: add fget_projid() to llapi

Project: fs/lustre-release

Branch: master

Current Patch Set: 1

Commit: d0e1ba0473ad804e8040ab1157096f2343d91145

--- Comment (adilger, Wed, 6 Apr 2022 09:42:48 +0000) ---
A better long-term solution would be to add STATX_PROJID support to the upstream kernel, and then it could also be added the Lustre statx imlpementation.

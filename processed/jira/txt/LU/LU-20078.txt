Key:        LU-20078
Summary:    next_extent panic due to calling osc_extent_tree_dump without object lock held
Link:       https://jira.whamcloud.com/browse/LU-20078
Type:       Bug
Priority:   Medium
Status:     Resolved
Resolution: Fixed
Assignee:   Qian Yingjin
Reporter:   Qian Yingjin
Created:    Fri, 3 Apr 2026 08:42:51 +0000
Updated:    Fri, 18 Sep 2026 18:39:42 +0000
Resolved:   Fri, 18 Sep 2026 18:39:42 +0000
Links:      Related: is related to LU-20631; Related: is related to LU-20705

--- Description ---
The core problems are:

	osc_extent_tree_dump0() walks the extent rbtree without holding osc_object_lock().

	LU‑19014 added a new, high‑frequency call to osc_extent_tree_dump() in osc_cache_writeback_range() for the IO_PRIO_DIRTY_EXCEEDED path.

	next_extent() assumes the passed extent is still in the tree and asserts on RB_EMPTY_NODE(); this is reasonable for normal tree walkers under the object lock, but unsafe for an unlocked debug dump.

if (result == 0 && prio == IO_PRIO_DIRTY_EXCEEDED &&
    !active_ext_check && atomic_read(&obj->oo_nr_ios) &&
    obj->oo_npages > 0) {
    osc_extent_tree_dump(D_CACHE, obj);
    active_ext_check = true;
    GOTO(repeat, result);
}

This panic only happened when the client is enabled debug with cache (debug=+cache) and subsystem_debug with osc (subsystem_debug=+osc).

--- Comment (gerrit, Fri, 18 Sep 2026 16:53:33 +0000) ---
"Oleg Drokin " merged in patch https://review.whamcloud.com/c/fs/lustre-release/+/65074/

Subject: LU-20078 osc: call osc_extent_tree_dump() without object lock held

Project: fs/lustre-release

Branch: master

Current Patch Set: 

Commit: dd5d669438d2120564ace15a6fd9e455ddf4b9e5

--- Comment (pjo, Fri, 18 Sep 2026 18:39:42 +0000) ---
Merged for 2.18
